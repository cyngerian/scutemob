[package]
name = "mtg-engine"
version = "0.1.0"
edition.workspace = true

[dependencies]
im = { version = "15", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
proptest = "1"

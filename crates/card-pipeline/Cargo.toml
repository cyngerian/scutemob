[package]
name = "mtg-card-pipeline"
version = "0.1.0"
edition.workspace = true

[dependencies]
mtg-card-db = { path = "../card-db" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

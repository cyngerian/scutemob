[package]
name = "mtg-network"
version = "0.1.0"
edition.workspace = true

[dependencies]
mtg-engine = { path = "../engine" }
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["full"] }

# Decision-Point Surface Audit

<!-- last_updated: 2026-07-27 -->

**Date**: 2026-07-26
**Task**: `scutemob-148`
**Bug class**: A point where the Comprehensive Rules give a *player* a choice, but the engine
makes that choice itself — deterministically, legally, and invisibly — because no `Command`
carries the player's answer.
**Scope**: read-only audit of the whole decision surface. No engine, simulator, card-def or
test change. The only files this task writes are this document and its index line in
[README.md](README.md).
**Motivation**: the 2026-07-26 finding that triggered-ability targets are auto-selected
engine-side (`abilities.rs:7174-7500`, proposed seed OOS-M11-4; renumbered from OOS-M11-3 at collection — scutemob-147 independently claimed OOS-M11-3 for fuzzer long-game nondeterminism), plus M11-local building a
human-input game loop whose decision surface must be known to be complete before the loop is
designed around it.

> **Severity-tally disambiguation** — same convention as
> [layer-bypass-audit.md](layer-bypass-audit.md). The class-B and class-D findings catalogued
> here are this audit's own class. They are **not** the "0 HIGH / 2 MEDIUM / 6 LOW" engine
> tally in `CLAUDE.md` → Current State, which counts the milestone-review issue index. Both
> are correct at once. Nothing here is scheduled yet; §8 proposes a queue, it does not create
> one.

---

## 1. The invariant

Per [methodology.md](methodology.md) Step 1, the property this audit tests is stated
declaratively:

> **DP-INV**: For every decision the CR assigns to a player, the engine either (a) obtains
> that player's answer through a `Command`, or (b) refuses to admit into a game any card
> that would reach the decision.

Branch (b) already exists and is machine-enforced. Architecture Invariant 9 / SR-2 make
`validate_deck` reject any card whose `CardDefinition` is not `Complete`, and the reason
given in the source is exactly the one that matters here:

```rust
    /// The definition exists but is marked `Inert`, `Partial`, or `KnownWrong`
    /// (see `cards::Completeness`). Such a card would take actions the rules
    /// engine never emits, or emit actions the card does not have, producing a
    /// state history that cannot be correctly rewound. Surfaced here, at deck
    /// build, rather than silently misbehaving on turn 6.
```
— `crates/engine/src/rules/commander.rs:46-50`, enforced at `commander.rs:232`.

An engine that picks a player's target, keeps or bottoms their scried cards, or discards
their cleanup card produces precisely "a state history that cannot be correctly rewound",
because the history records a decision no player made. DP-INV is therefore the same
invariant Architecture Invariant 9 already asserts, applied to *choices* rather than to
*abilities*.

**The gate is narrower than the invariant.** `crates/engine/tests/core/effect_choose_gate.rs`
(SR-33) bars exactly three DSL variants from `Complete`:

| gate test | variant barred |
|---|---|
| `no_complete_def_uses_the_choose_stub` (`:84`) | `Effect::Choose` |
| `no_complete_def_uses_the_may_pay_or_else_stub` (`:109`) | `Effect::MayPayOrElse` |
| `no_complete_def_uses_the_add_mana_choice_stub` (`:149`) | `Effect::AddManaChoice` |

Every other auto-chosen decision ships `Complete`. That is the hole this audit measures.

---

## 2. Classification and method

### 2.1 The taxonomy (from the task brief)

| class | meaning |
|---|---|
| **A** | Player-hooked: the choice rides a `Command` field and the handler reads it and acts on it. |
| **B** | Deterministic auto-fallback: the engine picks legally, without player input. Legal game state, zero agency. |
| **C** | Gated/stubbed: the decision is unimplemented and the corpus gate keeps affected cards out of `Complete`. |
| **D** | Missing or wrong vs CR: the behaviour diverges from the rules, or a hook exists that is never honoured. |

### 2.2 Evidence standard

A site is class **A** only if the choice was traced from a `Command` field, through
`process_command`'s dispatcher, into the handler line that consumes it. "The `Command` has a
field" was never accepted as evidence — three findings below (`face_down_kind`,
`ChooseDungeonRoom`, `Command::OrderReplacements` in reply to a `WouldDraw` prompt) are
exactly cases where a field or command exists and is discarded.

Every CR number cited here was looked up against `.scryfall-cache/MagicCompRules.txt` via the
mtg-rules MCP. Two corrections to the task brief resulted:

- **Scry is CR 701.22, not 701.19.** CR 701.19 is *Regenerate*. Surveil is **CR 701.25**.
- **CR 602.2e does not exist.** `abilities.rs:1384` cites it; CR 602.2 has only 602.2a/b.
  See finding **DP-1**.

Line numbers were verified by reading the files, and the four delegated research legs were
spot-checked against source before their claims were adopted. One agent-reported count was
wrong and is corrected here: `Effect::MayPayThenEffect` appears in **11** effectively-`Complete`
defs, not 12 — `mana_vault`, `temur_sabertooth`, `call_of_the_ring`, `boggart_shenanigans`,
`roil_elemental`, `ezuri_stalker_of_spheres`, `ruthless_technomancer` and `vampire_gourmand`
are `partial` or `inert`.

### 2.3 Corpus-impact method

Counts below come from a regex sweep over `crates/card-defs/src/defs/*.rs`, **excluding
`defs/mod.rs`** (the module file, which is caught by the glob but is not a card) — 1,804 def
files. A def is "effectively `Complete`" if it says `Completeness::Complete` **or** carries no
`completeness:` line at all (the DSL default) — **1,139 files, exactly matching
`tools/authoring-report.py`.**

This is an *approximation*: it is a source regex, not the serde walk `effect_choose_gate.rs`
uses, so it will miss a variant reached only through a deeply nested generated ability and
will over-count a variant that appears only in a comment. (`Effect::Choose` is the live example
of both traps at once: the bare string appears in 119 files, **all** of them prose in doc
comments; actual code use — `Effect::Choose {` — is zero.) Word boundaries matter too:
`Discover\b` is the mechanic (CR 701.57) and deliberately does not match the card name
*Kindred Discovery*, which is unrelated and `inert` besides.

**Method, so §10's re-derivation instruction is actionable.** Each §3.1 row is a regex over the
file's source text; a def counts once per row it matches. Nineteen rows are a plain match on the
variant name as written in the table. Two are compound predicates on the same variant and must
both be included in the union:

- *targeted triggered ability* = `AbilityDefinition::Triggered` **and** `targets:\s*vec!\[\s*TargetRequirement::`
- *modal triggered ability* = `AbilityDefinition::Triggered` **and** `modes:\s*Some`

Runnable script: `scratchpad/count-classb.py` from this task's session
(`/tmp/claude-1000/-home-skydude-projects-scutemob--worktrees-scutemob-148/…/scratchpad/`).

---

## 3. Corpus impact

### 3.1 `Complete` defs containing at least one auto-chosen decision

> **SUPERSEDED (PB-DP10, `scutemob-158`).** The counts below are a 2026-07-26 source-regex
> estimate. The authoritative, machine-computed numbers are printed on every `cargo test` by
> `crates/engine/tests/core/decision_gate.rs::decision_site_reconciliation_report`
> (`core::decision_gate::decision_site_reconciliation_report`), enumerated from `all_cards()`
> per SR-36 rather than grepped, and ratcheted against silent growth by
> `no_complete_def_introduces_an_unrecorded_auto_chosen_decision` (T4) +
> `auto_chosen_complete_union_is_ratcheted` (T6). **Two CR cites below were wrong and are
> corrected here**: `DiscardCards … (CR 701.8)` — CR 701.8 is *Destroy*; discard is CR
> **701.9**, and CR **701.9b** is the load-bearing sentence PB-DP7 already used correctly for
> the cleanup discard. `PutOnLibrary … (CR 701.20)` — CR 701.20 is *Reveal*; there is no "put
> on library" keyword action, and the governing rules are CR **608.2d** (which cards) + CR
> **401.4** (their order). The `WheelDraw` needle has **no runtime key at all** — it is a
> *type* (`Effect::WheelHand { draw: WheelDraw }` serializes as `"draw": {"Fixed": …}`), not a
> variant of the walked tree; the `WheelHand` row is **NO-DECISION** (the whole hand is
> discarded, so the pick order the sibling `DiscardCards` row worries about is unobservable),
> not AUTO. As measured 2026-07-27 (PROTOCOL 31 / HASH 68, `mtg-engine` on this branch): 4 rows
> are **SERVED** (`triggered_targets` 77, `search_library` 73, `scry` 16, `surveil` 8), 2 are
> **GATED** at a hard zero by `effect_choose_gate.rs` (`choose_stub`, `may_pay_or_else`), 1 is
> **NO-DECISION** (`wheel_hand`), and the remaining 15 rows are **AUTO-CHOSEN** — see the live
> test output for exact per-row counts. The still-auto union (this table's non-served rows,
> unioned rather than summed) is **97** `Complete` defs, frozen in `decision_gate.rs`'s
> `BASELINE`; the all-rows union (this row's own 277 analogue, unioned across all 22 rows
> including the served ones) is **267**.
>
> **This gate is bounded, and the bound is worse than it sounds.** Every row above is a
> predicate over a *DSL variant name*, so it can only see a decision someone actually
> encoded as a decision-bearing variant. A card whose "may"/"choose" was dropped at
> authoring time — never expressed as `MayPayThenEffect`, `Choose`, a mode, or anything
> else — produces a serialized tree with no trace of the choice and hits zero rows,
> forever. This is not hypothetical: PB-DP10's own closing review (2026-07-27) found
> Smuggler's Copter authored as an unconditional `Sequence(DrawCards, DiscardCards)` for a
> printed "you **may** draw a card" (CR 118.12) — it appears in `BASELINE` at all only
> because of the incidental `DiscardCards`. **That class is strictly worse than any row in
> this table**, because a recorded auto-choice is at least a legal outcome, while a dropped
> "may" is not. See **OOS-DP10-9**.
>
> **Per-row reconciliation, audit estimate vs. the gate's measured count (fix-review
> cycle, 2026-07-27):**
>
> | row | audit estimate | measured | mechanism |
> |---|---:|---:|---|
> | `triggered_targets` | 84 | 77 | compound row: file-level regex conjunction (audit) vs. per-`AbilityDefinition::Triggered`-node qualification (gate) |
> | `search_library` | 74 | 73 | unexplained, ±1, within regex noise |
> | `proliferate` | 25 | 25 | n/a — unit-variant string matching, exact |
> | `discard_cards` + `wheel_hand` | 23 | 13 + 10 | the audit's row bundled two runtime predicates; the split is exact: 13 + 10 = 23 |
> | `scry` / `surveil` | 16 / 8 | 16 / 8 | n/a |
> | `sacrifice_permanents` | 11 | 11 | n/a |
> | `may_pay_then_effect` | 11 | 10 | unexplained, ±1, within regex noise |
> | `choose_color_or_type` | 10 | 10 | n/a |
> | `look_at_top_or_route` | 10 | 10 | n/a (the count agrees; whether every counted def carries a real choice does not — see OOS-DP10-6's upper-bound caveat) |
> | `counter_unless_pays` | 7 | 7 | n/a |
> | `modal_trigger` | 5 | 4 | unexplained, ±1, within regex noise |
> | `change_targets` / `bolster_amass` | 3 / 3 | 3 / 3 | n/a |
> | `put_on_library` | 3 | 1 | the audit's regex counted the variant name inside `Completeness` note strings (`witchs_cottage.rs`, `gravepurge.rs`), not real `Effect::PutOnLibrary` usage — only `brainstorm.rs` is real |
> | `connive` | 2 | 1 | same mechanism: `spymasters_vault.rs` mentions `Effect::Connive` only inside a `partial(...)` note; only `raffines_informant.rs` is real |
> | `discover` | 1 | 1 | n/a |
>
> Three deltas are left honestly unexplained rather than assigned a plausible-sounding
> mechanism this suite has not verified — see §6's note on why an honest gap beats a
> plausible story here.
>
> **And the headline figure, 277 → 267.** The two are *unions*, not sums, so the row deltas
> above do not add up to the gap: a def matching three rows moves three row counts and one
> union slot. The 10-def difference is the per-row drift **after de-duplication** — dominated
> by `triggered_targets` (−7, the compound-row mechanism) and by the `put_on_library` (−2) and
> `connive` (−1) note-string false positives, with the three ±1 residuals and the overlap
> between rows accounting for the remainder. The honest statement is that **267 is measured
> and 277 was estimated**; the mechanisms above explain the direction and the bulk of the
> magnitude, and no attempt was made to force the two numbers to agree (plan §11 P6: the gate
> is the instrument, the table is the estimate).

| decision site | defs | effectively `Complete` |
|---|---:|---:|
| targeted triggered ability (CR 603.3d) | 104 | **84** |
| `SearchLibrary` (CR 701.23) | 108 | **74** |
| `Proliferate` (CR 701.34a) | 35 | 25 |
| `DiscardCards` / `WheelHand` / `WheelDraw` (CR 701.9/701.9b) | 32 | 23 |
| `Effect::Scry` (CR 701.22a) | 20 | 16 |
| `SacrificePermanents` (CR 701.21a) | 21 | 11 |
| `MayPayThenEffect` (CR 118.12) | 19 | 11 |
| `ChooseColor` / `ChooseCreatureType` | 22 | 10 |
| `LookAtTopThenPlace` / `RevealAndRoute` | 24 | 10 |
| `Effect::Surveil` (CR 701.25a) | 9 | 8 |
| `CounterUnlessPays` (CR 118.12a) | 7 | 7 |
| modal triggered ability (CR 603.3c) | 8 | 5 |
| `ChangeTargets` (CR 115.7d) | 5 | 3 |
| `PutOnLibrary` (CR 608.2d + 401.4) | 6 | 3 |
| `Bolster` / `Amass` | 5 | 3 |
| `Connive` (CR 701.50) | 3 | 2 |
| `Discover` (CR 701.57) | 1 | 1 |
| `MayPayOrElse` (CR 118.12a) | 12 | **0** ← gated |
| `AddManaFilterChoice` | 7 | **0** ← held by nothing until PB-DP10's T7 (see below) |
| `Effect::Choose` | 0 | **0** ← gated, unused |
| `TheRingTemptsYou` (CR 701.54) | 2 | **0** ← held by nothing until PB-DP10's T7 |

> **277 of 1,139 effectively-`Complete` defs — 24.3% — contain at least one decision the
> engine makes for the player.**

The four zero-`Complete` rows at the bottom are **not** a control group that "gets there two
different ways", as this table originally claimed. `MayPayOrElse` and `Effect::Choose` are
genuinely held out by the SR-33 gate, which works exactly as designed for the variants it
names. But `AddManaFilterChoice` and `TheRingTemptsYou` were held at zero by **nothing but
hand-authoring discipline** — the SR-33 gate bars the key `AddManaChoice`, a different serde
key than `AddManaFilterChoice`, and does not reach it at all. PB-DP10's `T7`
(`hard_zero_rows_have_no_complete_defs`) converts both into machine-checked hard zeros at zero
engine cost. The gate previously did not name the other seventeen rows at all; PB-DP10 closes
that gap at the corpus-measurement level (not the engine level — see the PB-DP10 row in §8).

### 3.2 Decisions that need no card at all

Six findings are in core turn structure and combat and are reachable in **every** game
regardless of decklist: priority after casting (**DP-1**), the mulligan (**DP-2**), cleanup
discard (**DP-3**), combat damage assignment (**DP-13**), same-controller trigger ordering
(**DP-14**), and the mana-pool spend order (**DP-26**).

---

## 4. Findings by decision class

Each row cites the site verified by reading. `crates/engine/src/` is elided from paths.

### 4.1 Spell casting (CR 601.2)

| choice | CR | class | site |
|---|---|---|---|
| Targets announced at cast | 601.2c | **A** | `rules/casting.rs:5805-5936` `validate_targets_inner`; two-pass slot fit + `enforce_inter_target_distinctness` (`:6119`); stored `:4522` |
| Mode announcement | 601.2b / 700.2a | **A** when supplied | `rules/casting.rs:3506-3554`; range, duplicate, `min_modes`, `max_modes` all checked |
| Mode announcement, **omitted** | 601.2b / 700.2a | **A** *(was **D**)* | **A since PB-DP3** (`scutemob-151`) — an omitted mode announcement is rejected before costs are determined or paid (`rules/casting.rs:3507-3620`). One CR-702.120a-scoped exemption survives: escalate with `count > 0` announces the mode *count* via `AdditionalCost::EscalateModes` and derives the identities `0..=count` (**OOS-DP3-1**); the derived count is itself bounds-checked. Free-cast producers that bypass `handle_cast_spell` are unaffected — **DP-20** / **OOS-DP3-3**. See **DP-4** |
| Value of X | 601.2b / 107.3 | **A** | `rules/casting.rs:3975-3985`; stored `:4588` |
| Alternative cost | 118.9 | **A** | `rules/casting.rs:148-177`, kind cross-checked `:840`. Caveat at `:405-406`: flashback/escape/madness are inferred from zone+keyword and cannot be declined — **B** |
| Kicker / multikicker | 702.33d | **A** | `rules/casting.rs:2670-2705`; single-kicker >1 rejected `:2675` |
| Prototype | 702.160 | **A** | `rules/casting.rs:2336-2412`, `:4561`, `:4636-4654` |
| Additional costs (sacrifice, discard, …) | 601.2b / 118.8 | **A** for the first | `rules/casting.rs:3310-3315` errors when a required cost is omitted |
| Additional costs, 2nd and later | 118.8 | **D** | `rules/casting.rs:3307-3309` — only `required_costs[0]` is validated, despite the comment claiming otherwise |
| Convoke / Improvise / Delve membership | 702.51 / 702.126 / 702.66 | **A** | `rules/casting.rs:5429-5542`, `:5561-5650`, `:5666-5726`. Empty vec = no reduction; no auto-tap |
| Which pip a convoke creature pays | 702.51a | **B** | `rules/casting.rs:5493-5522` — colored-first, first match in WUBRG order, generic only as fallback |
| Hybrid pip payment, **supplied** | 107.4e | **A** | `CastSpellData::hybrid_choices` (`rules/command.rs:683`) → `rules/casting.rs:3991` `flatten_hybrid_phyrexian(cost, &hybrid_choices, &phyrexian_life_payments)?`. Same hook on `ActivateAbility` (`command.rs:137`) via `abilities.rs:775-782` and on `TapForMana` (`command.rs:58`) via `rules/mana.rs:252-261`. Shipped by PB-RS2 for OOS-RS-2 |
| Hybrid pip payment, **omitted** | 107.4e | **B** by design | `crates/card-types/src/state/game_object.rs:238-284` — an unindexed `{A/B}` defaults to first colour `A`; `{2/C}` to the coloured half. A short vector is the deliberate contract; an over-long one is rejected loudly (`:198-206` hybrid, `:207-215` Phyrexian, with the rationale at `:187-197`) |
| Phyrexian pip payment, **supplied** | 107.4f | **A** | `CastSpellData::phyrexian_life_payments` (`rules/command.rs:689`), same three call sites as above |
| Phyrexian pip payment, **omitted** | 107.4f | **B** by design | `game_object.rs:287-309` — `unwrap_or(false)` ⇒ pay with mana, not life. Phyrexian-**hybrid** `{A/B/P}` paid with mana always takes colour `A`; no choice channel exists for that sub-case (`:297-304`) |
| Which pool mana pays a generic pip | 601.2h | **B** | `crates/card-types/src/state/player.rs:203-244` — coloured pips first, restricted before unrestricted, then generic in fixed order C→G→R→B→U→W |
| `face_down_kind` | 702.37c | **D** | `rules/casting.rs:67` binds `_face_down_kind` and never reads it; the kind is re-derived from the def at `:4661-4683` (Disguise > Megamorph > Morph) |
| Target **division** ("divided as you choose") | **601.2d** | **D** | Nothing anywhere: no `CastSpellData` field, no `Effect` variant, no resolution path. Sole in-corpus card `crates/card-defs/src/defs/fire_covenant.rs:25-37` is `Effect::Nothing`, marked `partial` |

The engine itself never taps a land. `grep -rn "solve_mana\|auto_tap" crates/engine/src/`
is empty; the only auto-tapper is `crates/simulator/src/driver.rs:202-209`, and it emits real
`Command::TapForMana` values through the normal surface. **Architecture Invariant 3 holds
here.**

### 4.2 Activated and loyalty abilities (CR 602.2b, 606)

| choice | CR | class | site |
|---|---|---|---|
| Targets | 602.2b / 601.2c | **A** | `rules/abilities.rs:459-481`, validated *before* costs are spent (`:432-433`) |
| X in an activation cost | 107.3k | **A** | `rules/abilities.rs:713-719`; `None` ⇒ 0 |
| Modes | 700.2a / 602.2b | **A** *(was **B**)* | **A since PB-DP3** (`scutemob-151`) — `rules/abilities.rs:337-398`. The `min_modes` bypass DP-4 named here was fixed in the same PB as its cast-path twin: an omitted mode announcement on a modal activated ability is now rejected before costs are spent. `min_modes: 0` correctly accepts an empty announcement and resolves **no** mode (the Spell path hard-rejects that shape instead — deliberate asymmetry, **OOS-DP3-2**) |
| Discard-as-cost card | 602.2 | **A** | `rules/abilities.rs:849-873`; hard error when required and absent |
| Sacrifice-as-cost permanent | 602.2 / 701.21a | **A** | `rules/abilities.rs:1005-1085`; filter, "another" (CR 109.1), and can't-be-sacrificed all checked |
| **Forage**: exile 3 from graveyard **or** sacrifice a Food | **701.61a** | **B** | `rules/abilities.rs:1162-1214` — Food branch wins whenever any Food exists; Food and graveyard cards both picked lowest-`ObjectId`-first |
| Loyalty ability targets | 606 / 601.2c | **A** | `rules/engine.rs:2533-2545`, validated before loyalty is spent |
| Loyalty −X value | 606.6 | **A** | `rules/engine.rs:2552-2566` |
| Loyalty once-per-turn | 606.3 | **A** | `rules/engine.rs:2491-2495`, set `:2576` |
| Loyalty **timing** | **606.3** | **D** | `rules/engine.rs:2451-2660` contains no `priority_holder`, no `active_player`, and no split-second check — see **DP-21** |
| Modal loyalty ability | 700.2a | **D** (narrow) | `rules/command.rs:615-626` — `ActivateLoyaltyAbility` has no `modes_chosen` field; unrepresentable |
| Forecast targets | 602.2b | **D** | `rules/abilities.rs:1725-1748` wraps `targets` into `SpellTarget` with no `validate_targets_*` call at all — no requirement, count, hexproof, shroud or protection check |
| Bloodrush target | 207.2c | **D** (narrow) | `rules/abilities.rs:1845-1880` — hand-rolled checks; protection/hexproof/shroud not consulted |

### 4.3 Mana and special actions

| choice | CR | class | site |
|---|---|---|---|
| Colour of "add one mana of any colour" at a mana ability | 106.1b / 605.3b | **A** | `Command::TapForMana { chosen_color }` (`rules/command.rs:38-66`), required when `any_color`, rejected when not |
| Same colour choice inside `execute_effect` | 106.1b | **C** | `effects/mod.rs:2272-2286` adds `{C}` and ignores `count`; gated by `effect_choose_gate.rs:149/195` |
| Filter-land 3-way mana choice | 605.1a | **B** | `effects/mod.rs:2289-2299` — always one of each colour; AA and BB unreachable. All 7 filter lands are `known_wrong` (PB-RS2) |
| `AddManaOfAnyColorAmount` / `AddManaMatchingType` / `AddManaOfChosenColor` | 106.12a / 614.12 | **B** | `effects/mod.rs:2388`, `:2410`, `:2430` — colourless fallback |
| `PlayLand` | 305.1 | **A** | `rules/lands.rs:25`, priority checked `:31` |
| Foretell / Plot / Suspend / Cycling | 702.143 / 702.170 / 702.62 / 702.29 | **A** | `rules/foretell.rs:41-90`, `rules/plot.rs:48-122`, `rules/suspend.rs:52-146`, `rules/abilities.rs:1417-1472` — each checks priority and pays through `pay_cost` |
| `CrewVehicle` creature set | 702.122a | **A** | `rules/abilities.rs:8676-8761` — duplicate, self-crew, battlefield, controller, untapped, is-creature, power threshold |
| `SaddleMount` creature set | 702.171a | **A** | `rules/abilities.rs:8819+`, plus a proper sorcery-speed gate at `:8846-8864` |
| `ActivateNinjutsu` returned attacker | 702.49a | **A** | `rules/abilities.rs:2145-2259` — every clause checked |
| `ActivateCraft` material set | 702.167a | **A** for selection, **D** for legality | `rules/engine.rs:1343-1393`; but `:1357-1366` accepts `ZoneId::Graveyard(_)` for **any** player and never checks `mat_obj.controller` — you can craft with an opponent's permanents |
| `TurnFaceUp` method | 702.37e | **A** | `rules/engine.rs:1469-1596` |
| `BringCompanion` | 702.139a | **A** | `rules/commander.rs:914+`; sorcery-speed + once-per-game enforced |
| `ChooseDungeonRoom` | **309.5a** | **C**/**D** | `rules/engine.rs:526-530` — `room` bound to `_`; the command validates, returns `Ok`, emits nothing, mutates nothing. Real pick at `rules/engine.rs:2215-2217` = `exits.first()`; first dungeon hardcoded `LostMineOfPhandelver` (`:2174-2178`) |
| Ring-bearer | 701.54a | **B** | `Command::TheRingTemptsYou` has no creature field; `rules/engine.rs:2293-2297` picks the lowest-`ObjectId` creature |

### 4.4 Mulligan and commander zone

| choice | CR | class | site |
|---|---|---|---|
| Take-or-keep | 103.5 | **A** | `Command::TakeMulligan` / `KeepHand`, dispatched `rules/engine.rs:245-257` |
| The **shuffle** inside a mulligan | **103.5** | **D** — **SHIPPED (PB-DP2)** | `rules/commander.rs:826-846` — see **DP-2** |
| `cards_to_bottom` **placement** | **103.5** | **D** — **SHIPPED (PB-DP2)** | `rules/commander.rs:911-915` — see **DP-2** |
| `cards_to_bottom` count | 103.5c | **A** | `rules/commander.rs:873-885` — `len() == mulligan_count.saturating_sub(1)` |
| `cards_to_bottom` relative order | 103.5 | **A** | iterated in vector order |
| Mulligan timing | 103.5 | **D** (minor) | dispatch has only `validate_player_exists`; a mulligan is legal on turn 5 |
| Commander graveyard/exile return | 903.9a | **A**, non-blocking | `rules/commander.rs:410-420` emits `CommanderZoneReturnChoiceRequired` and pushes `pending_commander_zone_choices`; both answer commands validate and are honoured. But the pending vector is consulted only by the re-emit suppressor, `state/hash.rs:7704`, and the two handler `retain`s — never by priority, SBA or turn progression |
| Commander zone-change model | 903.9a | **D** (documented) | Modelled as an SBA *after* the move, not a replacement. `state/builder.rs:1181-1184` states the choice; the commander genuinely enters the graveyard as a new object (CR 400.7) and `CreatureDied` fires first |
| Commander hand/library redirect | **903.9b** | **B** | `state/builder.rs:1199-1229` registers an unconditional `RedirectToZone(Command)`. One applicable effect ⇒ `AutoApply` ⇒ always redirects; the owner's "may" is dropped |
| Casting from the command zone / tax | 903.8 | **A** | `rules/casting.rs:254`, `:2654-2663`, `:4691-4695` |

### 4.5 Combat (CR 508, 509, 510)

| choice | CR | class | site |
|---|---|---|---|
| Attacker set + attack targets | 508.1 | **A** | `rules/combat.rs:66-184` (validation), `:613-618` (recorded). Validation-only: the engine never computes the maximal requirement-satisfying set for the player |
| Attack requirements (goad, must-attack) | 508.1d / 701.15b | **A** *since PB-DP4* — **this row was originally rated A in error; the true pre-PB-DP4 class was D** | `rules/combat.rs:272-432`. **This row's original "A" was wrong**: the requirement-yields-to-restriction carve-out at `:412-424` covered only `CantAttackOwner`, never `CantAttackYouUnlessPay`, so CR 508.1d's *"that player is not required to pay that cost"* was violated and a forced attacker plus an unpayable tax on every viable opponent **deadlocked** the declare-attackers step (declaring is illegal on the tax check, omitting is illegal on the must-attack check). PB-DP4 added `has_uncosted_attack_target`, used by both the goad and the `MustAttackEachCombat` blocks, closing **OOS-RS3-4**. Residual: the goad *directional* check (`:336-374`) still has no cost carve-out — **OOS-DP4-3** |
| **Attack cost** (Propaganda) | **508.1c** restriction; **508.1h/i/j** payment (was mis-cited **508.1g**, which is *optional* "as it attacks" costs — the Exert row below) | **A** *since PB-DP4* | `rules/combat.rs:185-265` (cost computation) + the debit in the mutation section — see **DP-10**. The total is built as a real summed `ManaCost` per defending player and debited via `casting::pay_cost` (CR 508.1j), colour-correct, with restricted mana correctly excluded (CR **106.6**). Two residual deviations: no CR 508.1i mana-ability window (**OOS-DP4-2**) and a hybrid/Phyrexian tax is rejected rather than paid (**OOS-DP4-1**) |
| Enlist | 702.154a | **A** | validated `rules/combat.rs:449-539`, tapped `:602-612`, persisted `:634-636` |
| Exert | 701.43d | **A** | validated `rules/combat.rs:551-587`, applied `:637-649`, consumed by untap `rules/turn_actions.rs:1123-1125` |
| Blocker set | 509.1 | **A** | `rules/combat.rs:783-1095` — a thorough restriction set incl. menace at `:1067-1095` |
| Block requirements | 509.1c | **A**, narrow | only Provoke-style `forced_blocks` (`rules/combat.rs:1104-1305`); no generic "must block if able" / Lure |
| **Cost to block** | 509.1 | **D** | no `GameRestriction` variant, no `Command::DeclareBlockers` field |
| Damage assignment **order** | 509.2 | **A** if sent, **B** if not | honoured `rules/combat.rs:1387-1441`; fallback `:1485-1510` iterates `OrdMap` ⇒ **ascending blocker `ObjectId`**. **Nothing ever asks**: there is no `BlockerOrderRequired` event and no `LegalAction` |
| Damage assignment **amounts** | 510.1a-d | **B** | `rules/combat.rs:1536-1596` — exactly lethal to each non-last blocker, all remaining power dumped on the last; trample assigns the *minimum* lethal (max trample-through, `:1557-1576`). No command exists |
| Deathtouch lethal = 1 | 702.2b/c | **A** | `rules/combat.rs:1546-1547` |
| Attack a battle | 506.1 | **D** | `AttackTarget` (`crates/card-types/src/state/combat.rs:14-19`) has only `Player` and `Planeswalker` (Battle subsystem deferred) |
| Banding | 702.21 | **C** | zero occurrences in `crates/engine/src` or `crates/card-types/src`; no `KeywordAbility` variant, so nothing can silently misbehave. Marker: `docs/mtg-engine-ability-coverage.md:195` |

### 4.6 Triggered-ability targets (CR 603.3d)

**Class B.** `rules/abilities.rs:7174-7500`, inside `flush_pending_triggers`
(`abilities.rs:6950`). For `PendingTriggerKind::Normal` and `CardDefETB`, the ability's
`TargetRequirement`s are read from layer-resolved characteristics and satisfied by
first-match scan:

```rust
            // CR 603.3d: For CardDef-based triggered abilities (Normal / CardDefETB),
            // look up the target requirements from the ability definition and
            // auto-select legal targets using deterministic first-match fallback.
            // If any required target has no legal candidate, skip this trigger.
```
— `abilities.rs:7178-7181`

Exact rules by requirement kind:

- `TargetPlayer` / `TargetCreatureOrPlayer` / `TargetAny` / `TargetPlayerOrPlaneswalker`:
  **first non-lost, non-conceded opponent in `turn_order`, else the controller**
  (`abilities.rs:7249-7270`).
- `TargetOpponent`: first active opponent, **never** self-fallback (`:7276-7291`).
- Graveyard-card requirements: first match in `state.objects` iteration order, i.e.
  **ascending `ObjectId`** (`:7293-7362`).
- `UpToN`: contributes **zero** targets for a permanent inner requirement (`:7419-7422`).
- Battlefield permanents: first match in ascending `ObjectId` (`:7425+`), correctly filtered
  through `layers::expect_characteristics` and `validate_target_protection`
  (`:7434-7450` — CR 613.1f, so Humility is respected).

This is **CR-compliant**: protection/hexproof/shroud are honoured and a requirement with no
legal candidate removes the ability from the stack, exactly as CR 603.3d directs. It is not
a rules violation — it is a total loss of agency across **84 effectively-`Complete` defs**.
Modal triggers are the same shape: `abilities.rs:8398-8405` sets `modes_chosen = vec![0]`
(CR 603.3c), affecting 5 `Complete` defs.

**No hook exists.** The `Command` enum (`rules/command.rs:19-638`) has no trigger-target
variant, and `rules/events.rs` has no `TriggerTargetRequired` event.

### 4.7 Trigger stack ordering (CR 603.3b)

| choice | class | site |
|---|---|---|
| APNAP order **across** players | **A** (correctly deterministic; not a player choice) | `rules/abilities.rs:6963-6975`; `apnap_order` at `:8438-8452` rotates `turn_order` to start at the active player |
| Relative order **within** one player's batch | **B** | same `sort_by_key` — there is no secondary key and no second sort. `slice::sort_by_key` is stable, so same-controller triggers keep insertion order; insertion is always `push_back` over sweeps that iterate `state.objects` (an `OrdMap`). Effective rule: **ascending source `ObjectId`, then ability index, then sweep-call order** |

No `Command` and no `GameEvent` for trigger ordering exists.

### 4.8 Optional ("may") triggers and intervening-if (CR 603.4)

| choice | class | site |
|---|---|---|
| Costless "you may X" on a trigger | **D** | `AbilityDefinition::Triggered` (`crates/card-types/src/cards/card_definition.rs:338-365`) has **no** `optional` field. Cards are authored mandatory and marked `known_wrong` — e.g. `crates/card-defs/src/defs/consecrated_sphinx.rs:26-44`: `// Note: "you may" optional not in DSL — always draws`, `Completeness::known_wrong("'you may draw two cards' implemented as a mandatory draw")`. 19 defs carry such a marker |
| "May pay {cost}", tax shape | **C** | `Effect::MayPayOrElse` — `effects/mod.rs:3425-3428` discards `cost`/`payer` and always runs `or_else`. Gated; 0 `Complete` defs |
| "May pay {cost}", benefit shape | **B** | `Effect::MayPayThenEffect` — `effects/mod.rs:3432-3465` → `try_pay_optional_cost` (`:8531-8544`): **the only branch is affordability**, so the payer is force-fed life, sacrifices and discards whenever legal. Ungated; 11 `Complete` defs |
| "Counter unless controller pays" | **B** | `effects/mod.rs:3473-3483` — `cost` discarded, always counters. 7 `Complete` defs |
| Intervening-if at **resolution** | **A** | `rules/resolution.rs:2139-2156` re-evaluates the card-def `Condition` and removes the ability if it no longer holds. **PB-DP6 correction:** the cite above was stale by ~20 lines, and the row's **A** was half-earned — the closure built `EffectContext::new`, zero-filling `kicker_times_paid` and `x_value`, so `Condition::WasKicked` read **false at resolution too**. Repaired to `new_with_kicker` (`scutemob-154`) |
| Intervening-if at **queue time** | ~~D~~ → **A** | **CLOSED by PB-DP6 (`scutemob-154`).** All **14** card-def trigger-queue sites now gate on the intervening-if via one shared helper, `abilities::carddef_intervening_if_holds_at_queue_time` (`:9137`), paired with an exhaustive, no-wildcard `effects::condition_is_queue_time_evaluable`. The original cell was wrong in three ways: (1) the cites were stale by 300-400 lines (`replacement.rs:1446-1456` → `:1838`; `abilities.rs:6910-6916` → `:7018`), filed as **OOS-DP6-8**; (2) *"only two paths check it"* conflated two different mechanisms — the runtime `Option<InterveningIf>` (a 2-variant enum, `check_intervening_if`) was already gated at all 13 of its sites and was never the gap; DP-15 is only about the card-def `Option<Condition>`; (3) the audited roster of 3 sites was a fifth of the real one. Remaining known holes are **seeded, not open-ended**: OOS-DP6-1 (condition dropped by lowering — fails at *both* ends, needs a HASH bump), OOS-DP6-2 (the one deliberately-ungated Category-C site), OOS-DP6-5 / OOS-DP6-9 (`TurnFaceUp` and Haunt lack the *resolution* half) |

The queue-time gap produced a **false-positive trigger**: condition false when the ability
would go on the stack, true at resolution ⇒ this engine fired, real Magic never triggers.
The false-negative direction was handled by the resolution check. **Both halves now hold**
(CR 603.4 requires the check in both places, and PB-DP6 retained the resolution re-check
rather than treating the new gate as making it redundant — state changes between queueing
and resolution, so it does not).

**The framing above missed the third possibility, and it is the worst one.** A condition can
be *dropped entirely* rather than merely checked late: when a card-def triggered ability is
lowered into a runtime `TriggeredAbilityDef`, `intervening_if` is hardcoded `None` because
the two types are different, so the condition is checked in **neither** place. Confirmed by
execution at PB-DP6 close-out: this makes `aurelia_the_warleader` — `Complete` and
deck-legal — an **unbounded infinite-combat loop**. See **OOS-DP6-1**, which is the highest
open item this audit has produced outside Tier 0.

### 4.9 Resolution-time choices

There is **no resolution-time decision channel at all**. `execute_effect_inner` returns `()`
and cannot suspend; `GameState` has six `pending_*` fields and none of them is an
effect-choice; the `Command` enum has no general `MakeChoice`.

> **SUPERSEDED for the first three rows by PB-DP9 (`scutemob-157`).** There IS a
> resolution-time decision channel now: `GameState.pending_effect_choice` +
> `GameEvent::EffectChoiceRequired` -> `Command::AnswerEffectChoice`, backed by an
> **abort-and-replay** continuation for `resolve_top_of_stack` (clone at entry; an effect
> that needs an unanswered choice records the question and returns; the wrapper restores the
> clone wholesale and emits the question; the answer is banked and the resolution re-runs from
> the top). `execute_effect_inner` still returns `()` and still cannot suspend — that premise
> was correct and is exactly why the mechanism is an abort rather than a pause. **The channel
> generalises to every remaining row in this table for the cost of one `EffectChoiceQuestion`
> / `EffectChoiceAnswer` variant pair and zero new plumbing** — no new `Command`, no new
> `GameEvent`, no new `LegalAction`, no new `DecisionKind`, no new admission-gate entry. That
> reuse, not the three effects themselves, is PB-DP9's real deliverable.

| choice | CR | class | site & rule |
|---|---|---|---|
| Modal `Effect::Choose` | 700.2 | **C** | `effects/mod.rs:3419-3424` — always `choices.first()`; `prompt` inert. Gated; 0 defs use it |
| **Scry** keep-or-bottom | **701.22a** | ~~B~~ → **A** | **SHIPPED, PB-DP9 (`scutemob-157`)** — the scrying player announces the bottom/top partition and its order (CR 401.4). Also fixed in the same arm: CR **701.22b** (`Scry 0` was emitting a `Scried` event) and CR **400.7** (scry-to-bottom renumbered the cards, which never left the library). Roster **16**, confirmed by enumeration. Original finding: `effects/mod.rs:3072-3098` — **every** scried card goes to the bottom, ObjectId-ascending; "keep on top" is unreachable |
| **Surveil** keep-or-graveyard | **701.25a** | ~~B~~ → **A** | **SHIPPED, PB-DP9 (`scutemob-157`)** — the surveilling player announces the graveyard/top split. Roster ~~8~~ → ~~7~~ → **8** (enumerated; the first enumeration's tree walk was itself incomplete -- see the fix-cycle note in §8.1). Original finding: `effects/mod.rs:3101-3132` — **all** looked-at cards are milled, so Surveil N ≡ Mill N, which can also contribute to a CR 704.5b deck-out the player never agreed to |
| Library search pick | 701.23a/b/d | ~~B~~ → **A** | **SHIPPED, PB-DP9 (`scutemob-157`)** — the searching player announces the card found, and CR **701.23b**'s fail-to-find is reachable for the first time (with CR **701.23d**'s quantity-only search correctly excluded from it). Roster ~~74~~ → ~~69~~ → **73** (enumerated; the first enumeration's tree walk was itself incomplete -- see the fix-cycle note in §8.1). Original finding: `effects/mod.rs:3026` — `candidates.iter().min_by_key(\|&&id\| id.0)`, i.e. **lowest `ObjectId`** among filter matches |
| Discard-as-effect | 701.8 | **B** | `effects/mod.rs:8611-8619` `discard_cards` — lowest `ObjectId` in hand, repeated. "Reveal your hand, opponent chooses" has no representation at all |
| `WheelDraw` (wheels: Windfall, Wheel of Fortune, …) | 701.8 | **A** — no choice exists | `Effect::WheelHand` (`card_definition.rs:2503-2511`) discards the player's **whole** hand — `effects/mod.rs:698` and `:730` both call `discard_cards(state, p, hand_size, events)` — so the lowest-`ObjectId` pick order in the row above is unobservable. The `WheelDraw` enum (`:2538-2550`) only sizes the redraw (`ThatMany` / `Fixed` / `GreatestDiscarded`) — a count, not a player choice. 10 defs. Listed because the task brief named it; there is nothing to hook |
| Sacrifice-as-effect / edicts | 701.21a | **B** | `effects/mod.rs:8193-8206` `sacrifice_permanents_for_player` — `n` lowest `ObjectId`s. Under `EachPlayer` this systematically takes each player's earliest-entering permanent |
| Cascade "you **may** cast" | 702.85a | **B** | `rules/copy.rs:366-368` — no decline branch; the `if` is only the legality test. The free-cast also gets `targets: vec![]` (`copy.rs:389`) and `modes_chosen: vec![]` ⇒ mode 0 (`copy.rs:430`) |
| Discover "you may cast" | 701.57 | **B** | `effects/mod.rs:3837-3848` — always casts |
| `PlayExiledCard` (Hideaway payoff) | 702.75a | **B** | `effects/mod.rs:4361-4364` — always plays |
| Hideaway "exile one of them" | 702.75a | **B** | `rules/resolution.rs:5903-5904` — top card always |
| Exploit "you may sacrifice" | 702.110a | **B** | `rules/resolution.rs:3853-3854` — **always declines**; TODO at `:3835` names the missing `Command::ExploitCreature` |
| Riot: counter **or** haste | 702.136a | **B** | `rules/resolution.rs:904` — always +1/+1 counter; TODO at `:905` names `Command::ChooseRiot` |
| Proliferate "choose any number" | 701.34a | **B** | `effects/mod.rs:3732` — auto-selects **all** eligible. The doc at `:3718-3731` flags that this can kill its own controller via poison (CR 704.5c). 25 `Complete` defs |
| `ChangeTargets` "you may choose new targets" | 115.7d | **B** | `effects/mod.rs:6562-6566` — always declines when optional; when `must_change` (115.7a) picks the smallest `ObjectId` without re-checking the original `TargetRequirement` |
| Bolster tie-break / Amass which Army | 701.39a / 701.47a *(this row's cite was wrong — corrected 2026-07-27, PB-DP10; the source itself already had it right)* | **B** | `effects/mod.rs:2624-2628`, `:2738-2744` — lowest `ObjectId` |
| `ChooseCreatureType` / `ChooseColor` | 106.12 / 614.12a | **B** | `effects/mod.rs:3660-3664`, `rules/replacement.rs:1747-1760` — most common among **the controller's own** battlefield permanents, which is the wrong optimisation for "choose, then destroy the rest" shapes. **The tie-break was also NONDETERMINISTIC** on the two `ChooseCreatureType` sites until PB-DP9's fix cycle (`max_by_key` over a `HashMap`, and a lone Elf Warrior is already a tie); both are `BTreeMap`s now — see **OOS-DP9-10** |
| `LookAtTopThenPlace` `optional` flag | — | **B** | `effects/mod.rs:5076` destructures `optional: _`; the field is inert by construction. Winner picked lowest-`ObjectId` at `:5157`, and `place_cost` is paid even into a whiff (`:5100-5117`) |
| Coin flip / dice | 705.2 / 706.2 | **A** | `effects/mod.rs:3981-3997`, `:4002-4014` — seeded from `state.timestamp_counter`. Correctly deterministic for replay; not a choice |

### 4.10 Replacement-effect ordering (CR 616.1)

The zone-change path is genuinely correct and is the model the rest of the engine should
follow.

| choice | class | site |
|---|---|---|
| Ask-or-not condition | **A** | `rules/replacement.rs:82-122` `determine_action`. Exact rule: **ask iff `applicable.len() >= 2` AND (`self_ids.len() == 0` OR `self_ids.len() >= 2`)**. With ≥2 applicable and exactly one self-replacement it auto-applies — correct per CR 616.1a |
| Self-replacements first | **A** | `rules/replacement.rs:63-73` (CR 614.15/616.1a), reinforced by the two-pass ETB call order at `:1080-1107` |
| Correct chooser | **A** | `rules/replacement.rs:775-787` — affected object's **controller**, owner only as fallback; the comment explains why owner is wrong after a control change |
| Apply-one-then-recheck loop | **A** | `rules/replacement.rs:795-864`, `:954-978`, `:1034-1046` — CR 616.1f, with `already_applied` threading for CR 614.5 |
| Zone change genuinely **waits** | **A** | `rules/sba.rs:670-688` defers and pushes `PendingZoneChange`; `:529-533` / `:736-740` skip the object on later passes. Object-scoped, not game-scoped |
| Sender trust boundary | **A** | `rules/replacement.rs:139-193` — rejects a non-affected sender (`:163-172`) and any id not currently applicable via `find_applicable`, not mere existence (`:183-189`) |
| **ETB** replacement ordering | **B** | `rules/replacement.rs:1109-1120` — `NeedsChoice ⇒ choices.first()`. Tie-break: self-replacements first, then `state.replacement_effects` registration order. Defensible today (the shipped ETB modifications commute) but a CR 616.1 divergence in principle |
| Eight other `find_applicable` sites | **B** | Of 13 call sites only 5 route through `determine_action`. The rest apply **all** applicable effects in registration order and never ask. Two are semantically order-sensitive: `apply_damage_prevention` (`rules/replacement.rs:2387` — prevent-N vs redirect vs double interleave differently) and `apply_counter_replacement` (`:2878` — `DoubleCounters` before/after `HalveCounters` differ on odd counts). The other five commute |
| Regeneration vs umbra armor | **B** | `rules/sba.rs:546-558`, self-flagged MR-SR29-02; also `effects/mod.rs:1014` |
| **`WouldDraw` multi-replacement** | ~~**D**~~ → **A** | **SHIPPED (PB-DP5, `scutemob-153`).** There were **three** emit sites, not the two listed here: `rules/turn_actions.rs::draw_card`, the twin at `effects/mod.rs::draw_one_card` (renamed `draw_cards_for_player`) and a third the audit never named, `rules/replacement.rs::draw_card_skipping_dredge` (the post-dredge-decline path, reachable via `handle_choose_dredge(None)` and golden script `replacement/014`). All three now record a `GameState.pending_draws` entry; `handle_order_replacements` grows a draw arm; the answer completes the draw through the chosen order. See **DP-5** |

### 4.11 Cleanup and other turn-structure choices (CR 514.1)

| choice | CR | class | site |
|---|---|---|---|
| Hand-size discard | **514.1** | ~~**B**~~ → **A** — **SHIPPED (PB-DP7)** | `rules/turn_actions.rs::{cleanup_actions, handle_discard_to_hand_size}` — see **DP-3**. The active player now answers `Command::DiscardToHandSize` with the whole subset; the engine **blocks** in the cleanup step until it arrives. This is the first pending decision in the engine that genuinely gates progress |
| "Until end of turn" expiry | 514.2 | **A** | `rules/turn_actions.rs:1369-1370`; damage clear `:1348-1350`; pools `:1375` |
| No priority in cleanup | 514.3 | **A** | `crates/engine/src/state/turn.rs:63-65` |
| Extra cleanup round | 514.3a | **A** | `rules/engine.rs:1731-1762` + the non-advance guard at `:1678-1695`, with a 100-round cap |
| Echo / cumulative upkeep / recover pay-or-sacrifice | 702.30a / 702.24a / 702.59a | **A** plumbing, **A** enforcement *since PB-DP4* (with a stated CR 608.2d timing deviation) | `rules/engine.rs::force_resolve_overdue_payments`, hooked into `handle_all_passed`'s stack-empty branch — see **DP-11**. The three `pending_*` vectors are now consulted: an unanswered payment gets the CR 118.12a "didn't pay" branch at the first subsequent priority round that ends with an empty stack. Deviation: CR 608.2d wants the choice *during* the ability's effect; this engine defers it by one priority round (which is what preserves the CR 608.2g mana-ability window and keeps the choice a real one) |
| "You may choose not to untap" | 502.2 | **D** | `rules/turn_actions.rs:942-1150` untaps unconditionally; only static flags gate it |
| Vote / Council's Dilemma / Will of the Council | 701.36 | **D** | zero occurrences across `crates/engine/src` and `crates/card-types/src` |
| Legend rule: which to keep | 704.5j | **B** | `rules/sba.rs:960-965` — **highest** `ObjectId` is kept (most recently entered). Flagged MR-SR29-01 |

### 4.12 Priority itself (CR 117.3)

Not in the task's class list, but it is the decision every other decision hangs off, and it
is wrong. See **DP-1**.

---

## 5. Ranked B/D findings

One row — **DP-32** — is marked `A*`. Its *choice* is class A (honoured, never defaulted); it
is listed here because the surrounding machinery is not, and a reader ranking work items needs
to see it next to the rest. It is the only non-B/D row in this section.

Ranking dimension is **human-play impact**: how often a human at one seat of a 4-player
Commander game hits the site × how far the engine's pick is from what a player would choose ×
whether it ships in `Complete` cards or in core rules.

> **PB-DP10 note (`scutemob-158`).** The still-open class-B rows with a card-def corpus
> presence are now **baselined by the gate, not closed**: their `Complete` defs are frozen
> in `decision_gate.rs`'s `BASELINE` and ratcheted against silent growth (T4/T6), but the
> class does not change. This covers **DP-16** (sacrifice/discard edicts →
> `sacrifice_permanents` / `discard_cards`), **DP-17** (Proliferate → `proliferate`),
> **DP-18** (`CounterUnlessPays` → `counter_unless_pays`), **DP-19** (`MayPayThenEffect` →
> `may_pay_then_effect`), part of **DP-20** (Discover → `discover`; Cascade and
> `PlayExiledCard` have no dedicated §3.1 row), and part of **DP-25** (`ChangeTargets` →
> `change_targets`; ring-bearer → `the_ring_tempts_you`, held at a hard zero by `T7`, not
> baselined — no `Complete` def uses it; Riot/Exploit/Hideaway/legend rule have no dedicated
> row). **DP-13, DP-14, DP-26 and DP-31 have no corresponding §3.1 row at all** (combat
> damage assignment, trigger ordering, and cost/redirect micro-choices are global engine
> behaviours or `Command`-level choices, not a per-card `Effect` variant a serde walk can
> enumerate) and are therefore untouched by this batch's gate.

### Tier 0 — correctness class: the engine's behaviour diverges from the CR

Four of the five are class **D** — wrong, not merely un-consulted. **DP-3 is class B**, and
sits here because it is the only decision in the whole audit with *no `Command` at all* on a
turn-based action every human hits.

Reachability differs from severity and the two are deliberately not the same list: DP-1, DP-2
and DP-3 are core-reachable (no card required), while DP-4 needs one of three specific
`Complete` cards in the deck and DP-5 needs two `WouldDraw` replacements on the board. The
core-reachable set is §3.2's six.

**Correction (PB-DP5, `scutemob-153`):** DP-5's reachability was in fact **zero**, not merely
narrow — **no card in the 1,804-def corpus registers a `WouldDraw` replacement at all**, so
"needs two on the board" could never be satisfied from a legal deck. That does not change its
Tier-0 placement (the defect is in engine machinery every future draw-replacement card would
route through, and its planning surfaced **OOS-DP5-7**, which *is* live), but it is a caution
about this table's method: "which cards does it need" was answered from the finding's shape
rather than by enumerating the corpus, and the enumeration was cheap.

| id | class | finding | CR | site |
|---|---|---|---|---|
| **DP-1** | D | **SHIPPED (PB-DP1, `scutemob-149`).** Priority after casting / activating / a special action goes to the ACTOR, not the active player. CR 117.3c: "If a player has priority when they cast a spell, activate an ability, or take a special action, **that player** receives priority afterward." The comment at `casting.rs:4712` misquoted CR 601.2i as "Then the active player receives priority" (it actually reads "If the spell's controller had priority before casting it, they get priority"); `abilities.rs:1384` cited **CR 602.2e, which does not exist** (fixed to CR 602.2b). Verified breakdown of the "~20 sites" estimate: **14 Group-A sites** (of which 6 actually flip behaviour: `casting.rs:handle_cast_spell`, `abilities.rs::handle_activate_ability`/`handle_cycle_card`/`handle_activate_bloodrush`/`handle_ninjutsu`/`handle_crew_vehicle`; the other 8 are AP-gated identity writes), **3 Group-B sites ruled no-change** (`engine.rs:757/958/1072` — echo/cumulative-upkeep/recover reassign priority at resolution time, when no player holds it; comment-only fix, seeded OOS-DP1-1), **8 Group-D sites** (2 handlers already correct by construction, 4 missing the CR 117.4 `players_passed` reset, 3 missing the CR 116.3/117.3c grant — a fix-cycle review pass added the missing entry priority guard to all 3, closing part of OOS-DP1-2), and **5 confirmed false positives** that were never DP-1 (`engine.rs:1759`, `:1805` and `combat.rs:1373` are CR 117.3a turn-based-action/step-start grants; also two `handle_activate_loyalty_ability`/`handle_level_up_class` sites the original roster missed entirely). | 117.3c, 601.2i | `rules/casting.rs:4712-4715`; `rules/abilities.rs:1384-1387`, `:1552`, `:1753`, `:1967`, `:2102`, `:2341`, `:2504`, `:2681`, `:2857`, `:8791`, `:9000`, `:9202`; `rules/engine.rs:1461` (craft), `handle_turn_face_up`, `handle_activate_loyalty_ability`, `handle_level_up_class` |
| **DP-2** | D | **SHIPPED (PB-DP2, `scutemob-150`).** **A mulligan is a content no-op, and `cards_to_bottom` goes to the library's TOP.** `handle_take_mulligan` moves every hand card to the library — `Zone::insert` is `push_back` and `Zone::top()` is `v.last()`, so they land on top — emits a **phantom** `GameEvent::LibraryShuffled` with no permutation, then draws 7. **The same seven cards return, reversed.** Separately, `handle_keep_hand` bottoms cards with `move_object_to_zone` (top) instead of `push_front` (bottom), so the cards you bottom are the next cards you draw. This is the OOS-RS-1 top/bottom inversion class that PB-RS1 swept — the mulligan was not in its roster. Tests (`crates/engine/tests/rules/commander.rs:1400-1495`) assert hand counts and events only, never library position. **Both halves are now fixed**: `handle_keep_hand` bottoms with `move_object_to_bottom_of_zone` (`push_front`), so `cards_to_bottom` index 0 ends up ABOVE later entries and the pre-existing library — including its top card — is untouched; `handle_take_mulligan` runs a real seeded Fisher-Yates `Zone::shuffle` (seeded from the existing `state.timestamp_counter`, the MR-M7-17 idiom) after the hand→library moves and **before** both the `LibraryShuffled` event and the 7 draws, so the event is no longer phantom (Architecture Invariant 4). 4 fail-before/pass-after probes; PROTOCOL 27 / HASH 63 unmoved. Closes **OOS-M11-1**, widened per §7 to cover the `handle_keep_hand` half. **CR correction**: this row's original cite of `103.4b` was stale — live CR 103.4b is the *Vanguard starting life total*; both the shuffle and the bottoming live in a single sentence of CR 103.5, with 103.5c supplying the multiplayer free-first-mulligan adjustment (the engine's own source comments already cited 103.5 correctly). | 103.5, 103.5c | `rules/commander.rs:808-848` and `:886-890`; `crates/card-types/src/state/zone.rs:109`, `:159-164`, `:187`; `state/builder.rs:286` |
| **DP-3** | ~~B~~ → **A** | **SHIPPED (PB-DP7, `scutemob-155`).** Original finding: **cleanup discard has no `Command` at all** and auto-picks the **highest `ObjectId`** in hand — the most recently drawn card — one at a time. Hand is `Zone::Unordered` (an `OrdSet`), `object_ids()` yields ascending, `.last()` takes the top. Madness is correctly honoured on that path, so the auto-picker could involuntarily exile-and-Madness a card the player would never have chosen (three `Complete`, deck-legal defs were live-wrong: `fiery_temper`, `stensia_masquerade`, `markov_baron`). **FIXED**: `cleanup_actions` now recomputes CR 402.2 no-max-hand-size, and if the hand is over, records `GameState.pending_cleanup_discard: Option<PendingCleanupDiscard>`, emits **one** `GameEvent::CleanupDiscardChoiceRequired { player, count, hand }` and returns — the auto-discard `loop` is gone entirely. The active player answers `Command::DiscardToHandSize { player, cards }` with the **whole subset** (CR 514.1 is a single turn-based action that discards *n* cards; CR 703.4n; CR 701.9b gives the choice to the affected player), which `handle_discard_to_hand_size` validates (pending exists, sender matches, exact count, no duplicates, every id live and in the sender's own hand) and then performs in **ascending `ObjectId` order** regardless of the order supplied, so the subset's order cannot become a hidden channel into CR 603.3b madness-trigger ordering (**OOS-DP7-4**). Madness now fires only off a chosen card. **The generalisable deliverable is the gate, not the discard** — see §8's PB-DP7 row and the plan's §1. **Two premise corrections.** (1) The suite's framing of CR 514.1 as one simultaneous turn-based action covering the discard *and* the damage clear *and* the "until end of turn" expiry is **wrong**: CR 514.1 and CR 514.2 are two separate turn-based actions ("First… Second…"), only 514.2's items are simultaneous, and the pool empty is **CR 500.5 / 703.4q** — not part of CR 514 at all — so the pause sits at a **CR-supplied boundary**, not a modelling compromise, and nothing of CR 514.2 has run when it is taken. (The engine's own comments cited **CR 500.4** for the pool empty in five places, and PB-DP7's closing review caught that too: 500.4 is *step-begin* effect expiry, an unrelated rule. All five corrected, along with a `clear_damage` doc that cited 514.1 for what is 514.2 work.) (2) This row's cited line numbers were stale: the discard loop was at `:1305-1372`, not `:1280-1293`. **Wire: PROTOCOL 27 → 28 and HASH 64 → 65**, both gate-computed and both history tables append-only — §8's prediction confirmed on both halves. **0 card-def edits, 0 completeness flips**; 21 new tests, 9 existing tests re-pointed at the choice, tests 3,809 → **3,832** across two fix cycles. | 514.1 (+ 514.2, 500.4, 402.2, 701.9b, 703.4n, 702.35a) | `rules/turn_actions.rs::{cleanup_actions, handle_discard_to_hand_size, default_cleanup_discard}`; `rules/engine.rs::{BlockingDecision, blocking_decision, enter_step, process_command, handle_concede}`; `rules/command.rs::Command::DiscardToHandSize`; `rules/events.rs::GameEvent::CleanupDiscardChoiceRequired`; `crates/card-types/src/state/stubs.rs::PendingCleanupDiscard`; `crates/card-types/src/state/zone.rs:130-135` |
| **DP-4** | ~~D~~ → **A** | **SHIPPED (PB-DP3, `scutemob-151`).** The fix is a **validation lift**, not a bolted-on empty-check: the range / duplicate / `min_modes` / `max_modes` checks now run whenever the object is modal, so it also closes the auto-select across the other **37** `min_modes: 1` defs — the headline below understated the scope to the 3 commands. `rules/abilities.rs`'s activated-ability twin (§4.2) shipped in the same PB. A narrow CR 702.120a exemption keeps escalate's backward-compat path alive with its derived count bounds-checked (**OOS-DP3-1**); `resolution.rs`'s `vec![0]` fallback is **retained** because four free-cast producers build Spell stack objects without calling `handle_cast_spell` (`copy.rs:386` cascade, `copy.rs:614` discover, `resolution.rs:5167` cipher, `resolution.rs:5837` suspend — **OOS-DP3-3**). §8's "mirror the Spree guard" prescription was deliberately **not** followed: the Spree guard was kept (it fires earlier and owns the CR 702.172a message). **No wire change — PROTOCOL 27 / HASH 63 unmoved.** Blast radius: 3 test lines, 2 golden scripts, 1 harness line, the simulator/TUI callers, one SR-15 registry declaration, **0 card-def edits**. Original finding: **An empty `modes_chosen` bypasses `min_modes` entirely.** The range / duplicate / `min_modes` / `max_modes` checks live only inside the `!modes_chosen.is_empty()` branch. Cast-time target slicing then assumes `vec![0]` (`casting.rs:3645-3653`) and resolution re-derives `vec![0]` (`resolution.rs:335-341`). **Cryptic Command, Austere Command and Incendiary Command all declare `min_modes: 2, max_modes: 2` and all three are `Complete`** — cast one with no modes and it pays the full cost and resolves *one* mode, silently. The Spree path *does* hard-reject empty modes (`casting.rs:2940-2944`); the general modal path has no equivalent. | 601.2b, 700.2a | `rules/casting.rs:3506`, `:3536-3549`, `:3555-3559`, `:3645-3653`; `rules/resolution.rs:335-341`; `crates/card-defs/src/defs/cryptic_command.rs:31-32`, `austere_command.rs:27-28`, `incendiary_command.rs:37-38` |
| **DP-5** | ~~D~~ → **A** | **SHIPPED (PB-DP5, `scutemob-153`).** Original finding: **the `WouldDraw` multi-replacement prompt is unanswerable and the draw is destroyed.** `draw_card` emitted `ReplacementChoiceRequired` and returned early recording **no** pending state — there was no draw-pending field on `GameState` at all — while `handle_order_replacements` required a matching `pending_zone_changes` entry, so the `Command::OrderReplacements` the player was being asked for was rejected and the draw could never complete. **FIXED** by a new `GameState.pending_draws: Vector<PendingDraw>` (`{ player, already_applied, remaining, sets_has_drawn_for_turn }`, sorted `already_applied` for SR-9b determinism), recorded at **all three** emit sites, plus a `resolve_pending_draw` mirroring `resolve_pending_zone_change`'s CR 616.1f re-check. `handle_order_replacements` routes between a pending zone change and a pending draw **by applicability**, which is total rather than heuristic: `trigger_matches` only matches same-variant `ReplacementTrigger` pairs and ends `_ => false`, so the two candidate sets are provably disjoint and the tie-break is unreachable. Both SR-29 security checks survive on the draw arm (the sender must be the affected chooser; every ordered id must be *currently* applicable via `find_applicable`, not merely registered). **Three corrections to this row's own premises.** (1) **"Reachable with any two `WouldDraw` replacements" overstates the card exposure to zero**: **no card in the 1,804-def corpus registers a `WouldDraw` replacement** (the only corpus hit is an `inert` completeness *note* in `out_of_the_tombs.rs:32`), so DP-5 was unreachable from a legal deck and **0 card defs were edited**. The honest framing is "class-D engine defect + the precondition for ever authoring the `WouldDraw` family", not "live-wrong on a `Complete` def today". (2) **The damage was worse than filed**: because the draw *sequence* kept iterating after the deferral, `Effect::DrawCards { count: 3 }` emitted **three** unanswerable prompts and drew **zero** cards. `draw_cards_for_player` now owns the sequence and breaks on deferral per CR **614.11a**, stashing the remainder in `PendingDraw.remaining` for the resume to finish. (3) The existing test cited here (`tests/rules/replacement_effects.rs`) asserted only deferral and is now strengthened in place to assert the draw completes through the **chosen** order. **Deviations, stated:** a deferred draw does **not** suspend the rest of the effect — "draw three, then discard three" runs its second half against a hand that does not yet hold the drawn cards (**OOS-DP5-5**); the resume does not re-offer dredge on draws 2..N (**OOS-DP5-3**); a "draw 3 from an empty library" now makes one attempt rather than three (CR 121.3, outcome-identical — one `PlayerLost` instead of three); and nothing gates priority, SBAs or step advancement on `pending_draws`, so an unanswered entry leaves the game exactly as playable as before (hard constraint: no hang; **OOS-DP5-2** owns the eventual deadline). **The review's stress test found one real defect in the first implementation and it was fixed at the source**: the runner's claim that a single re-check is equivalent to the CR 616.1f loop is **false** in `determine_action`'s CR 616.1a branch — `AutoApply` is returned with 2+ effects still applicable when exactly one is a self-replacement, and a non-`SkipDraw` self-replacement then returned `Proceed`, performing the draw and silently dropping the remainder *including a `SkipDraw` that CR 616.1f says must then apply*. `check_would_draw_replacement` now runs its own bounded re-check, mirroring the sibling `WouldChangeZone` path. **§8's wire prediction is CONFIRMED: HASH 63 → 64** (forced by the gate, not hand-bumped; appended v64 `HASH_SCHEMA_HISTORY` row, no existing row edited), **PROTOCOL 27 unmoved**, no new `Command` / `GameEvent` / `Effect` variant — `Command::OrderReplacements` was reused as §8 predicted. 17 tests (13 + T10b + T14 + T15 + 1 strengthened), tests 3,781 → **3,797**. Seeds **OOS-DP5-1..8** filed below; the largest is **OOS-DP5-7**, a *live* free-card exploit on `Command::ChooseDredge` found while planning this PB and arguably higher-severity than DP-5 itself. | 616.1, 616.1a, 616.1f, 614.11, 614.11a (+ 121.3, 702.52a) | `rules/replacement.rs::{check_would_draw_replacement, perform_one_draw, resolve_pending_draw, handle_order_replacements, draw_card_skipping_dredge}`; `rules/turn_actions.rs::draw_card`; `effects/mod.rs::draw_cards_for_player`; `crates/card-types/src/state/replacement_effect.rs::PendingDraw`; `state/{mod,builder,hash}.rs` |

### Tier 1 — silently wrong in most games, driven by `Complete` cards

| id | class | finding | CR | `Complete` defs | site |
|---|---|---|---|---:|---|
| **DP-6** | B | Triggered-ability targets auto-selected by first-match (**OOS-M11-4**) — **SHIPPED, PB-DP8 (`scutemob-156`)** | 603.3d | ~~84~~ → **77** (derived by enumerating `all_cards()` per SR-36, not by grep; +21 more carry a non-`Complete` marker — the test that prints it is `pb_dp8_trigger_target_choice::test_dp8_roster_enumeration`. The audit's 84 and the PB-DP8 planner's grep-derived 74 are BOTH wrong) | ~~`rules/abilities.rs:7174-7500`~~ → `rules/abilities.rs:7308-7798` at PB-DP8 planning time (the cites here were stale by ~137 lines and the range undershot the arm's end; same class as OOS-DP6-8 — *a site cite in this document is a snapshot*) |
| **DP-7** | ~~B~~ → **A** | Library search picks the lowest `ObjectId` — every tutor fetches for you — **SHIPPED, PB-DP9 (`scutemob-157`)**. The searching player now announces the card found (CR 608.2d / 701.23a) through `Command::AnswerEffectChoice`, and CR **701.23b**'s fail-to-find is reachable for the first time. **The fail-to-find carve-out in the DP-7 brief was wrong**: it said failing to find is permitted "except where the search reveals the card", but CR **701.23e** makes revealing orthogonal to finding; the real carve-out is CR **701.23d**'s quantity-only search ("a card", "three cards"), which MUST find. Residual: `Effect::SearchLibrary`'s `reveal` field is still inert and CR 701.23h ("search twice before shuffling is one search") is unmodelled — **OOS-DP9-9** | 701.23a/b/d/e | ~~74~~ → ~~69~~ → **73** `Complete` (+**25** non-`Complete`), enumerated from `all_cards()` per SR-36. **The 69 was also wrong** and is corrected by the fix cycle: the shipped tree walk was hand-written and skipped `AbilityDefinition::{Spell,Triggered,Activated}::modes`, `AbilityDefinition::{SagaChapter,LoyaltyAbility}` and split-card halves entirely, plus `Effect::CoinFlip`. It is now a structurally complete serde walk of the whole `CardDefinition`. Newly found: Binding the Old Gods, Evolution Charm, Insatiable Avarice, Thirsting Roots (all `Complete`) + Tooth and Nail, Urza's Saga (`partial`). The printing test is `pb_dp9_effect_choice::test_dp9_roster_enumeration` | ~~`effects/mod.rs:3032`~~ → `:3026` at PB-DP9 planning time (this document's cite was stale; same class as OOS-DP6-8) |
| **DP-8** | ~~B~~ → **A** | Scry sends **all** cards to the bottom; "keep on top" is unreachable — **SHIPPED, PB-DP9 (`scutemob-157`)**. The scrying player now announces the bottom/top partition and its order (CR 701.22a, CR **401.4**). **Two corrections made in the fix.** (1) The engine emitted `Scried { count: 0 }` for a `Scry 0`, so a "whenever you scry" trigger could fire off one — CR **701.22b** says no scry event occurs; the surveil arm already had the mirror-image CR 701.25c guard. (2) Scry-to-bottom **renumbered every scried card** (`move_object_to_bottom_of_zone` mints a fresh `ObjectId` unconditionally) even though the card never left the library — a CR **400.7** deviation that also consumed `timestamp_counter`, the shuffle seed source. Replaced by a `Zone::reposition_within` permutation; **OOS-DP9-11** sweeps the other same-zone callers. **The default flipped**: the engine's deterministic default is now the IDENTITY (keep everything on top), not bottom-everything | 701.22a/b (+ 400.7, 401.4) | **16** `Complete` — confirmed by enumeration, and still 16 after the fix cycle repaired the walk; the only one of the three `Complete` counts this document got right. The non-`Complete` tail was NOT right: ~~+1~~ → **+3** (Retreat to Coralhelm, Wrenn and Seven newly found) | ~~`effects/mod.rs:3089-3098`~~ → `:3072-3098` at PB-DP9 planning time |
| **DP-9** | ~~B~~ → **A** | Surveil sends **all** cards to the graveyard; Surveil N ≡ Mill N — **SHIPPED, PB-DP9 (`scutemob-157`)**. The surveilling player now announces the graveyard/top split (CR 701.25a). Of the three rows this is the one with a genuine *correctness* argument beyond agency loss: cards leaving the library means `Surveil N` could contribute to a CR **704.5b** deck-out the player never agreed to. **The default flipped** to the identity (keep everything on top) | 701.25a/c/d | ~~8~~ → ~~7~~ → **8** `Complete` (+**1** non-`Complete`). The document's original 8 was right for the wrong reason: the shipped enumeration's incomplete walk dropped Connive // Concoct (a split card, `Complete`) and Kaito, Bane of Nightmares (a loyalty ability, non-`Complete`). Corrected by the fix cycle | ~~`effects/mod.rs:3123-3130`~~ → `:3101-3132` at PB-DP9 planning time |
| **DP-10** | **SHIPPED (PB-DP4, `scutemob-152`)** — was D | **Propaganda attack tax is checked but never charged.** The mana pool is inspected once (`combat.rs:250-253`) and never debited anywhere in the 600-line handler. Float the mana, attack free, keep the mana. Colour is also flattened to generic (`:218-227`). **FIXED**: the tax is now a real summed per-defender `ManaCost` debited in the mutation section (CR order 508.1f tap → 508.1h total → 508.1j pay); colour is preserved; **restricted mana no longer counts toward affordability** (CR **106.6** — `can_pay_cost`/`pay_cost` are called with `spell: None`, resolving the `total_with_restricted()`-vs-`spend(cost, None)` inconsistency in the strict direction; every `ManaRestriction` variant is spell-scoped, so this is correct and is a deliberate behaviour flip); a hybrid/Phyrexian/X tax is **rejected** rather than silently dropped (**OOS-DP4-1**; pre-PB-DP4 the pips summed to 0 and the attack was free — the OOS-RS-2 class). `GameEvent::ManaCostPaid` is reused, so **no wire change**. The in-code claim that interactive payment *"requires a new `DeclareAttackers` command field"* is **falsified and deleted** — the tax derives entirely from `state.restrictions` + the declared attackers. **CR cite corrected: 508.1g → 508.1c/h/i/j.** **CR 508.1d honoured**, closing **OOS-RS3-4**. `propaganda.rs` and `ghostly_prison.rs` (both `Complete`) stop being live-wrong with **0 def edits** | ~~508.1g~~ **508.1c / 508.1h/i/j** | — | `rules/combat.rs:185-265` + debit site |
| **DP-11** | **SHIPPED (PB-DP4, `scutemob-152`)** — was D | **Echo / cumulative upkeep / recover never enforce the "otherwise, sacrifice".** `resolution.rs:2785` asserts "The game pauses until a `Command::PayEcho` is received"; **no code implements that pause.** The three `pending_*` vectors are read only inside their own handlers — never by priority, SBA or step advancement. Pass priority and the permanent is neither paid for nor sacrificed. Compounding: none of the three has a `LegalAction`, so in a bot/M11 game the command is never sent. **FIXED** — and the design decision is the substance of this row: the vectors are now consulted by `force_resolve_overdue_payments`, called from `handle_all_passed`'s **stack-empty** branch, which applies the CR **118.12a** "didn't pay" branch to every unanswered payment before the game leaves that priority round. **§8's phrasing — "wire: none if the 'otherwise' is applied at resolution rather than gated on priority" — is confirmed, but the boundary chosen is neither of those two**: it is *the end of the priority round after* resolution, because (a) applying it **at** resolution destroys the CR 608.2d/608.2g choice and makes `Command::PayEcho` unreachable (contradicting the LegalAction requirement), and (b) **gating priority deadlocks** any seat that never sends the command — `driver.rs` answers a rejected command with a silent `PassPriority`, so a refused pass is an infinite retry with no error, strictly worse than the bug. Auto-**decline**, never auto-pay (CR 118.12a; auto-pay is DP-19's bug class and would spend mana the player was saving). APNAP order (CR 101.4), entries snapshotted before any handler mutates the vector. Extra-round-not-advance so dies-triggers land in the correct step (the CR 514.3a pattern); the extra round is guarded on `!payment_events.is_empty()` so an all-no-op sweep falls through. **Accepted deviation from CR 608.2d**: within the deferral window the permanent still exists (and a recover card still sits in its graveyard) though the CR would already have sacrificed/exiled it — it can be tapped for mana, sacrificed to another cost, targeted or blocked with; the *outcome* is not deviated from. Three `Complete` defs (`mogg_war_marshal`, `avalanche_riders`, `grim_harvest`) stop being live-wrong with **0 def edits**; `mystic_remora`'s `known_wrong` note becomes accurate. Residual: the deadline is postponable by keeping the stack non-empty (**OOS-DP4-12**). **PROTOCOL 27 / HASH 63 unmoved** | 702.30a, 702.24a, 702.59a (+ 118.12a, 608.2d/g, 101.4) | — | `rules/engine.rs::force_resolve_overdue_payments` + `handle_all_passed` stack-empty branch |
| **DP-12** | D | Costless "you may" on a trigger has **no DSL representation**; such cards are authored mandatory and marked `known_wrong` | 603.4 | 19 marked | `card_definition.rs:338-365`; `consecrated_sphinx.rs:26-44` |
| **DP-13** | B | Combat damage: amounts fully auto-computed (min-lethal, dump-the-rest-on-the-last, max trample-through); assignment order defaults to ascending blocker `ObjectId` and **nothing ever prompts** for `OrderBlockers` | 509.2, 510.1a-d | — | `rules/combat.rs:1485-1510`, `:1536-1596` |

### Tier 2 — real, narrower

| id | class | finding | CR | site |
|---|---|---|---|---|
| **DP-14** | B | Same-controller trigger ordering is a stable sort ⇒ ascending source `ObjectId`; no `Command`, no event | 603.3b | `rules/abilities.rs:6963-6975` |
| **DP-15** | ~~D~~ → **A** | **SHIPPED (PB-DP6, `scutemob-154`).** Original finding: intervening-if checked at resolution but **not at queue time** except ETB and graveyard paths ⇒ false-positive triggers. **FIXED** by one shared gate — `abilities::carddef_intervening_if_holds_at_queue_time` — wired at all **14** card-def trigger-queue sites (upkeep, first main, postcombat main, end step, begin combat, tap-for-mana ×2, ETB, tribute, cast-this-spell, exerted-attacks, combat-damage-to-player, turned-face-up, ring-tempts-you, graveyard), backed by an exhaustive `effects::condition_is_queue_time_evaluable` with **no `_` arm** (the SR-5 idiom, so a new `Condition` variant is a compile error until classified). **Four corrections to this row's own premises.** (1) **The roster was a fifth of the real one**: 3 named sites vs 14 actual, partitioned out of 82 `PendingTrigger::blank` call sites by a mechanical rule (does the enclosing block destructure `AbilityDefinition::Triggered`?). One of the three "sites" was a *comment*, not a site. (2) **Two mechanisms were conflated.** The runtime `Option<InterveningIf>` — a 2-variant enum checked by `check_intervening_if` — was already gated at all 13 of its call sites. DP-15 is exclusively about the card-def `Option<Condition>`. (3) **The ETB path the row credits as one of the two correct ones was itself broken, in the *opposite* direction.** Its gate built `EffectContext::new`, which zero-fills `kicker_times_paid`, so `Condition::WasKicked` was unconditionally **false** — `nullpriest_of_oblivion` and `thieving_skydiver` (both deck-legal `Complete`) never queued their kicked ETB trigger *at all*. That is a false **negative**, the failure mode this batch's hard constraint 3 exists to prevent, shipped inside the gate the audit held up as class-A. A review pass then found the *same* zero-fill in the resolution-time re-check, so a first fix would have made the trigger queue and then fizzle — net-unobservable. Both halves repaired. (4) **A third failure mode exists that this row does not describe**: a lowered trigger loses its condition entirely (**OOS-DP6-1**). **Yield: 0 completeness-marker flips and 0 card-def source edits**, as §8 predicted — 15 defs stop over-firing (11 observably; 3 are `partial`/`known_wrong` and deck-blocked), 2 start firing at all. Exactly 2 def caveats retired (`loyal_apprentice.rs`, `siege_gang_lieutenant.rs`); ~20 "re-checked at resolution" notes deliberately **kept**, because that sentence stays true. Hard constraint honoured throughout: the default at every site is **queue the trigger** — an unanswerable condition never suppresses. PROTOCOL **27** / HASH **64** unmoved; tests 3,797 → **3,809**; seeds OOS-DP6-1..10 filed in §8.1 | 603.4 | fixed: `rules/abilities.rs:9137` (helper), `:7018`, `:3764`, `:4084`, `:5064`, `:5948`, `:6003`; `rules/turn_actions.rs:310`, `:483`, `:561`, `:781`, `:1768`; `rules/replacement.rs:1838`, `:1873`; `rules/mana.rs:847`; `effects/mod.rs` (predicate); `rules/resolution.rs` (ctx repair) |
| **DP-16** | B | Edicts / sacrifice / discard effects pick lowest `ObjectId` (23 + 11 `Complete` defs) | 701.21a, 701.8 | `effects/mod.rs:8193-8206`, `:8611-8619` |
| **DP-17** | B | Proliferate auto-selects **all** eligible — can kill its own controller via poison | 701.34a | `effects/mod.rs:3732` |
| **DP-18** | B | `CounterUnlessPays` always counters (7 `Complete` defs) | 118.12a | `effects/mod.rs:3473-3483` |
| **DP-19** | B | `MayPayThenEffect` always pays when able (11 `Complete` defs) | 118.12 | `effects/mod.rs:3432-3465`, `:8531-8544` |
| **DP-20** | B | Cascade / Discover / `PlayExiledCard` always cast; cascade free-cast also gets no targets and mode 0. **PB-DP3 cross-reference (OOS-DP3-3):** four producers build `StackObjectKind::Spell` with `modes_chosen: vec![]` *without* calling `handle_cast_spell`, so PB-DP3's cast-time mode guard cannot reach them and `resolution.rs`'s `vec![0]` fallback must stay live until DP-20 is closed — `copy.rs:386` (cascade), `copy.rs:614` (discover), `resolution.rs:5167` (cipher copy), `resolution.rs:5837` (suspend). Whoever closes DP-20 owns retiring that fallback | 702.85a, 701.57 | `rules/copy.rs:366-368`, `:389`, `:430`; `effects/mod.rs:3837-3848`, `:4361-4364` |
| **DP-21** | D | Loyalty abilities: no priority check, no active-player check, no split-second check | 606.3, 702.61a | `rules/engine.rs:2451-2660` |
| **DP-22** | B | 8 of 13 `find_applicable` sites bypass `determine_action`; `apply_damage_prevention` and `apply_counter_replacement` are order-sensitive | 616.1 | `rules/replacement.rs:2387`, `:2878` |
| **DP-23** | D | Craft materials accept **any** player's battlefield permanents and graveyard cards | 702.167a | `rules/engine.rs:1357-1366` |
| **DP-24** | D | Accepted-and-discarded inputs: `ChooseDungeonRoom.room` bound to `_`; `CastSpellData.face_down_kind` bound to `_` and re-derived | 309.5a, 702.37c | `rules/engine.rs:526-530`; `rules/casting.rs:67` vs `:4661-4683` |
| **DP-25** | B | Fixed-branch resolution picks: Riot → counter; Exploit → decline; Hideaway → top card; ring-bearer → lowest `ObjectId`; legend rule → highest `ObjectId`; `ChangeTargets` → unchanged | various | §4.9, §4.11 |
| **DP-26** | B | Cost-payment micro-choices: generic pool spend order fixed (C→G→R→B→U→W, restricted first); convoke pip assignment colored-first WUBRG; forage prefers Food; `ExileOtherGraveyardCards` takes the `n` lowest | 601.2h, 702.51a, 701.61a | `player.rs:203-244`; `casting.rs:5493-5522`, `:3944-3946`; `abilities.rs:1162-1214` |
| **DP-27** | D | Target **division** (CR 601.2d) unimplemented end-to-end — no command field, no `Effect`, no resolution path | 601.2d | `fire_covenant.rs:25-37` |
| **DP-28** | D | Absent subsystems: Vote / Council's Dilemma (701.36); "you may choose not to untap" (502.2); cost-to-block; Battle as an `AttackTarget`. Banding is **C** (no enum variant, deliberately deferred) | 701.36, 502.2, 509.1 | §4.5, §4.11 |
| **DP-29** | D | `ActivateForecast` targets stored with **zero** validation; `ActivateBloodrush` target skips protection/hexproof/shroud | 601.2c | `rules/abilities.rs:1725-1748`, `:1845-1880` |
| **DP-30** | D | Only `spell_additional_costs[0]` is validated despite the comment claiming all are | 118.8 | `rules/casting.rs:3307-3309` |
| **DP-31** | B | Commander hand/library redirect (CR 903.9b) registered unconditionally ⇒ always opts in; the owner's "may" is dropped | 903.9b | `state/builder.rs:1199-1229` |
| **DP-32** | A* | Commander graveyard/exile choice (CR 903.9a) is honoured but **does not gate progress**; and it is modelled as a post-move SBA, not a replacement, so `CreatureDied` fires and CR 400.7 makes the commander a new object first | 903.9a | `rules/commander.rs:410-420`; `state/builder.rs:1181-1184` |

---

## 6. What the audit did **not** cover

Stated so a re-audit knows where the edges are.

- **Card-by-card verification.** The corpus counts in §3.1 were a regex sweep, not the serde
  walk `effect_choose_gate.rs` uses — treat the original table as a magnitude, not an exact
  roster. **PB-DP10 (`scutemob-158`) superseded this**: `crates/engine/tests/core/
  decision_gate.rs::decision_site_reconciliation_report` now enumerates all 22 rows from
  `all_cards()` and prints exact numbers on every `cargo test`. Two mechanisms explain most
  of the drift from the original estimate to the measured numbers: the compound rows
  (`triggered_targets` / `modal_trigger`) are qualified per-`AbilityDefinition::Triggered`-node
  in the gate but were a file-level regex conjunction in the original sweep (which over- or
  under-counts depending on whether a def's other abilities happen to share the file), and the
  unit-variant rows (`proliferate`, `the_ring_tempts_you`) require string-value matching that
  no walk before this batch performed (see the PB-DP10 row in §8). **Two more mechanisms,
  found by PB-DP10's closing fix-review cycle** (2026-07-27; full per-row table in §3.1's
  superseded note): (iii) the audit's regex counted a variant name appearing inside a
  `Completeness` note string (a comment-like field the serde walk cannot see) as a real hit —
  the whole of the `connive` 2→1 and `put_on_library` 3→1 deltas; (iv) the audit's original
  row 4 bundled two runtime predicates (`DiscardCards` and `WheelHand`) under one count, and
  the split is exact: `discard_cards` 13 + `wheel_hand` 10 = 23. Three further deltas
  (`search_library` 74→73, `may_pay_then_effect` 11→10, `modal_trigger` 5→4) are left
  **unexplained, ±1, within regex noise** rather than assigned a mechanism this suite has not
  verified — this suite has published a plausible-sounding roster as fact and been wrong three
  times before (PB-DP6's 3-vs-14, PB-DP8's 84-vs-77, PB-DP9's 74/16/8-vs-69/16/7-vs-73/16/8),
  and an honest gap beats another plausible story.
- **The simulator's `StubProvider` gaps** (Adventure, alt-costs, modes, convoke/improvise/delve)
  are move-generator limits, not engine decision-surface gaps, and are already catalogued as
  M11-local risk R4.
- **Whether each class-B tie-break is the *worst* choice.** This audit establishes what the
  engine picks; it does not model how much equity a player loses. That is the ranking input
  §5 approximates by frequency and by "distance from a sensible pick".
- **Deferred subsystems** already tracked elsewhere: Battle (OOS retriage plan), Banding
  (ability-coverage doc), P2P/hidden-information (M10).

---

## 7. OOS-M11 seed assessment

Explicitly requested. **`memory/primitives/rider-seed-triage-2026-07-19.md` was not modified**
— that queue is paused at PB-RS4 and belongs to the coordinator.

### OOS-M11-1 — mulligan no-shuffle: **CONFIRMED as filed; covers one of two defects**

The seed itself is exact. The **§8 risk table, row R2** — the row that proposes the id
`OOS-M11-1`, at `m11-session-plan.md:800` as committed at the time of this audit and `:814` in
that worktree's working copy, so grep for `OOS-M11-1` rather than trusting the line — reads
"A mulligan today returns the same hand — a live-wrong rules path (CR 103.5 requires a
shuffle)", and this audit confirms it verbatim. (The plan's §1 fact 1 hedges to
"near-identical"; the seed does not, and the seed is right.)

`Zone::insert` on an ordered zone is `push_back`
(`crates/card-types/src/state/zone.rs:109`) and `Zone::top` is `v.last()` (`:159-164`), so the
seven cards moved hand→library are the seven cards drawn back. No RNG is invoked — the engine
*does* have a deterministic seeded PRNG (`effects/mod.rs:8697-8703`, `:3049`, `:3148`, seeded
from `state.timestamp_counter`), and `handle_take_mulligan` calls none of it. The emitted
`GameEvent::LibraryShuffled` is a phantom, which is an Architecture Invariant 4 problem in its
own right ("events are the single source of truth for what happened").

**Widen the seed.** What the seed does *not* cover is a second, independent, cheaper defect on
the same command pair — this audit's genuinely new contribution here: `handle_keep_hand`
(`rules/commander.rs:886-890`) puts `cards_to_bottom` on the **top** of the library. That half
is a one-line `push_front` fix with no wire impact and it is a strictly-wrong rules path today.
Recommended rank: **Tier 0**, above every RS-queue item, on the same "live-wrong on a
`Complete` path" criterion that put OOS-RS-1 at the head of the RS queue.

**CLOSED by PB-DP2 (`scutemob-150`, 2026-07-26) — including the widening this audit
recommended.** Both halves shipped together in `rules/commander.rs`: `handle_take_mulligan`
now runs a real seeded Fisher-Yates `Zone::shuffle` before the `LibraryShuffled` event and
the draws (so the event is no longer a phantom), and `handle_keep_hand` now uses
`move_object_to_bottom_of_zone` (`push_front`) so bottomed cards land on the bottom. The seed
is `state.timestamp_counter` — the same deterministic PRNG source this section pointed at —
so replay determinism (SR-9b) holds and **no wire change was needed** (PROTOCOL 27 / HASH 63
unmoved), falsifying §8's HASH-bump prediction for the (b) half. 4 fail-before/pass-after
probes in `crates/engine/tests/rules/commander.rs`; tests 3,721 → 3,725.

### OOS-M11-2 — mana solver: **CONFIRMED, correctly ranked low, and it should not enter the primitive queue**

Both halves verified. `crates/simulator/src/mana_solver.rs` contains **zero** references to
`mana_pool` (so a human who taps manually and then casts is over-tapped), and it reads
`obj.characteristics.mana_abilities` directly (`mana_solver.rs:44`) rather than
`calculate_characteristics`, so a Cryptolith-Rite-granted or animated-land mana ability is
invisible to it.

But this is **simulator-side**, and the engine's own payment paths are layer-correct and
authoritative — `solve_mana_payment` returns a `Vec<Command>` of real `TapForMana` values that
`process_command` then judges. It cannot produce a wrong game state; it produces a wrong
*suggestion*. It belongs in M11-local (Session 3 item 7 already fixes the pool half), **not**
in the primitive queue, which is for engine correctness. Recommended rank: below every class-D
finding in §5.

> **Rider added by PB-DP4 (`scutemob-152`).** The attack-tax debit makes the pool-blindness
> cost real in combat too: a bot that taps for the tax and then has that mana actually *spent*
> will over-tap on its next action, because the solver never reads the pool. Still an
> M11-local Session 3 item, not a primitive-queue item — but it now costs the bot mana rather
> than only a suggestion. See also **OOS-DP4-8** (the provider offers attack targets whose tax
> the player cannot pay at all).

> **CONFIRMED by `scutemob-159` (2026-07-27), the seed re-rank.** Both halves re-checked at
> source: `crates/simulator/src/mana_solver.rs` still contains **zero** occurrences of
> `mana_pool`, and still reads `obj.characteristics.mana_abilities` directly at `:35` rather
> than `calculate_characteristics`. `memory/m11-session-plan.md:914` (risk R3) already assigns
> the pool half to Session 3 item 7. **This section's exclusion stands and is now recorded as a
> queue decision, not just a recommendation**: OOS-M11-2 is explicitly listed in
> `memory/primitives/seed-rerank-2026-07-27.md` §1d ("NOT the primitive queue — owned
> elsewhere") so a future dispatcher does not re-rank it. The reasoning is the one this section
> gives: the engine's own payment paths are layer-correct and authoritative, and
> `solve_mana_payment` returns a `Vec<Command>` that `process_command` then judges, so it cannot
> produce a wrong game state — only a wrong suggestion.

### OOS-M11-4 (né OOS-M11-3; renumbered — ID taken by scutemob-147's fuzzer-nondeterminism seed) — triggered-ability targets: **CONFIRMED; reclassify B, not D** — **CLOSED by PB-DP8 (`scutemob-156`, 2026-07-26)**

> **CLOSED.** `Command::ChooseTriggerTargets` + `GameEvent::TriggerTargetChoiceRequired` +
> `GameState.pending_trigger_targets` suspend `flush_pending_triggers` mid-batch and resume it
> when the trigger's controller answers (PROTOCOL 28 → **29**, HASH 65 → **66**). The compliant
> CR 603.3d fallback is preserved verbatim as `abilities::default_trigger_targets`, which the
> *caller* submits as a real `Command` — the engine still never auto-picks on a decision path
> and still knows nothing about seat kind (Architecture Invariant 1). Roster **77**, not 84.
> Two corrections this closure forced, both recorded in §8's PB-DP8 row: an `UpToN` slot was
> not "contributing 0 targets" but **removing the whole trigger**, and the four guarded call
> sites needed an explicit priority-grant obligation on the pending entry.

Verified at `rules/abilities.rs:7174-7500` (stale — the arm was at `:7308-7798` when PB-DP8
re-verified it). The fallback is **CR 603.3d-compliant**: it uses layer-resolved characteristics
(`layers::expect_characteristics`, `:7434` → really `:7571`), honours protection/hexproof/shroud
(`validate_target_protection`, `:7438-7450` → really `:7573-7582`), never self-targets a
`TargetOpponent` requirement (`:7272-7291` → really `:7410-7425`), and removes the trigger from
the stack when a required slot has no legal candidate — exactly what CR 603.3d directs. **All
four claims re-verified true on the PB-DP8 branch.**

**One exception PB-DP8 found and this section did not:** the compliance claim holds for every
*required* slot and fails for an **`UpToN` ("up to N target ...") slot**. That arm returned
`None`, and the surrounding loop reads `None` as "no legal target" and removes the whole trigger.
CR 601.2c makes zero targets a legal announcement for "up to", so CR 603.3d's removal clause
never applies — Sword of Sinew and Steel and Elder Deep-Fiend (both `Complete`) had never once
put their trigger on the stack. Fixed by PB-DP8 as a consequence of making `optional` mean
anything at all.

So this is not (mostly) a rules violation. It is a **total loss of agency on 77 effectively-`Complete`
defs** — the largest single-site agency loss in the corpus after tutors. Recommended rank:
**top of Tier 1**, immediately below the Tier-0 correctness items, and it is the flagship
motivator for the pending-decision work in §8 because it is the first finding that *requires* a
new `Command` (and therefore a PROTOCOL bump). **Shipped as PB-DP8, `scutemob-156`.**

---

## 8. Proposed primitive-queue insertions

**THE PB-DP SUITE IS COMPLETE — PB-DP1..PB-DP10 ALL SHIPPED** (`scutemob-149..158`; final:
PB-DP10, `scutemob-158`, the invariant-level gate). Proposals only, historically; the RS queue
is paused at PB-RS4 and this audit does not edit it — the coordinator ranked PB-DP1..DP10
ahead of it and the queue below is now a shipped record, not a to-do list.

Ordered correctness-first, matching the RS queue's own convention that a live-wrong path on a
`Complete` card outranks everything else.

| proposed | finding | wire impact | why here |
|---|---|---|---|
| **PB-DP1** | **DP-1** — priority holder after cast / activate / special action (CR 117.3c) — **SHIPPED** (`scutemob-149`) | **none** | ~20 mechanical sites, no new type (verified breakdown: 14 Group A / 3 Group B / 8 Group D / 5 false positives, see §5 DP-1). Highest correctness-per-line ratio in the whole audit, and it is the precondition for a human seat behaving like a player rather than a spectator. The original site list also missed `handle_activate_loyalty_ability` and `handle_level_up_class` — both fixed. A fix-cycle review pass (0 HIGH / 3 MEDIUM / 8 LOW) added an entry priority guard to the three Group-D "grant" handlers (`handle_turn_face_up`, `handle_activate_loyalty_ability`, `handle_level_up_class`), closing part of OOS-DP1-2; the CR 606.3 "their own turn" sorcery-timing gap on loyalty/level-up remains open (DP-21's scope) |
| **PB-DP2** | **DP-2** — mulligan. Split: (a) `cards_to_bottom` → `push_front`; (b) real shuffle — **SHIPPED** (`scutemob-150`) | **none** — the "(b) needs a seed on `GameState` ⇒ **HASH bump**" prediction is **falsified**: (b) reuses the existing `timestamp_counter` seed source (`effects/mod.rs:8697-8703`), so no `GameState` field was added and **PROTOCOL 27 / HASH 63 are unmoved** | (a) is a one-liner and closes a live-wrong path today. (b) is the OOS-M11-1 seed proper. **The split was unnecessary**: (b) did **not** need to trail (a) — with no wire cost, both halves shipped together as a 2-edit PB in one file. M11-local Session 2's pregame `redeal` is therefore no longer load-bearing for correctness |
| **PB-DP3** | **DP-4** — `min_modes` floor when `modes_chosen` is empty — **SHIPPED** (`scutemob-151`) | **none** — prediction **confirmed**: PROTOCOL 27 / HASH 63 unmoved, no `Command`/`GameEvent`/`Effect` variant and no `GameState` field touched | 3 `Complete` cards resolve half a spell at full price. **The "mirror the Spree guard at `casting.rs:2940-2944`" prescription was deliberately not followed**: the Spree guard was kept intact (it fires earlier, during cost computation, and owns the CR 702.172a message that `spree.rs:854` asserts), and the general fix is a **lift** — the range/duplicate/`min_modes`/`max_modes` checks moved out of the `!modes_chosen.is_empty()` gate so they run whenever the object is modal. That made the yield larger than the row predicted: **37 more `min_modes: 1` defs** plus the activated-ability twin (§4.2) were fixed alongside the 3 commands, for **0 card-def edits**. Escalate keeps a narrow CR 702.120a exemption (**OOS-DP3-1**) |
| **PB-DP4** | **DP-10** + **DP-11** — attack tax debit; echo/cumulative-upkeep/recover enforcement — **SHIPPED** (`scutemob-152`) | **none** — prediction **confirmed**: PROTOCOL 27 / HASH 63 unmoved, no new `Command`/`GameEvent`/`GameState` variant or field. The existing `GameEvent::ManaCostPaid` carries the tax debit and the three `Pay*` commands + three `*PaymentRequired` events already existed. The row's conditional ("*if* the 'otherwise' is applied at resolution rather than gated on priority") held, though **the boundary actually chosen is neither** — see §5 DP-11 | Two "the cost is checked but never collected" bugs of the same shape. **The bundling rationale held**, and the shared lesson is sharper than the row predicted: *an affordability check is not a payment*, and both fixes amount to **making the check and the payment the same predicate** — DP-10 by calling `can_pay_cost`/`pay_cost` on one summed `ManaCost` instead of comparing a flattened `u32` against `total_with_restricted()`, DP-11 by making the code that *created* the pending entry and the code that *consumes* it reachable from the same control flow. Yield larger than filed: **5 `Complete` defs are live-wrong today and are made right with 0 card-def edits** (`propaganda`, `ghostly_prison`, `mogg_war_marshal`, `avalanche_riders`, `grim_harvest`). Also closed **OOS-DP1-1** (by deletion — the three priority bodges) and **OOS-RS3-4** (CR 508.1d must-attack-vs-cost), and found two bugs the audit had not filed: an unguarded life subtraction in the cumulative-upkeep `Life` arm (CR 119.4) and a mis-rated §4.5 "**A**" row. Tests 3,747 → **3,781** |
| **PB-DP5** | **DP-5** — `WouldDraw` pending-choice state — **SHIPPED** (`scutemob-153`) | **HASH 63 → 64** — prediction **CONFIRMED**, and it is the first PB-DP to move a version constant. The bump was forced by the gate (`declaration_fingerprint_is_pinned`), not hand-applied; the v64 `HASH_SCHEMA_HISTORY` row is appended with freshly-computed fingerprints and no existing row was edited. **PROTOCOL 27 unmoved**: `Command::OrderReplacements` was reused exactly as this row predicted, so no `Command` / `GameEvent` / `Effect` variant was added and `ReplayLog` (a `{ u8, Vec<Command> }` that never embeds `GameState`) keeps the new field off the wire | The engine asked a question it could not accept an answer to and silently ate a draw. **The row's implicit premise about yield needs correcting, and §5 does so: card yield is 0** — no def in the corpus registers a `WouldDraw` replacement, so this is a class-D engine defect and the precondition for authoring that family, not a live-wrong `Complete` card. Scope also grew by one site the audit never named (`draw_card_skipping_dredge`) and by the CR 614.11a sequence bug the finding did not mention (`DrawCards { count: 3 }` emitted **three** unanswerable prompts and drew **zero**). Its planning turned up **OOS-DP5-7**, a live free-card exploit on `Command::ChooseDredge` of the same trust-boundary class — deliberately deferred, and a strong candidate to rank next |
| **PB-DP6** | **DP-15** — intervening-if at queue time (CR 603.4) — **SHIPPED** (`scutemob-154`) | **none** — prediction **CONFIRMED**: PROTOCOL 27 / HASH 64 unmoved, no new `Command` / `GameEvent` / `Effect` / `Condition` variant and no field on `GameState`, `PendingTrigger` or `Characteristics`. The one temptation to bump was declined and seeded (**OOS-DP6-1** needs an `Option<Condition>` on `TriggeredAbilityDef`, which is hashed via `Characteristics`) | **The "retires a whole class of def-level caveats" premise was wrong, and it is worth recording why.** Exactly **two** caveats named the queue-time gap (`loyal_apprentice.rs`, `siege_gang_lieutenant.rs`); the ~20 other CR 603.4 notes say "*re-checked at resolution*", which stays true, and a further nine say "*the `Condition` DSL lacks the variant*", which is a different finding this PB does not touch. Blanket-clearing on the row's premise would have deleted ~29 accurate notes. The real value landed elsewhere: **the roster was 14 sites, not 3**, and the two paths the audit credited as already-correct were **both** carrying their own defects — the ETB gate zero-filled `kicker_times_paid` so `WasKicked` was unconditionally false (two deck-legal `Complete` defs never triggered at all), and the resolution-time re-check had the identical zero-fill, so fixing only the queue side would have produced a queue-then-fizzle no-op. A false **negative** inside a class-A row is the sharpest lesson of this batch: *an audit row's "already correct" is a claim, not a fact.* Yield: **0 completeness flips, 0 card-def source edits**, 15 defs stop over-firing, 2 start firing; tests 3,797 → **3,809**; ten seeds (OOS-DP6-1..10), of which **OOS-DP6-1 is worse than DP-15 itself and should be ranked next**: a lowered trigger's condition is checked at neither end, which a close-out probe confirmed *by execution* makes `aurelia_the_warleader` — a `Complete`, deck-legal, widely-played commander — an **unbounded infinite-combat loop** |
| **PB-DP7** | **DP-3** — cleanup discard hook (CR 514.1) — **SHIPPED** (`scutemob-155`) | **PROTOCOL 27 → 28 *and* HASH 64 → 65** — prediction **confirmed on both halves**, and it is the suite's first `Command`. `Command::DiscardToHandSize` + `GameEvent::CleanupDiscardChoiceRequired` force the protocol bump; `GameState.pending_cleanup_discard` forces the hash bump. All four fingerprints gate-computed, both history tables append-only | **The pilot worked, and the reusable asset is the gate — but the completeness argument is the part to copy, not the answer.** §8's sequencing note asked for "make a pending decision actually block". The shipped mechanism is `BlockingDecision` + `blocking_decision(&GameState) -> Option<_>` consulted at exactly **two** engine sites: a progress guard in `enter_step` and an admission gate at the top of `process_command`. That set is small because it was **derived, not enumerated** — nothing advances a step or a turn except the call sites of `turn_structure::advance_step`/`advance_turn` (5 sites, 3 functions on this branch), and one of the three (`handle_all_passed`) is already unreachable while blocked because `priority::pass_priority` rejects when `priority_holder` is `None`. The third, `handle_concede`, needed a *clear*, not a gate. **PB-DP8 must re-derive from the `flush_pending_triggers` call-site set the same way — the method transfers, the answer does not** (that set is ~20 command paths plus both `enter_step` branches). Design detail, written for DP8/DP9 to read: plan `memory/primitives/pb-plan-DP7.md` §1, with §1.5 stating what DP-8 inherits (the enum, both gates, the `LegalAction`/`DecisionKind`/`LocalGame` shape, the "engine never auto-picks; a pure helper supplies the bot default" pattern) and what it must design fresh (cardinality — CR 603.3b makes DP-8's pending state a `Vector` answered as a *sequence*; and partial-flush resumption, which DP-7 gets for free only because `cleanup_actions` is idempotent once the hand is at max). **§1.6 is the honest one: PB-DP9 does NOT inherit the resume mechanism.** DP-7 and DP-8 pause *between* actions; DP-9 pauses **inside** `execute_effect` with an effect list still to run. That is a continuation problem the engine has already met and lost — **OOS-DP5-5** — and it needs a resumable effect-list cursor on the stack object (another `GameState` shape change + a re-entrancy audit of every `execute_effect` caller). Do not let "the pilot proved the pattern" be read as "DP-9 is plumbing". Also decided here, and load-bearing for the block-vs-deadline question the suite will keep meeting: **a block is only safe where the CR supplies no default.** PB-DP4 correctly chose a deadline because CR 118.12a *has* a "didn't pay" branch and the decision sits inside a priority window; CR 514.1 has neither (it is mandatory, and CR 514.3 gives cleanup no priority at all), so the answering command is the only legal move and the deadlock PB-DP4 feared is structurally unreachable. Yield: **0 completeness flips, 0 card-def edits** as predicted, but 3 `Complete` Madness defs stop being involuntarily exile-able; tests 3,809 → **3,832**. **Three things the closing review found that DP-8 and DP-9 should expect to meet too**, because none of them is specific to the discard: (1) **a block needs a liveness story.** The first implementation recorded an entry for `turn.active_player` without checking the player was alive — and a player *can* be `has_lost` during their own cleanup, because this engine marks the flag without removing the player or their objects (CR 800.4a is unimplemented). The result was an entry the gate's own liveness filter made invisible while nothing cleared it: CR 514.2 skipped for that turn (**CR 800.4j** says the turn completes anyway), and all three consumers pinned on a dead player forever. Any new blocking kind must answer "what if the player it names leaves the game?" *and* "who clears the entry then?" — the concede path is not the only exit. (2) **an answering command needs a moment guard, not just a payload guard.** The handler validated the cards exhaustively and never checked `turn.step`, so the command was accepted out of step and the resume re-ran turn-based actions for whatever step was current. (3) **the gate's predicate must be the thing consumers read.** The engine gated on a liveness-filtered `blocking_decision()` while the simulator, `LocalGame` and the TUI read the raw field, so they could disagree — and when they did, the provider returned no legal actions for *every* seat. Fixed by promoting the predicate to `pub` behind a `GameState` accessor and rewiring all three; that public predicate is the surface PB-DP8 should build on. Also, for whoever writes DP-8/DP-9's tests: answering via `process_command` **completes and advances** in one round-trip when nothing else is pending, so a test that needs to hold the intermediate moment still must call the handler directly (PB-DP7's second-pause and out-of-step tests are the worked examples). **A fourth lesson came from the closing `/review`, and it is about the block's blast radius rather than the block itself: a gate that stops the engine also stops every loop built on top of it.** The TUI's auto-pass loop spun on `PassPriority` every 50ms against a blocked human seat — its stop condition required the main phase, and the rejection was swallowed into a status message — so the key that would clear the block was unreachable. Pre-DP7 that path sailed through because cleanup auto-discarded. **PB-DP8/DP9 must enumerate the *driving loops* (TUI auto-pass, `GameDriver`, `LocalGame::advance`, the fuzzer), not just the consult sites**; the plan's §7.4 listed the TUI's display and input surfaces and missed its loop. The same review also caught the earlier fix cycle asserting, in a source comment, that an SR-19 gate covered the new hashed struct when it did not (**OOS-DP7-11**) — worth remembering that *a gate cited in a comment is a claim like any other* |
| **PB-DP8** | **DP-6 / OOS-M11-4** — trigger-target hook (CR 603.3d) — **SHIPPED** (`scutemob-156`) | **PROTOCOL 28 → 30 *and* HASH 65 → 67** (two bumps: 28→29 / 65→66 in the implement phase, then 29→30 / 66→67 in the fix cycle when review finding HIGH-2 put a real `max: u32` on `TriggerTargetOption`, changing a wire type again — both gate-forced, both histories append-only) — prediction **confirmed on both halves**. `Command::ChooseTriggerTargets` + `GameEvent::TriggerTargetChoiceRequired` (discriminant 130) force the protocol bump and, unlike PB-DP7's, this one **changes the closure's type count**: `TriggerTargetOption` is new and, through it, `SpellTarget` enters the wire closure for the first time (`command.rs` had only ever carried bare `Target`). `GameState.pending_trigger_targets` forces the hash bump. All five fingerprints gate-computed, both history tables append-only. **53 scattered sentinels across 44 files re-pinned — exactly the count OOS-DP7-8 complains about — plus a 54th the plan's enumeration missed** (`pb_dp5_pending_draw_choice.rs`, which spells the constant `mtg_engine::HASH_SCHEMA_VERSION` and so escaped the regex) | **Roster: 77, not 84** — enumerated from `all_cards()` per SR-36, printed by a test, and *neither* the audit's 84 nor the planner's grep-derived 74 was reproduced. **0 card-def edits, 0 completeness flips**, as predicted. Yield beyond agency: **two live-wrong `Complete` cards made right by accident** (Sword of Sinew and Steel, Elder Deep-Fiend) — see the `UpToN` correction in §7. **Four things worth carrying to PB-DP9.** (1) **The consult-site set was far smaller than the PB-DP7 row predicted.** That row said "~20 command paths plus both `enter_step` branches"; the real answer is **six guards** (the implement phase found four, the fix cycle added `handle_all_passed`'s forced-overdue-payment branch, and the plan's own "four" counted `enter_step` once instead of twice), because all 30 `check_and_flush_triggers` call sites in `process_command` are followed by exactly `all_events.extend(events);` and the end of the match arm, and `process_command`'s tail only pushes history. **PB-DP1 is why**: it moved priority assignment *into* the handlers, ahead of the flush. The derivation that transfers is "the guard set is every statement that executes after the suspending call, within the same `process_command` invocation". (2) **A guard that returns early inherits a debt.** Each of the four sites was about to grant priority; simply returning leaves a resumed game with nobody holding priority — a hang. The plan did not say who grants it, and it cannot be inferred at resume time (the fifth call site owes nothing). Fixed with an explicit `resume_site: FlushResumeSite` on the pending entry (a `bool` named `grant_priority_on_resume` at implement close; widened by the fix cycle when review Finding 4 showed the priority grant was not the only thing those sites owed -- the CR 726 loop check and the cleanup ratchet had to be reproduced too), set by the guards and inherited when the same batch suspends again. **Any future blocking decision taken mid-turn-based-action needs the same question asked.** (3) **The pending state is an `Option`, not the `Vector` PB-DP7's §1.5 predicted.** CR 603.3b's batch is answered as a *sequence*, and "answered as a sequence" is precisely what makes the outstanding set a singleton; the plurality lives in a `remaining: Vector<PendingTrigger>` **inside** the entry, which is what it actually is — the un-flushed tail of one batch. (4) **`LocalGame` had a latent variant-blindness bug PB-DP7's review did not catch**: `advance()` read the liveness-filtered predicate correctly and then hard-coded `DecisionKind::CleanupDiscard`, so a second variant would have handed a browser client the wrong picker. Now an exhaustive `match`, which makes that obligation compile-forced. Contrast the TUI, whose auto-pass loop and `acting_player` **did** generalise for free (verified by reading, not assumed) — the DP-7 lesson about driving loops held. Fuzzer A/B (8 fixed seeds, `--max-turns 200`, vs `main`): 2 seeds byte-identical, 4 same winner with different turn counts, 1 same error class, 1 flipped from `Winner: P2` to the pre-existing `EngineError(PlayerEliminated)` shape `main` already produces on other seeds. **The plan's "any winner or turn-count change is a bug" oracle is falsified by its own §10**: an extra `Command` per non-forced trigger shifts `RandomBot`'s RNG stream, so trace divergence is structural, not a regression. Tests 3,837 → **3,871** (3,858 at implement close; the review's 12 findings — 2 HIGH, 5 MEDIUM, 3 LOW engine + 2 HIGH, 2 LOW card/oracle — were **all** fixed in-batch, adding 13 more, and the two seeds the implement phase had deferred (OOS-DP8-9/10) were fixed rather than left open). **HIGH-1 is the one to carry forward**: the resume bound the answered targets *lazily*, behind the CR 603.3d removal guard, so a head trigger removed at resume time leaked its answer onto the **next** trigger of the batch — a different ability, possibly a different controller. The plan's premise that "state cannot change between offer and resume" is false, because the admission gate admits `Concede` from any player. **Any future blocking decision that suspends a batch must bind its answer positionally, not lazily.** **A closing `/review` (1 HIGH, 1 MEDIUM, 6 LOW) then found the sharpest lesson of the whole batch, and it is about the FIX CYCLE, not the implementation: the HIGH was introduced by the fix cycle's own Finding-5 gate, and the branch's own test constructed the deadlocking state and asserted the wrong thing about it.** Gating `handle_concede`'s priority advance under a foreign announcement stopped a concede stepping over the batch — and stranded `priority_holder` on the conceded player for the `FlushResumeSite::None` case (the common one), which no `PassPriority` from anyone can ever clear. Three transferable rules: (i) **when a guard SKIPS work, enumerate what that work was going to do and name where each part is picked up** — the gate's source comment asserted the resume would grant priority, and for the most common resume site it does not; (ii) **a test that constructs a hazardous state and does not assert against it is worse than no test**, because it reads as coverage (`test_dp8_foreign_concede_does_not_step_over_the_suspended_batch` built the exact 4-step deadlock and never looked at `priority_holder`); (iii) **a debt-carrying pending entry needs a merge rule, not a choose-one rule** — the CR 800.4d reap discharged a stale debt inside the caller's own flush and double-granted priority (OOS-DP8-13). Also closed: the engine could refuse its own default answer for two mutually-distinct slots (SR-38 hole, OOS-DP8-4 narrowed), the script harness's pump-skip was step-local and kind-blind (OOS-DP8-14), and the ESM criterion-5545 test was vacuous — it hashed a state it had cloned INTO the call, and now drives `handle_choose_trigger_targets(&mut state, ..)` once per rejection class, proven non-vacuous by a mutate-before-validate probe. Tests 3,871 → **3,875**; PROTOCOL 30 / HASH 67 unmoved by the closing cycle. **A SECOND closing `/review` (1 MEDIUM, 2 LOW) then confirmed the HIGH closed and found the same deadlock class surviving one step further out — the batch's real lesson, stated a third time.** The repair the first closing cycle added sits at the end of `resume_trigger_flush`, which covers **answers**; a **departure** is the other way out of a suspended batch and completes it through `drop_departed_trigger_flush` without ever reaching the resume. `handle_concede`'s own advance is guarded on `priority_holder == Some(player)`, so it can only repair holdership belonging to *this* conceder — never one stranded by an earlier departure. Two concedes in a row under one suspended batch (P2 concedes as holder while P1's announcement is open, then P1 concedes instead of answering) reproduced the identical unrecoverable deadlock. Fixed with an unconditional backstop at the **end** of `handle_concede` rather than at the review's suggested site immediately after the drop (there it would preempt the existing advance on the ordinary concede path, where `next_priority_player() == None` owes `handle_all_passed` per MR-M2-03, not a grant to the active player). **The transferable rule (iv): a choke point placed on the "answered" path is not a choke point — enumerate every exit from a blocking decision, and departure is always one of them.** And (v), the meta-lesson: **this was the THIRD time in one batch that a source comment asserted a property the code did not have** — the pre-fix gate comment, its replacement ("nothing is skipped here that is not picked up there", false for the departure exit), and `default_trigger_targets`' "one exception" doc (which named the wrong failure mode for the both-slots-forced half). OOS-DP7-11's lesson generalises: a comment that *justifies* skipping work is a claim about reachability, and reachability claims need the same enumeration a match arm does. Tests 3,875 → **3,878**; PROTOCOL 30 / HASH 67 unmoved again; OOS-DP8-13 mostly closed, OOS-DP8-4 narrowed to its default-quality half. |
| **PB-DP9** | **DP-7 / DP-8 / DP-9** — search, scry, surveil hooks — **SHIPPED** (`scutemob-157`) | **PROTOCOL 30 → 31 *and* HASH 67 → 68** — prediction **confirmed on both halves**, both gate-computed, both histories append-only. But the prediction of "new `Command`**(s)**" is **falsified in the singular direction: ONE command, not three.** CR **608.2d** is the rule all three implement, and CR 701.22a / 701.23a / 701.25a are three *instances* of it with identical timing ("while applying the effect"), identical validity condition ("can't choose an option that's illegal or impossible") and identical actor. So `Command::AnswerEffectChoice { player, choice_id, answer: EffectChoiceAnswer }` and `GameEvent::EffectChoiceRequired` (discriminant **131**) cover all three, giving **one** admission-gate entry, **one** `LegalAction`, **one** `DecisionKind`, **one** `BlockingDecision` variant and **one** harness action string. That directly corrects **OOS-DP8-14**, which predicted PB-DP9 would add three answering action strings to the pump-skip predicate. Like PB-DP8's, this bump **changes the closure's type count** (`EffectChoiceQuestion` and `EffectChoiceAnswer` are both new and both reachable from both frames). 44 sentinel files re-pinned via the **symbol** grep of the plan's §4.3, not a numeric regex. `GameEvent::private_to()` and `reveals_hidden_info()` also land here; they are **methods**, not declared shapes, and do not move the digest | **The prediction that mattered most was the DESIGN one, and §8's own sequencing note had it wrong.** `pb-plan-DP7.md` §1.6 (repeated in this row and in the note below) prescribed **"a resumable effect-list cursor on the stack object"**. That is **impossible**: `resolve_top_of_stack` **pops** the stack object at `resolution.rs:39-42`, before a single effect runs, so nothing living on the stack object can carry a continuation for effect execution. What shipped instead is an **abort-and-replay**: `resolve_top_of_stack` clones the state at entry; an effect that needs an unanswered choice records the *question* and returns; the wrapper **restores the clone wholesale** (the stack object is back, no card has moved, no event happened) and returns exactly one event, the question; the answer is banked on `GameState` and the resolution is re-run **from the top**, retracing the identical deterministic path and consuming the banked answer at the choice point. Five consequences, each worth carrying: (1) **there is no continuation data structure at all** — `Sequence`, `Conditional`, `ForEach`, `Repeat`, `MayPayThenEffect` and the per-player loops inside the asking effects are all handled with zero machinery, because the replay re-executes them, and re-deriving is *correct* rather than merely convenient since they are pure functions of a state the roll-back restored; (2) **the re-entrancy audit is three units, not twenty** — of the 17 production `execute_effect` callers, **15 are inside the single function `resolve_top_of_stack`**, one is `mana.rs:864` (gated by CR 605.1b/605.4a) and one is `replacement.rs:1964` (a literal `Effect::CreateToken`, provably unreachable); (3) **PB-DP8's hardest recurring bug class — "a guard that returns early inherits the obligation of the statements it skipped" — does not exist here**, because a total state restore has no debt: nothing was skipped, nothing happened. Its one residual, `handle_all_passed`'s two post-resolution statements, is factored into `finish_stack_resolution`; (4) **"the suspended object leaves the stack" is structurally unreachable** rather than a live hazard — the object was never removed and no admitted command removes stack objects; (5) the price is real and named: the resolution runs k+1 times for k choices (k is 1 or 2 across the whole corpus), one extra `GameState` clone per resolution (**measured: `full_turn_4p` 253 µs on `48353a36` vs 229 µs on-branch — no regression; `imbl` makes the clone a handful of refcount bumps**), and **SR-9b determinism becomes a RUNTIME requirement, not just a test one**. The detector for that last one is a question-equality check before consuming each banked answer. **Roster: 73 / 16 / 8 `Complete` (+25 / +3 / +1 non-`Complete`)**, enumerated from `all_cards()` per SR-36. **The batch's own first answer, 69 / 16 / 7, was ALSO wrong and is superseded here** (closing-review Finding 5): the shipped walk was a hand-written `match` that never descended into `AbilityDefinition::{Spell,Triggered,Activated}::modes`, never visited `AbilityDefinition::{SagaChapter,LoyaltyAbility}` or split-card halves at all, and omitted `Effect::CoinFlip` while its own doc comment claimed to cover it. Ten defs were missing (Binding the Old Gods, Evolution Charm, Insatiable Avarice, Thirsting Roots, Connive // Concoct `Complete`; Tooth and Nail, Urza's Saga, Retreat to Coralhelm, Wrenn and Seven, Kaito Bane of Nightmares non-`Complete`). The walk is now a **structurally complete serde walk of the serialized `CardDefinition`** — it visits every field of every variant at every depth by construction and cannot rot as the DSL grows. This is the third consecutive batch in which a roster number published as fact was wrong; the lesson is narrower than "enumerate, don't grep" — *a hand-written tree walk is a reachability claim and needs the same enumeration a match arm does*. **0 card-def edits, 0 completeness flips**, as predicted. **Three in-scope correctness fixes beyond the agency restoration**, all CR-derived and none of them in the plan's original brief: CR **701.22b** (`Scry 0` was emitting `Scried { count: 0 }`); CR **400.7** (scry-to-bottom renumbered every scried card and consumed `timestamp_counter`, the shuffle seed source — fixed with a `Zone::reposition_within` permutation, seed OOS-DP9-11 for the sweep); and CR 601.2c's principle applied to CR **701.23d** (a quantity-only search with exactly one candidate is determined and asks nothing). **The deliberate flip a reviewer should weigh hardest: the scry and surveil DEFAULTS are NOT status-quo-preserving.** PB-DP8 preserved its default byte-for-byte; PB-DP9 does that only for search (lowest `ObjectId`, zero churn) and flips scry/surveil to the **identity**. Both old defaults are CR-legal under "any number", but they are the systematically maximal-harm legal answer, and surveil-as-mill can contribute to a CR 704.5b deck-out. The cost is visible and was paid: **25 unit tests + 1 golden script** changed, every repair CR-justified, none by weakening an assertion. **A second deliberate deviation, from the PB-DP7/PB-DP8 precedent this time:** the three new `GameState` fields go into `public_state_hash` but **NOT** into `loop_detection.rs`'s mandatory-state fingerprint, because they GROW between replay *k* and replay *k+1* of the same resolution, so including them would make two structurally identical CR 726 positions fingerprint differently and could silently mask a mandatory loop. Recorded as **obligation (7)** on the `BlockingDecision` doc block, which is the first evidence that PB-DP8's six-obligation list generalises (it discharged 6/6 plus the new one). Tests 3,878 → **3,905** at implement close, → **3,906** after the fix cycle; seeds **OOS-DP9-1..16** filed below. **The closing review found two HIGHs, both on the concede exit** — the exit this suite keeps missing — and both are fixed: (1) the owner-concede test was VACUOUS (a 2-player fixture ends the game, so the discharge's resolve-and-drive half never executed while the test's doc claimed it did), rebuilt on a 3-player fixture with unconditional assertions; (2) a FOREIGN concede kept the answer bank bound to a pre-concede board, driving the replay's question-equality check into `debug_assert!(false)` — **a panic on a legal command sequence**, reproduced before fixing. The rule was re-derived rather than patched: the bank is valid only while the board the questions were asked against is the board the replay re-executes; `Concede` is the only other admitted command and it always mutates that board, so **any** concede now abandons the bank and re-drives, and the discharge moved to after every board mutation `handle_concede` performs. PROTOCOL 31 / HASH 68 unmoved by the fix cycle |
| **PB-DP10** | **gate widening** — the invariant-level fix for the whole PB-DP suite — **SHIPPED** (`scutemob-158`) | **none (test-only)** — prediction **confirmed**: PROTOCOL 31 / HASH 68 unmoved (`git diff --name-only main -- crates/engine/src crates/card-types/src crates/card-defs/src` empty), no card-def source edits, no completeness flips. Two new test-only files (`crates/engine/tests/core/decision_site_walk.rs`, `decision_gate.rs`, 18 new `#[test]`s after the fix-review cycle below), plus a rewire of `effect_choose_gate.rs`'s and `pb_rs1_roster_sweep.rs`'s object-key walks onto the canonical one (behavior-neutral, confirmed: 14/14 and 1/1 tests still pass with identical printed counts) and a doc-only note on `pb_dp9_effect_choice.rs::roster` | **Closes the queue this row headlines and marks the PB-DP suite COMPLETE.** The batch's headline technical finding, stated plainly: the serde walk every prior gate in this codebase used (`effect_choose_gate.rs`, `pb_rs1_roster_sweep.rs`, `pb_dp9_effect_choice.rs::roster`) matches OBJECT KEYS ONLY, and is therefore **blind to a unit `Effect` variant** — `Effect::Proliferate` and `Effect::TheRingTemptsYou` serialize to a bare JSON *string* (`serde_json::to_value(Effect::Proliferate) == Value::String("Proliferate")`, pinned directly by `T2`), not an object key, so a verbatim reuse of any prior walk would report **0** for `proliferate`'s 25 `Complete` defs while looking green — exactly the "gate reports success while checking nothing" failure mode OOS-DP7-11 was filed for. The new canonical walk (`decision_site_walk.rs::json_contains_variant`) matches BOTH shapes, with a `PROSE_FIELDS` denylist (`T3`) suppressing a false positive when a card's own oracle text, name, or a granted ability's `description` happens to spell a variant name exactly. **Measured taxonomy** (22 rows, not the original 21 — row 4 splits into `discard_cards` AUTO and `wheel_hand` NO-DECISION): **4 SERVED** (`triggered_targets` 77, `search_library` 73, `scry` 16, `surveil` 8 `Complete` defs — reproduced by value against PB-DP8's and PB-DP9's own published rosters, `T10`/`T11`), **2 GATED** (`choose_stub`, `may_pay_or_else`, both still 0 `Complete`), **1 NO-DECISION** (`wheel_hand`), **15 AUTO-CHOSEN**. Two of those fifteen — `add_mana_filter_choice` and `the_ring_tempts_you` — were **hand-maintained zeros held by nothing**: the SR-33 gate bars the key `AddManaChoice`, a different serde key than `AddManaFilterChoice`, and does not reach it at all; `T7` converts both into machine-checked hard zeros at zero engine cost. **Both unions**: the all-rows union (this section's own 277 analogue) is **267** `Complete` defs; the still-auto union — the rows with no hook at all, frozen in `decision_gate.rs`'s `BASELINE` — is **97**. **The gate, and its own bound.** `T4` (`no_complete_def_introduces_an_unrecorded_auto_chosen_decision`) fails on a `Complete` def hitting an `AutoChosen` row that is absent from `BASELINE` or present with a mismatched row set; `T6` ratchets the union count itself, closing the hole a per-def check alone cannot ("a new def slots into a freed seat"). **The fail-closed claim was proven END-TO-END, on a real card def, not only on a synthetic corpus**: `Effect::Proliferate` was temporarily added to `lightning_bolt.rs` (a `Complete` def that is not in `BASELINE`) and both `T4` and `T6` went red — `T4` naming the card, the row, the CR and the engine site (`Lightning Bolt is NOT in BASELINE but hits {"proliferate"}`), `T6` naming the union drift — then the def was restored and both went green. That is the acceptance criterion's actual claim ("fails on unmarked new instances") demonstrated by execution rather than by reading. **This does not close DP-INV** — the failure message says so explicitly, and the two legal exits are demote or record, never "implement the choice" (that needs the owning engine PB, DP-13..DP-31, still open). Also shipped: a source-scan collision inventory (`T12`, scoped to the plan's own cited cases — `Discover`'s accepted collision with `KeywordAbility::Discover`, the harmless `SearchLibrary`/`Scry`/`Surveil` twins in `state/stubs.rs`'s wire types structurally unreachable from `CardDefinition`, and the `ChooseColor`/`ChooseCreatureType` collision across `Effect` and `ReplacementModification` — the row most exposed to a silent count change and, until the fix-review cycle below, the only one left unguarded); a completeness proof that `PROSE_FIELDS` covers every `String`-typed field AND every newtype-over-`String` field (`SubType`, `CardId` — the only two such types in the crate) reachable from the DSL, across `card_definition.rs`, `game_object.rs`'s `TriggeredAbilityDef`, `state/types.rs`, `state/replacement_effect.rs` and `state/targeting.rs` (`T13`); a drift guard between `ROWS`'s `Gated` rows and `effect_choose_gate.rs`'s own bars (`T14`); and a residual-seed honesty check (`T16`). **And it can only see a decision the DSL encoded** — every row is a predicate over a variant name, so a "you may X. If you do, Y" authored as a bare `Effect::Sequence` (Smuggler's Copter, found in the fix-review cycle below) hits zero rows and passes; that class is strictly worse than the class this gate records, and this gate does not detect it (OOS-DP10-9). 18 new tests, tests 3,910 → **3,928**, PROTOCOL 31 / HASH 68 unmoved. Seeds **OOS-DP10-1..11** filed in §8.1; **closes OOS-DP7-7** (the §3.1 re-derivation is now computed, printed and ratcheted on every run, not merely owed). **Fix-review cycle (2026-07-27):** `pb-review-DP10.md` found 2 HIGH / 6 MEDIUM / 6 LOW; all 14 applied. Headline: `BASELINE`'s 97-entry freeze was populated mechanically and the plan §5.3 class-B/class-D triage was never performed against oracle text — a spot-check found two class-D members (Smuggler's Copter, Shambling Ghast; see OOS-DP10-8), so `BASELINE`'s doc comment and `T4`'s message now say so explicitly rather than implying a reviewed roster. `T4`'s non-vacuity probe was rewritten to actually drive the extracted `offenders()` function (previously it re-checked two predicates T1/T5 already covered and never exercised the subset/superset mismatch arm — half the ratchet's design rationale); `t4_failure_message_names_the_bound` (cited by the module doc but not previously written) now exists. A wrong CR cite (`106.12` for `ChooseColor`) is fixed to `614.12a`/`608.2d`. `look_at_top_or_route` and `wheel_hand`'s `why` text were both narrowed to state their true bound (upper-bound / CR 404.3 respectively) rather than overreach |

**Sequencing note.** PB-DP1..PB-DP6 need no wire change at all and could ship inside the
existing paused-queue cadence. PB-DP7..PB-DP9 are a coherent block that should be planned
together, because they all need the same missing machinery: a pending-decision that actually
*gates* progress. The engine already has that machinery seven times over
(`pending_commander_zone_choices`, `pending_zone_changes`, the three payment vectors,
`DredgeChoiceRequired`, `MiracleRevealChoiceRequired`) — but §4.4 and §4.11 show that only
`pending_zone_changes` genuinely blocks. **The generalisable design work is "make a pending
decision actually block", not "add another pending vector".**

> **UPDATED by PB-DP7 (`scutemob-155`).** The machinery now exists:
> `rules::engine::{BlockingDecision, blocking_decision}`, a progress guard in `enter_step` and
> an admission gate at the top of `process_command`. **PB-DP8 and PB-DP9 should read
> `memory/primitives/pb-plan-DP7.md` §1 before planning anything** — §1.5 says what DP-8
> inherits and what it must design fresh (its guard set is *not* this one; re-derive it from
> `flush_pending_triggers`'s call sites), and §1.6 says plainly that **DP-9 does not inherit
> the resume mechanism at all** — it pauses inside `execute_effect` and needs a resumable
> effect-list cursor (the OOS-DP5-5 problem), which is a separate `GameState` shape change.
>
> **CORRECTED by PB-DP9 (`scutemob-157`).** The first half of that sentence held — DP-9 does
> pause inside `execute_effect` and did need a `GameState` shape change. The second half did
> not: **"a resumable effect-list cursor ON THE STACK OBJECT" is impossible**, because
> `resolve_top_of_stack` pops the stack object (`resolution.rs:39-42`) before any effect runs.
> What shipped is an **abort-and-replay** — clone at entry, restore wholesale on suspension,
> bank the answer on `GameState` and re-run the resolution from the top — which needs no
> continuation structure at all and, unlike a cursor, makes "the suspended object leaves the
> stack" structurally unreachable instead of a live hazard. **OOS-DP5-5 is NOT closed by
> this**: a deferred draw suspends only its own effect, whereas PB-DP9 aborts the whole
> resolution, and the two mechanisms do not compose for free. See the PB-DP9 row above.
> PB-DP7 also established the block-vs-deadline test: **block only where the CR supplies no
> default.** PB-DP4's payments have CR 118.12a's "didn't pay" branch and live inside a
> priority window, so they stay deadlines; CR 514.1 has neither, so it blocks. Retrofitting
> the other six mechanisms onto the gate is **OOS-DP7-1**, and the dredge round-trip's
> "the engine pauses" doc comment turns out to be **fiction** (**OOS-DP7-2**).

### 8.1 Seeds filed by shipped PB-DP work

> **RE-RANKED 2026-07-27 (`scutemob-159`).** These 109 rows were merged with the paused PB-RS
> queue and the previously-unranked backlog, and the resulting **authoritative dispatch queue is
> `memory/primitives/seed-rerank-2026-07-27.md` §4 (PB-DX1..PB-DX18)** — read that before picking
> work from this table. Its top five, in order: **OOS-DP6-1** (the dropped intervening-if, the
> only open seed that is live-wrong *and* unbounded *and* on a `Complete` def), **OOS-DP5-7**
> (`ChooseDredge` free-card exploit, live today, needs no card), **OOS-DP6-3** (2 flips, zero
> engine change), **OOS-DP10-8** (the 97-entry `BASELINE` triage) and **OOS-OS7-2** (CR 611.2c,
> 7 `Complete` defs live-wrong). Per-row verdicts that task established are marked inline below
> with a `↻ scutemob-159` note; where it corrected a row, the correction is stated in the row
> itself rather than only in the successor doc.

Durable inventory for seeds this suite discovers. `memory/primitive-wip.md` is rewritten
wholesale by the next `/implement-primitive` run, so a seed recorded only there is lost —
seeds land **here**, in the suite's own binding spec. Same role §1c plays for the RS queue in
`memory/primitives/rider-seed-triage-2026-07-19.md`.

| seed | finding | class | status |
|---|---|---|---|
| **OOS-DP1-1** | **Echo / cumulative upkeep / recover reassign priority to the active player out of band.** `rules/engine.rs` `handle_pay_echo` / `handle_pay_cumulative_upkeep` / `handle_pay_recover` write `priority_holder = Some(active_player)` at *resolution* time, when no player holds priority — so CR 117.3c's antecedent is false and PB-DP1 correctly left the behaviour alone (comment-only fix). The write is a bodge standing in for the payment pause **DP-11** says was never implemented. Correct fix is the pause itself, owned by **PB-DP4**. **CLOSED by PB-DP4 (`scutemob-152`), by deletion**: all three `priority_holder = Some(active_player)` / `players_passed = OrdSet::new()` pairs are gone. Why they were safe to delete rather than correct: `resolve_top_of_stack` already clears the pass set and grants priority to the active player at the end of *every* resolution, so for echo and cumulative upkeep (controller **is** the active player) the writes were identity writes — but for **recover** the controller can be non-active, and the write was actively **yanking priority away** from whoever legitimately held it. Answering a resolution-time payment is neither a CR 117.3c action nor a CR 117.4 action-between-passes, so neither write had a rule behind it. Deleting them is also load-bearing for DP-11's design: a sweep that called a handler which reset the pass round mid-`handle_all_passed` would corrupt the very round it runs in. | correctness, deferred | filed by PB-DP1 (`scutemob-149`); **CLOSED by PB-DP4 (`scutemob-152`)** |
| **OOS-DP1-2** | **Residual missing entry priority guards, and the separate CR 606.3 sorcery-timing gap.** PB-DP1's fix cycle added an entry priority guard to `handle_turn_face_up`, `handle_activate_loyalty_ability` and `handle_level_up_class`. Still unguarded: `handle_activate_craft` (AP-gated by construction, so lower severity) and `handle_bring_companion` (its `:941` sorcery-speed gate does not imply the actor held priority; it resets `players_passed` unconditionally, so an active player who already passed can restart the pass round). **Separately**, loyalty and level-up still lack the CR 606.3 / 716.2a "only during their own main phase, stack empty" check — that is a timing gap, not a priority gap, and belongs to **DP-21**. Two PB-DP1 probes (`test_dp1_loyalty_activation_grants_actor_priority`, `test_dp1_level_up_class_grants_actor_priority`) exercise p2 acting on p1's turn and will need rewriting when that gate lands. | correctness, partial | partially closed by PB-DP1 (3 of 5 guards) |
| **OOS-DP1-3** | **Stale pre-renumber CR citations survive corpus-wide.** `116.3a/b/c/d` is the pre-renumber name of today's `117.3a-d` / `117.4`. PB-DP1 fixed every in-engine occurrence; ~60 golden-script `"note"` fields, one `cr_sections_tested` array, `docs/mtg-engine-milestone-reviews.md:326-327` and seven `memory/abilities/*.md` records still carry it. Cosmetic — batch into a doc pass, not a PB. | cosmetic | filed by PB-DP1 (`scutemob-149`) |
| **OOS-DP1-4** | **Mana abilities do not reset `players_passed` — a real CR 117.4 deviation, deliberately preserved.** CR 117.4 ends a pass round only when players pass "without taking any actions in between," and activating a mana ability *is* an action, so the non-reset is a genuine deviation rather than the settled behaviour CR 117.3b's parenthetical was cited for. Preserved under PB-DP1's explicit PRESERVE directive and now **pinned in two places**: probe `test_dp1_mana_ability_does_not_reset_players_passed` and golden script `stack/066_krosan_grip_split_second_blocks_counterspell.json`, whose all-pass round completes only because of it. Whoever closes this must edit both. | correctness, deviation | filed by PB-DP1 (`scutemob-149`) fix cycle |
| **OOS-DP2-1** | **`handle_keep_hand` never verifies that `cards_to_bottom` entries are in the player's hand.** It checks only the *count* (`rules/commander.rs:877-885`) and then moves each id from wherever it happens to be. A malformed or hostile `KeepHand` can bottom a card from the battlefield, from a graveyard, or **from another player's hand**. Needs an `obj.zone == ZoneId::Hand(player)` guard per entry, plus a duplicate-id check. | correctness, validation gap | filed by PB-DP2 (`scutemob-150`) |
| **OOS-DP2-2** | **Starting hand size is hard-coded to 7.** `handle_take_mulligan` draws `for _ in 0..7`; CR 103.5 says "equal to their **starting hand size**", and CR 103.5a (Vanguard) plus any starting-hand-size-modifying effect can change it. No `starting_hand_size` exists on `PlayerState`. Adding one is a **HASH bump**, so it is its own PB. | correctness, deferred (wire) | filed by PB-DP2 (`scutemob-150`) |
| **OOS-DP2-3** | **All engine shuffles are predictable from public state.** `timestamp_counter` is a hashed, replayable field, so any client that can compute the state can compute every future shuffle — including its opponents' libraries. Pre-existing and engine-wide (4 sites), **not** introduced by PB-DP2; the deck order is already deterministic from `build_initial_state`. Bears on Architecture Invariant 7 and M10's hidden-information story: a networked build needs a server-held secret seed. | security / hidden-info, M10-gated | filed by PB-DP2 (`scutemob-150`) |
| **OOS-DP2-4** | **The seeded-shuffle idiom is copy-pasted at 4 sites** (`effects/mod.rs:3048`, `:3147`, `:8701`, and now `rules/commander.rs`). A `GameState::shuffle_library_seeded(&mut self, player)` on `state/mod.rs` would dedupe it. Deliberately not done in PB-DP2 (plan §4.4): the extraction touches three proven call sites with different error-handling contracts — `move_zone_all_then_shuffle` uses `expect_move_object_to_zone`, which swallows move errors, the opposite of what the mulligan handler needs per MR-M9-12. Batch into a cleanup pass. **PB-DP2 review addendum (finding 3):** `StdRng` is not algorithm-stable across `rand` major versions — a future `rand` bump would silently re-permute every seeded shuffle with no fingerprint gate catching it (`PROTOCOL_SCHEMA_FINGERPRINT` digests the type closure, not shuffle output), and PB-DP2 widens the blast radius from a single effect to the opening library order of every game. The eventual dedup helper should pin the PRNG (`rand_chacha::ChaCha8Rng`, or an in-tree Fisher-Yates) rather than `StdRng` (SR-8/SR-9b). | cosmetic / refactor | filed by PB-DP2 (`scutemob-150`); addendum filed by PB-DP2 review |
| **OOS-DP2-5** | **`RandomBot`/`HeuristicBot` send `KeepHand { cards_to_bottom: vec![] }` unconditionally** (`crates/simulator/src/random_bot.rs:240-242`), which `handle_keep_hand` rejects after a 2nd mulligan. Unreachable today — the whole mulligan path is gated off in the simulator (`legal_actions.rs:186` needs `turn_number == 0`, but `GameStateBuilder` defaults it to 1; see the in-source statement at `crates/simulator/src/local_game.rs:569-574`) — but it goes live the moment M11-local Session 2 sets `turn_number = 0`. | correctness, latent; M11-local S2 owns | filed by PB-DP2 (`scutemob-150`) |
| **OOS-DP2-6** | **The engine defers CR 103.5's bottoming from take-time to keep-time.** CR 103.5 puts shuffle → draw → bottom N all inside "take a mulligan"; the engine does `TakeMulligan` = shuffle + draw 7 and `KeepHand` = bottom `mulligan_count - 1` (the 103.5c free-first adjustment). Behaviourally equivalent — final opening-hand size is identical, and the next mulligan reshuffles the whole library anyway, so no observable difference survives — but it is a documented divergence from the CR sentence and would matter if any future effect ever observed the library between a mulligan and a keep. Record-only; no action recommended (moving the step would need a `cards_to_bottom` field on `Command::TakeMulligan` ⇒ PROTOCOL bump). | documentation / known divergence | filed by PB-DP2 (`scutemob-150`) |
| **OOS-DP2-7** | **Two more phantom `LibraryShuffled` emitters, plus a top/bottom inversion.** `ReplacementModification::ShuffleIntoOwnerLibrary` (CR 701.20) emits `GameEvent::LibraryShuffled` at `rules/replacement.rs:854` and `:965` without ever calling `Zone::shuffle` — the identical Architecture-Invariant-4 defect PB-DP2 fixed in `handle_take_mulligan`, still live at two sites, and reachable in ordinary play (unlike the mulligan). The comment at `:849` claims "Redirect to library AND shuffle the library"; only the redirect happens, and it lands the card on the library **top** (`push_back`), so a Darksteel Colossus that dies is redrawn next turn. `crates/card-defs/src/defs/darksteel_colossus.rs` is the only def using the variant; it is `known_wrong`, but its `completeness` note at `:56-59` asserts the replacement "itself is correct", which is false at the match arm — that note (and its stale header comment at `:9`) needs fixing alongside the code. The only test (`crates/engine/tests/core/card_def_fixes.rs:1115-1121`) asserts the event's presence and never the library's contents. Fix needs a seeded `Zone::shuffle` at both sites plus a position assertion in the test. **↻ `scutemob-159` (2026-07-27): confirmed at source, with two corrections.** (i) The cites are stale by ~220 lines — the live sites are `rules/replacement.rs:1084-1092` and `:1181-1200` (the OOS-DP6-8 line-drift class again); both still push `LibraryShuffled` with no `Zone::shuffle` anywhere. (ii) **"Reachable in ordinary play" is true of the engine site and false of the game**: `darksteel_colossus.rs` is the only def using the variant and it is `known_wrong`, so `validate_deck` rejects it and no legal deck reaches the bug. Ranks below the live-wrong-on-`Complete` items; queued as **PB-DX18** with OOS-DP2-4 and OOS-DP2-8. | correctness, **gated by `known_wrong`** (not live on a `Complete` path) | filed by PB-DP2 (`scutemob-150`) review |
| **OOS-DP2-8** | **CR 103.5's mulligan cap ("until their opening hand would be zero cards") is unenforced.** `handle_take_mulligan` (`rules/commander.rs:802-877`) has no cap; `handle_keep_hand` computes `required_bottom = mulligan_count.saturating_sub(1)` (`:901`), so past 8 mulligans `required_bottom` exceeds hand size and `KeepHand` becomes unsatisfiable from the hand (long before that, the draw loop at `:864-868` silently short-draws on an exhausted library). Pre-existing, not introduced by PB-DP2. Needs a cap in `handle_take_mulligan`; no wire change. | correctness, latent | filed by PB-DP2 (`scutemob-150`) review |
| **OOS-DP3-1** | **Escalate derives a contiguous mode set `0..=count` from an empty `modes_chosen`.** CR 702.120a permits *any* set of `count + 1` distinct modes; `rules/resolution.rs:321-334` always takes the first `count + 1` in printed order, so a Blessed Alliance escalated once can never be "gain 4 life + opponent sacrifices an attacker" (modes 0 and 2). PB-DP3 validates the derived *count* against `min_modes`/`max_modes` but deliberately leaves the *identities* alone — that is escalate's semantics, not DP-4's. **Not reachable from a legal deck**: both escalate defs (`blessed_alliance.rs:102`, `collective_resistance.rs:99`) are `Completeness::partial`, so `validate_deck` blocks them; no `Complete` card is live-wrong through this path. Fix = require explicit `modes_chosen` on escalate casts and cross-check `modes_chosen.len() == count + 1`. Blast radius: 9 tests in `mechanics_e_l/escalate.rs`, `primitives/pb_ac4_per_mode_targeting.rs:834` (whose error-message assertion depends on the current path), 2 `partial` defs, golden script `stack/148`. No wire change. | correctness / agency loss, gated by `partial` | filed by PB-DP3 (`scutemob-151`) |
| **OOS-DP3-2** | **A modal *Spell* with `min_modes: 0` cast with zero modes announced is unrepresentable, and PB-DP3 hard-rejects it.** CR 700.2a permits announcing zero modes on "choose up to N"; the engine cannot express it because `rules/resolution.rs:335-338` maps an empty `modes_chosen` on a Spell stack object to `vec![0]`, and `StackObject.modes_chosen` is a bare `Vec<usize>` (`crates/card-types/src/state/stack.rs:413`) with no discriminator to separate "controller chose zero" from a free-cast that never announced anything. Latent — no such card exists (the corpus's only `min_modes: 0` object is the *triggered* `hullbreaker_horror.rs:35-59`), and the **activated** path handles the shape correctly already (`rules/abilities.rs` leaves the base effect, resolving no mode). The Spell-rejects / Activated-accepts asymmetry is deliberate and documented at both code sites. Fix needs a discriminator (`Option<Vec<usize>>` on `StackObject`, or a `modes_announced` flag) ⇒ **HASH bump**, so it is its own PB. | correctness, deferred (wire) | filed by PB-DP3 (`scutemob-151`) |
| **OOS-DP3-3** | **Four free-cast producers bypass mode announcement entirely.** `rules/copy.rs:386` (cascade), `copy.rs:614` (discover), `rules/resolution.rs:5167` (cipher copy) and `resolution.rs:5837` (suspend free-cast) build `StackObjectKind::Spell` objects with `modes_chosen: vec![]` — the last two via `StackObject::trigger_default` (`stack.rs:517-555`), which zero-fills the field — without ever calling `handle_cast_spell`. PB-DP3's cast-time guard therefore cannot reach them, and they still auto-select mode 0 via `resolution.rs:335-338`. **This is why that fallback must stay live**; deleting it as apparent dead code would make every suspended or ciphered modal spell resolve nothing. Note the correction: PB-DP3's plan originally named `rules/engine.rs:2112/2176/2686/2853` here, but those build **RingAbility / RoomAbility / LoyaltyAbility / ClassLevelAbility** stack objects and cannot reach the arm at all — the list above is the verified one. Already covered by **DP-20**; cross-referenced from its §5 row. | correctness (DP-20 scope) | filed by PB-DP3 (`scutemob-151`), site list corrected by its review |
| **OOS-DP3-4** | **Modal *triggered* abilities auto-select mode 0 at queue time, and the "choose up to one" branch is dead code.** `rules/abilities.rs:8419-8429` sets `stack_obj.modes_chosen = vec![0]` for every modal trigger with at least one mode. Its `if min_modes == 0 { vec![0] } else { vec![0] }` has **two identical branches** — the "choose up to one" case was written and then not honoured, so `hullbreaker_horror` (CR 700.2b) always bounces something and can never decline. CR 700.2b also says "If no mode is chosen, the ability is removed from the stack", which the engine never does. Adjacent to **DP-6** (trigger-target auto-selection); ~~bundle with **PB-DP8**~~ — **PB-DP8 deliberately did NOT bundle it**; rank it as **PB-DP8b**, where it costs one `BlockingDecision` variant, one `Command`, one `LegalAction` arm and zero new consult sites. Reasoning in **OOS-DP8-7**. | correctness / agency loss | filed by PB-DP3 (`scutemob-151`) |
| **OOS-DP3-5** | **The cast-time `ModeSelection` lookup is neither face-aware nor half-aware.** `rules/casting.rs:3495-3506` reads `def.abilities` directly rather than the active face (the PB-OS4b/PB-RS4 contract) and ignores `def.adventure_face`, while `rules/resolution.rs:246-264` *does* consult the adventure face for modes. **Second sub-case found in review:** the lookup also ignores `casting_with_aftermath`, even though the `requirements` lookup ~120 lines below does branch on it — so an aftermath cast of a card with a modal front half would now be **hard-rejected** rather than silently mis-validated. Latent both ways: the intersection of the 41 `min_modes` defs with the adventure/aftermath defs is **empty** (verified by set intersection, twice). Same root-cause class as OOS-OS4-2 / OOS-RS-3. | correctness, latent | filed by PB-DP3 (`scutemob-151`), aftermath sub-case added by its review |
| **OOS-DP3-6** | **An escalate `count` larger than `modes.len() - 1` is silently clamped, not rejected.** `rules/casting.rs:3538` and `rules/resolution.rs:332-333` both `.min(modes.len())`, so paying escalate ×5 on a 3-mode spell costs 5 extra payments and yields 3 modes. CR 702.120a's additional cost is "for each mode you choose beyond the first", and a player cannot choose 6 modes of a 3-mode spell — the announcement is illegal, not clamp-able. The current behaviour is pinned as intended by `mechanics_e_l/escalate.rs::test_escalate_modes_exceed_available_clamped`, so closing this must edit that test. Latent for `Complete` cards (both escalate defs are `partial`). Fix belongs with OOS-DP3-1's escalate PB. | correctness, latent | filed by PB-DP3 (`scutemob-151`) review |
| **OOS-DP3-7** | **Replay-harness cast-action mode parity (DP-24 class).** Only `cast_spell`, `cast_spell_modal`, `cast_spell_entwine` and `cast_spell_escalate` can announce modes; ~28 alt-cost cast arms in `crates/engine/src/testing/replay_harness.rs` (flashback, evoke, bestow, miracle, escape, foretell, plot, warp, pitch, overload, retrace, jump-start, aftermath, prototype, dash, blitz, impending, emerge, spectacle, surge, cleave, mutate, morph, commander free-cast, …) hard-code `modes_chosen: vec![]`. Before PB-DP3 that silently discarded the script's `modes` field; **after** it, those actions can never cast a modal card at all — the cast is hard-rejected. Latent: no corpus card is both modal and alt-cost-castable. A note at the `cast_spell` arm records the asymmetry. | test-infrastructure gap, latent | filed by PB-DP3 (`scutemob-151`) review |
| **OOS-DP3-8** | **The `entwine_paid` arm bypasses all mode validation.** `rules/casting.rs:3510-3516` passes `modes_chosen` through unsorted and unchecked when entwine is paid, so out-of-range or duplicate indices reach the `StackObject`. Harmless today — resolution ignores `modes_chosen` entirely under entwine (`resolution.rs:313-316`, CR 702.42a expands to all modes) — but it is now the **only** unvalidated arm of the post-PB-DP3 match, and `rules/modal.rs::test_modal_entwine_overrides_modes_chosen` pins the pass-through. Deliberately left alone (plan §11 risk 8): entwine's own keyword validation lives at `casting.rs:2845+` and is not DP-4's business. | cosmetic / hardening | filed by PB-DP3 (`scutemob-151`) review |
| **OOS-DP3-9** | **`mtg-fuzzer` aborts with a stack overflow, and long games trip `stack_consistency`.** `cargo run --release --bin mtg-fuzzer -- --games 15 --seed 1` dies with `fatal runtime error: stack overflow, aborting` (SIGABRT / exit 134). **Pre-existing, not a PB-DP3 regression** — reproduced identically on `main` (`7e0596a5`) in a scratch clone at seeds 1 and 7, and confirmed again on this branch. `--games 5` completes, so it is game-count- or game-length-dependent rather than a startup fault. That same 5-game run emits a flood of `[stack_consistency] Object ObjectId(NNNN) in stack_objects but not in Stack zone (turn 191)` invariant violations, i.e. `state.stack_objects` and the `Stack` zone have diverged. Both symptoms sit in the 150-200+ turn regime that **OOS-M11-3** (fuzzer not run-to-run deterministic) already flags, and a `stack_objects`/zone divergence is a plausible shared root cause for all three — worth investigating together. **The crash reports it writes are not replayable**: each `crash-reports/crash_N.json` records `"total_commands": 10868` alongside an **empty** `"command_history": []`, so the one artifact meant to make a violation reproducible carries no reproduction — and the file's own `Replay violations with: mtg-fuzzer --replay <SEED>` hint leans on the seed determinism that OOS-M11-3 says is absent at exactly these turn counts. That is an `event-log-diagnosability` audit concern as much as a fuzzer one. Minor hygiene rider: `crash-reports/` is not in `.gitignore`, so a fuzzer run leaves untracked artifacts in a worker's tree. **Consequence for this suite**: the fuzzer binary is not currently usable as a smoke test for simulator-side changes (PB-DP3's bot-side edit was covered by the simulator's own unit tests instead). Bears on M10a and Tier 1 hashing. | correctness / test-infrastructure, pre-existing | filed by PB-DP3 (`scutemob-151`) `/review` |
| **OOS-DP4-1** | **A hybrid / Phyrexian / X attack tax is unpayable and is now hard-rejected.** `Command::DeclareAttackers` has no `hybrid_choices` / `phyrexian_life_payments` field, so the engine cannot ask which half of a `{2/W}` tax the player pays. Before PB-DP4 the pips were **silently dropped** by `combat.rs:221-227`'s field sum (a `{2/W}` tax contributed 0 and the attack was free — the OOS-RS-2 class); PB-DP4 rejects the declaration instead, and the fix cycle rescoped the rejection so it fires only when a declared attacker actually targets the unpayably-taxed defender (attacking an untaxed opponent, or a planeswalker, is unaffected). Latent: both corpus restriction defs (`propaganda.rs`, `ghostly_prison.rs`) are pure `{2}` generic. Fix needs the two `DeclareAttackers` fields PB-RS2 added to `ActivateAbility`/`TapForMana` ⇒ **PROTOCOL bump**, so it is its own PB. | correctness, latent (wire) | filed by PB-DP4 (`scutemob-152`) |
| **OOS-DP4-2** | **CR 508.1i's mana-ability window between attack-cost determination and payment does not exist.** CR 508.1h locks in the total, 508.1i gives the active player "a chance to activate mana abilities", 508.1j then pays. The engine determines *and* pays inside one `DeclareAttackers`, so the tax must already be floating — a player with untapped lands and an empty pool cannot attack past a Propaganda. Pre-existing (PB-DP4 made the payment real but did not change the window). Fix needs a two-phase declaration ⇒ a new `Command` ⇒ **PROTOCOL bump**. The same gap exists for every "as it attacks" cost. | correctness, deviation (wire) | filed by PB-DP4 (`scutemob-152`) |
| **OOS-DP4-3** | **Goad's *directional* requirement has no CR 508.1d cost carve-out.** PB-DP4 gave both must-attack "able" tests (the `combat.rs` goad block and the `MustAttackEachCombat` block) the CR 508.1d "not required to pay" carve-out via `has_uncosted_attack_target`. The separate goad check "must attack a player other than the goading player if able" (`combat.rs:336-374`) still computes `has_non_goading_target` from opponent liveness only, so a goaded creature can be forced onto a *taxed* non-goading opponent when the untaxed goading player was the free option. Same root cause as OOS-RS3-4, narrower reach. No wire change. | correctness, narrow | filed by PB-DP4 (`scutemob-152`) |
| **OOS-DP4-4** | **The replay harness cannot script an echo or cumulative-upkeep payment.** `testing/replay_harness.rs` implements `pay_recover` (`:906-923`) and **no** `pay_echo` / `pay_cumulative_upkeep` action, so a golden script can never exercise the CR 702.30a / 702.24a *payment* branch — only PB-DP4's forced *decline* at the boundary is script-reachable. Latent today (`stack/151` is retired, `stack/152` never reaches an upkeep). DP-24 class. Deliberately not added speculatively: a harness action with no script is dead code SR-9c would flag. | test-infrastructure gap, latent | filed by PB-DP4 (`scutemob-152`) |
| **OOS-DP4-5** | **A forced decline is indistinguishable from a player's decline in the event stream.** PB-DP4's boundary sweep calls the same `handle_pay_*(pay: false)` path a real `Command::PayEcho { pay: false }` takes, so the emitted `CreatureDied` / `RecoverDeclined` carry no marker saying "the engine declined this for you because you never answered." A playtester cannot tell a rules outcome from an engine default. Fixing it inside the engine needs an event field ⇒ **HASH/PROTOCOL bump**; the cheap fix is §9 rec 8's "engine chose this for you" annotation, derived client-side in M11-local Session 7 from the absence of a preceding `Pay*` command. | diagnosability / agency visibility | filed by PB-DP4 (`scutemob-152`) |
| **OOS-DP4-6** | **`ManaPool`'s restricted-mana docs cite CR 106.12, which is the wrong rule.** Live CR 106.12 is *"To 'tap [a permanent] for mana' is to activate a mana ability … that includes the {T} symbol"*. The restricted-mana rule is **CR 106.6** ("Some spells or abilities that produce mana restrict how that mana can be spent"). Stale cites survive at `crates/card-types/src/state/player.rs:20`, `:46`, `:209` and in this audit's §4.9 `ChooseCreatureType` row. PB-DP4 fixed only `:147` and `:181` (the two docs governing the API DP-10 calls). Same class as PB-DP2's stale `103.4b` and OOS-DP1-3's pre-renumber `116.3a`. Batch into a doc pass. | cosmetic / stale cite | filed by PB-DP4 (`scutemob-152`) |
| **OOS-DP4-7** | **Three near-duplicate mana-cost arithmetic helpers.** `rules/engine.rs` `multiply_mana_cost` (multiplies every field including hybrid/Phyrexian/X — correct for cumulative upkeep), `rules/combat.rs`'s new `add_mana_cost` (rejects those fields — correct for an attack tax), and a third copy in `crates/simulator/src/legal_actions.rs` (which must mirror the engine's exactly or the SR-38 affordability gate disagrees with the engine and a bot's `PayCumulativeUpkeep { pay: true }` starts getting silently rejected). A `ManaCost` `impl Add`/`Mul` in `crates/card-types` would dedupe all three — but the semantics genuinely differ on hybrid/Phyrexian, so a naive merge would reintroduce the free-pip class (OOS-RS-2). Needs an argued API, not a rename. | cosmetic / refactor | filed by PB-DP4 (`scutemob-152`) |
| **OOS-DP4-8** | **`StubProvider` offers attack targets the player cannot pay the tax for.** `LegalAction::DeclareAttackers { eligible, targets }` (`legal_actions.rs:55-58`) lists every live opponent and opponent planeswalker with no reference to `CantAttackYouUnlessPay`, so `RandomBot::choose_attackers` routinely composes a declaration the engine rejects — and `driver.rs` answers the rejection with a silent `PassPriority`, so the bot simply loses its combat. Pre-existing (the affordability check predates PB-DP4), but PB-DP4's real debit makes it bite more often: the same floating mana no longer funds a second combat phase. SR-38 class. Fix = filter `targets`, and/or return a per-target tax so the bot can budget. Simulator-only, no wire change. | simulator move-generation gap | filed by PB-DP4 (`scutemob-152`) |
| **OOS-DP4-9** | **The echo and recover payment paths emit no `ManaCostPaid`.** PB-DP4 gave the attack tax a `GameEvent::ManaCostPaid` (Architecture Invariant 4: a pool debit is a state change and must be evented). `handle_pay_echo` and `handle_pay_recover` debit the pool and emit only `EchoPaid` / `RecoverPaid`, neither of which carries the cost; `handle_pay_cumulative_upkeep`'s `Life` arm does emit `LifeLost` but its `Mana` arm emits nothing for the debit. Deliberately not changed in PB-DP4 to keep the event-stream delta minimal for a PB whose blast radius already spans two subsystems. Wire-neutral (the variant exists); the only risk is tests that count events exactly. | diagnosability / Invariant 4 | filed by PB-DP4 (`scutemob-152`) |
| **OOS-DP4-10** | **`ActiveRestriction.controller` is captured at ETB and never recomputed for `CantAttackYouUnlessPay`.** `rules/replacement.rs:2179-2183` sets `controller` once, when the ability registers its restriction. If control of the source (e.g. Propaganda) changes hands afterward, the attack tax keeps applying against the *original* controller rather than the current one. Pre-existing since PB-18 (the affordability-only check already read this field); PB-DP4 makes it charge real mana, raising the stakes of a wrong answer from "an incorrect rejection" to "an incorrect debit, or an incorrectly-skipped debit". Fix needs either a per-check recompute of current control at declare-attackers time, or a broader audit of whether other `ActiveRestriction` variants share the staleness. No wire change. | correctness, narrow, pre-existing | filed by PB-DP4 (`scutemob-152`) fix cycle |
| **OOS-DP4-11** | **A forced decline can strand the game on an unreachable `ChooseReplacement` wait.** If an echo or cumulative-upkeep permanent has 2+ applicable zone-change replacement effects when `force_resolve_overdue_payments` (or a direct `Pay*{pay:false}`) declines it, the `ZoneChangeAction::ChoiceRequired` arm in `handle_pay_echo` / `handle_pay_cumulative_upkeep` pushes a `PendingZoneChange` and emits `ReplacementChoiceRequired` — but no `LegalAction` exists for `Command::ChooseReplacement` in `crates/simulator`, so a bot or an M11-local seat can never answer it and the permanent sits in limbo. Exotic (needs 2+ registered zone-change replacements on the same permanent) and pre-existing on the manual `PayEcho{pay:false}` path; PB-DP4's automatic sweep reaches it without any player action, widening exposure. Fix needs `LegalAction` coverage for `ChooseReplacement` — simulator-only, no wire impact. | correctness / M11-local gap, exotic | filed by PB-DP4 (`scutemob-152`) fix cycle |
| **OOS-DP4-12** | **The DP-11 deadline can be postponed indefinitely by keeping the stack non-empty.** `force_resolve_overdue_payments` fires only in `handle_all_passed`'s stack-**empty** branch. If any player (not necessarily the one who owes) casts a spell or activates a non-mana ability before the round would otherwise end, the stack is non-empty and the deadline does not fire that round — repeatably, for as long as something keeps landing on the stack. Meanwhile the permanent (or recover card) survives in its pre-consequence state: tappable, sacrificable to another cost, targetable, millable, re-triggerable. Bounded only by the postponing player's resources, not by a fixed number of rounds. The eventual outcome is still CR-correct (CR 118.12a); only the *timing* is looser than "one extra round" suggests. **No fix prescribed** — this is the shape of the CR 608.2d deviation already accepted as the smallest available under the no-new-`Command` constraint; a fix needs either a new `Command` (a stack-empty check independent of priority) or a more aggressive deadline design. Documented precisely in `engine.rs`, `resolution.rs` and `state/mod.rs`. | design-deviation, documented | filed by PB-DP4 (`scutemob-152`) fix cycle |
| **OOS-DP4-13** | **Three PB-DP4 test-hardening gaps, from the closing `/review`.** (a) **No parity test binds the simulator's `multiply_mana_cost` copy to the engine's.** `legal_actions.rs`'s copy must mirror `rules/engine.rs`'s private original exactly or the SR-38 contract breaks *silently*: the provider offers a `PayCumulativeUpkeep { pay: true }` the engine then rejects, and `driver.rs` substitutes a `PassPriority`, so the bot just loses the permanent with no error. A doc comment says so; nothing enforces it. This is the executable half of **OOS-DP4-7**. (b) **CR 508.1j's "partial payments are not allowed" is unpinned** — the only multi-source attack-tax test is exactly funded; there is no underfunded multi-attacker probe asserting `Err` **and** an untouched pool. Structurally safe today (affordability precedes every mutation and the debit is one atomic `pay_cost`), but the safety is a property of the current code shape, not a tested invariant. (c) **Two edge cases uncovered**: a zero-cost attack tax (does the `ManaCost::default()` skip emit a spurious `ManaCostPaid { cost: 0 }`?) and a pending payment owed by a player who has already lost (verified by inspection to drain safely — `turn_order` is never pruned on loss — but not pinned by a test). All three are test debt, not defects; no wire change. | test-coverage | filed by PB-DP4 (`scutemob-152`) closing `/review` |
| **OOS-DP5-1** | **`Command::OrderReplacements` has no `LegalAction`.** `crates/simulator/src/legal_actions.rs` never offers it — `crates/simulator` and `tools/` contain **zero** occurrences of `OrderReplacements` — so no bot and no M11-local seat can ever answer a CR 616.1 prompt, for a **draw or a zone change**. The pre-existing `pending_zone_changes` path has the identical hole, so PB-DP5 did not create it; it made it matter, because there is now a second prompt kind that expects an answer. Same class as PB-DP4's §9 recommendation and **OOS-DP4-11** (`ChooseReplacement`). Simulator-only, no wire change. **Fold in here**: a `PendingDraw` is selected FIFO (`.position(|p| p.player == player)`), so a player holding two outstanding pending draws cannot say *which* one they are answering — an answer aimed at the newer entry is silently applied to the older. Card-neutral today and harmless while both entries are draws of the same shape, but a per-entry discriminator belongs in whatever surface finally exposes the choice. | agency / move-generation gap | filed by PB-DP5 (`scutemob-153`) |
| **OOS-DP5-2** | **No deadline for an unanswered `PendingDraw` / `PendingZoneChange`.** PB-DP4's `force_resolve_overdue_payments` has no CR 616.1 analogue. Deliberately **not** built in PB-DP5: with no `LegalAction` (OOS-DP5-1) a sweep would fire in ~100% of automated games and become a new "the engine chose for you" site — precisely the thing this PB exists to remove — for zero benefit today, since DP-5 is unreachable from a legal deck. Should ship **together with** OOS-DP5-1 as one PB. **Fold in here**: `pending_draws` entries are never cleaned up either — not at end of turn, not on player loss, not when the replacement's source leaves the battlefield. Not exploitable (every submitted id must still be *currently* applicable, and `validate_player_active` blocks a lost or conceded sender), but a stale entry feeds the state and loop-detection fingerprints forever and hands the drawing player unbounded timing control over an owed draw. A deadline sweep is the natural place to reap them. | correctness, deferred (design) | filed by PB-DP5 (`scutemob-153`) |
| **OOS-DP5-3** | **The resume never re-offers dredge.** `resolve_pending_draw` performs the deferred draw and the `remaining` sequence with `offer_dredge: false`, so CR 702.52a is not offered on draws 2..N of a resumed sequence. Deliberate: re-offering would restart a CR 616.1 chain the player has already begun and would open a second pause with nowhere to record it. Presently unobservable — the intersection of "has a dredge card" and "has 2+ `WouldDraw` replacements" is empty, because there are **no** `WouldDraw` cards at all. | correctness, stated deviation | filed by PB-DP5 (`scutemob-153`) |
| **OOS-DP5-4** | **`PlayerState::has_drawn_for_turn` is write-only dead state, and incoherently written.** No engine logic ever reads it. It is written by `turn_actions::draw_card` — which also serves the monarch end-step draw and Ravenous, neither of which is "the draw for the turn" — and by `draw_card_skipping_dredge`, so an *effect* draw routed through a dredge decline sets it while the direct effect path does not. PB-DP5 preserved the existing writes byte-for-byte behind a `PendingDraw.sets_has_drawn_for_turn` flag rather than unifying them, because the field **is** hashed: deleting or normalising it is a HASH bump and cannot be a drive-by. | cosmetic / dead state (wire) | filed by PB-DP5 (`scutemob-153`) |
| **OOS-DP5-5** | **A deferred draw does not stop the rest of the effect.** Only the *draw sequence* stops (CR 614.11a). "Draw three, then discard three" runs its second half against a hand that does not yet hold the drawn cards — a CR 614.11a / 121.1 timing deviation. Strictly better than the status quo (before PB-DP5 the draws were destroyed outright), and the alternative — suspending mid-resolution — needs a suspendable effect resolver, i.e. exactly the pending-decision machinery §8's sequencing note calls for in PB-DP7..DP9. **↻ `scutemob-159` (2026-07-27): that machinery now EXISTS.** PB-DP9's abort-and-replay (clone at entry, restore wholesale on suspension, bank the answer, re-run the resolution from the top) is a resolution-scoped suspend, so a deferred draw could stop the whole resolution the same way a search does. The seed is no longer blocked on something unbuilt — **re-scope it before ranking, do not copy the "needs machinery" dismissal forward.** | correctness, stated deviation — **blocker removed** | filed by PB-DP5 (`scutemob-153`) |
| **OOS-DP5-6** | **`WouldDraw` honours only `SkipDraw`.** Every other `ReplacementModification` on a draw is a silent no-op that does not even emit `ReplacementEffectApplied`. This is why two draw replacements can never differ in *outcome*, and why PB-DP5's order-honoured tests had to discriminate on the event stream rather than on game state. At least three `inert` defs are blocked on it: `laboratory_maniac.rs`, `teferis_ageless_insight.rs` and `out_of_the_tombs.rs` (which names the blocker verbatim in its completeness note). Minimum widening: `DrawAdditionalCards(u32)` (Alhammarret's Archive, Teferi's Ageless Insight) + a skip-and-redirect variant (Notion Thief) + a replace-with-effect variant (Laboratory Maniac). **This is the PB that gives DP-5's machinery any card yield at all** — own PB, and the natural next step after PB-DP5. | DSL gap / card yield | filed by PB-DP5 (`scutemob-153`) |
| **OOS-DP5-7** | **`Command::ChooseDredge` has NO pending-state gate — a live, reachable exploit.** `rules/engine.rs` validates only that the player exists; `handle_choose_dredge` validates the *card* (in graveyard, has `Dredge(n)`, library ≥ n) but **never that a draw is pending**. So any player, at any time, can send `ChooseDredge { card: None }` and take a **free extra card** (`draw_card_skipping_dredge` then draws unconditionally), or send `ChooseDredge { card: Some(x) }` and dredge at will. Exactly DP-5's trust-boundary class, but **live and reachable today** — dredge defs exist (`golgari_grave_troll.rs`) and the command is script-reachable. The fix reuses PB-DP5's machinery almost verbatim: give the `DredgeAvailable` pause its own `PendingDraw`-style entry and require-and-consume it in `handle_choose_dredge`. Existing dredge tests (`tests/mechanics_a_d/dredge.rs`) and golden script `replacement/014` all reach `DredgeChoiceRequired` first, so they would stay green. **Deliberately not folded into PB-DP5** (implement-phase default-to-defer), and confirmed still true on that branch. **Arguably a higher-severity finding than DP-5 itself** — it is the only item in this seed list that is wrong in a game you could play today. **↻ `scutemob-159` (2026-07-27): re-verified end to end and RANKED SECOND (PB-DX2), bundled with OOS-DP7-2** (the doc comments on this same command that claim a pause the engine does not implement). `rules/engine.rs:534-544` validates only `validate_player_exists`; `replacement::handle_choose_dredge` (`rules/replacement.rs:2925`) validates the *card* in its `Some` arm and validates nothing at all in its `None` arm, which goes straight to `draw_card_skipping_dredge`. **The `None` arm is the sharper half**: it needs no dredge card, no graveyard contents and no pending draw — only a player who exists. | correctness / trust boundary, **live**, **ranked PB-DX2** | filed by PB-DP5 (`scutemob-153`) |
| **OOS-DP5-8** | **The `AutoApply` draw arm applies an effect and emits nothing.** With exactly one applicable non-`SkipDraw` `WouldDraw` replacement, the effect is neither applied nor recorded — no `ReplacementEffectApplied`, no `already_applied` entry, no event at all. A player watching the log cannot tell the replacement exists. Same diagnosability class as **OOS-DP4-5**. Fold into the OOS-DP5-6 widening PB. | diagnosability | filed by PB-DP5 (`scutemob-153`) |
| **OOS-DP5-9** | **`already_applied` is not threaded out of the CR 616.1f re-check, so a re-offered id can be applied twice.** `perform_one_draw` records into `PendingDraw` the `already_applied` set it was *called* with, not any id that `check_would_draw_replacement`'s own internal re-check auto-applied on the way to the `NeedsChoice`. For an applicable set of `{S: a non-SkipDraw self-replacement, X, Y}` — CR 616.1a makes `determine_action` return `AutoApply(S)`, and the re-check then yields `NeedsChoice` on `{X, Y}` — the pushed entry's `already_applied` is **empty**, so on resume `find_applicable` re-offers `S` and a client can submit an id that was never in the offered `choices`, applying `S` a second time (CR 614.5 says each replacement applies once). **Inherited, not introduced**: `check_zone_change_replacement` has the identical gap and documents it as the registered M10 follow-up. **Unobservable today** — every non-`SkipDraw` draw modification is a game-state no-op, so "applied twice" and "applied once" are indistinguishable, which is exactly why this must be closed *with* **OOS-DP5-6**: the widening that makes draw modifications observable is the change that makes this a live double-application. Documented at the `perform_one_draw` doc comment. **Second, cosmetic item folded in:** `perform_one_draw`'s `expect_move_object_to_zone` failure path returns `DrawStepOutcome::Completed`, so a corrupted-state move failure is reported as a completed draw and a sequence resume keeps iterating; debug-assert-only, and the pre-existing `draw_one_card` had the same shape. | correctness, latent (blocked on OOS-DP5-6) | filed by PB-DP5 (`scutemob-153`) closing `/review` |
| **OOS-DP6-1** | **A card-def intervening-if is silently DROPPED when the ability is lowered into a runtime `TriggeredAbilityDef` — checked at neither end. Live, deck-legal, and it makes Aurelia the Warleader an infinite-combat engine.** `testing::replay_harness::build_face_ability_vectors` lowers certain `TriggerCondition`s (`WhenDies`, `WhenAttacks`, `WhenBlocks`, `WhenDealsCombatDamageToPlayer`, …) into `TriggeredAbilityDef`s and hardcodes `intervening_if: None` at all 34 push sites, because the card-def field is `Option<Condition>` and the runtime field is `Option<InterveningIf>` — different types, so the condition cannot ride along (self-documented at `replay_harness.rs:~2315`). The trigger is then dispatched by `collect_triggers_for_event` (`abilities.rs:3899`) and resolved by `resolution.rs:2205-2239`, **both of which read the runtime field** and therefore both see `None`. **Not test-only** despite the "only in `enrich_spec_from_def` for tests" comments scattered nearby: the same lowering is reached from `rules/resolution.rs:720` and `rules/face.rs:104` on the live permanent-creation path. **CONFIRMED BY EXECUTION, not source reading** (the PB-DP6 plan mandated this probe; it was run at close-out): using the real `aurelia_the_warleader` def and letting the engine drive itself — no state pokes — Aurelia attacked, her `WhenAttacks` trigger fired legitimately in combat 1 (`IsFirstCombatPhase` true) granting an extra combat; the engine itself then set `in_extra_combat = true` and reached `DeclareAttackers` of that extra combat; Aurelia attacked again and **the trigger fired a second time**, `additional_phases 0 → 1`, granting a *third* combat. A paired control/probe that set `in_extra_combat` directly produced **byte-identical** results in both arms (`dummy_untapped=true, additional_phases=1`), ruling out "consulted but mis-evaluated" and leaving only "never consulted". **`aurelia_the_warleader.rs` carries no `completeness` field, so it defaults to `Complete` and `validate_deck` admits it** — this is a live unbounded loop on a legal card in a popular commander, presumably terminating only via CR 104.4b loop detection. `karlach_fury_of_avernus.rs` (`known_wrong`) and `tatyova_steward_of_tides.rs` (`partial`) share the defect but are deck-blocked. **PB-DP6 deliberately did not fix it**: the clean fix is an `Option<Condition>` field on `TriggeredAbilityDef`, which sits inside `Characteristics` and is hashed (`state/hash.rs:3337`) ⇒ **HASH bump**, out of bounds for a no-wire PB. Alternatives: (b) re-route these conditions onto a `CardDefETB`-style dispatch that re-reads the registry — this is what the adjacent `WhenExertedAsAttacks` block already does, which is why *it* reaches PB-DP6's gate and `WhenAttacks` does not; (c) a no-bump index-correspondence trick (`build_face_ability_vectors` appends one runtime entry per matching card-def ability in `def.abilities` order, per condition, so the *k*-th runtime `SelfAttacks` entry maps to the *k*-th `WhenAttacks` card-def ability) — derivable at the queue site but fragile: it must be mirrored at resolution and re-derived on every face change. **Worse than DP-15 itself and the strongest candidate to rank next in this suite.** **↻ `scutemob-159` (2026-07-27): re-verified and RANKED FIRST across the merged inventory (PB-DX1).** `aurelia_the_warleader.rs:32-33` carries `intervening_if: Some(Condition::IsFirstCombatPhase)` and no `completeness` field; `build_face_ability_vectors` (`testing/replay_harness.rs:2382`) still hardcodes `intervening_if: None` at all **34** push sites; the lowering is reached from `rules/face.rs:104` and `rules/resolution.rs:864`, both live paths. CR 603.4 re-read via MCP — the check is required at both ends and happens at neither. **This is the only open seed among the 204 that is simultaneously live-wrong, on a `Complete` def, and unbounded.** Take alternative (a) or (b); **not (c)** — OOS-DP6-2 is a live example of an index space assumed and wrong. | correctness, **live-wrong on a `Complete` def**, deferred (wire), **ranked PB-DX1** | filed by PB-DP6 (`scutemob-154`); confirmed by execution at close-out; ranked first by `scutemob-159` |
| **OOS-DP6-2** | **`rules/abilities.rs:6252`'s `WheneverYouSacrifice` `retain` post-filter indexes the card-def ability list with a *runtime* index.** The trigger it filters was queued by `collect_triggers_for_event`, whose `ability_index` is into `characteristics.triggered_abilities`; the retain looks the ability up with `def.effective_abilities(is_transformed).get(t.ability_index)` — the card-def index space. PB-DP6 deliberately declined to add its CR 603.4 gate here for exactly this reason: reading `intervening_if` through a possibly-wrong index could suppress the *wrong* trigger, which the batch's hard constraint 3 forbids. This is the one Category-C site in PB-DP6's 82-site partition. The mismatch is pre-existing and independently worth fixing; closing it is the precondition for gating this site. | correctness, latent | filed by PB-DP6 (`scutemob-154`) |
| **OOS-DP6-3** | **Two `partial` blocker notes name the wrong DSL type and are stale.** `garruks_uprising.rs:68-71` and `inventors_fair.rs:74-77` say the card is blocked because **`InterveningIf`** offers only `ControllerLifeAtLeast` / `SourceHadNoCounterOfType`. But the def-level field is `Option<Condition>` (the card-def mechanism), not `Option<InterveningIf>` (the runtime one) — and `Condition::YouControlNOrMoreWithFilter`, which is what both cards need, already exists and is already used by six shipped defs. `ophiomancer.rs:54` records the same realisation for its own case ("Blocker stale"). Both cards are plausibly authorable today. Re-triage them; **do not** treat this as a DSL gap. **↻ `scutemob-159` (2026-07-27): chain-verified, and the seed under-states itself twice.** `Condition::YouControlNOrMoreWithFilter` is used by **21** shipped defs, not six (`card_definition.rs:3834`); and both notes' hedge that "the trigger-time half remains blocked" is now **false** — the variant is in `condition_is_queue_time_evaluable`'s true set (`effects/mod.rs:10139`) and PB-DP6 wired the card-def `intervening_if` into all 14 queue sites, so **both halves of CR 603.4 are available today**. `inventors_fair` additionally records that its search ability "currently permits illegal activation" and that `activation_condition: Some(Condition::YouControlNOrMoreWithFilter)` is the already-expressible fix. **2 flips, 0 engine lines, 0 wire — the cheapest item in the 204-seed inventory.** Ranked **PB-DX3**. | documentation / **card yield, zero engine** | filed by PB-DP6 (`scutemob-154`) |
| **OOS-DP6-4** | **A trigger suppressed by CR 603.4 is invisible in the event stream.** After PB-DP6 the ability simply never appears — there is no `GameEvent` distinguishing "the intervening-if was false, so it never triggered" from "this card does nothing". A playtester cannot tell a rules outcome from a bug. Same diagnosability class as **OOS-DP4-5** (forced decline vs player decline). Cheap fix is client-side derivation in M11-local; an engine-side event is a **PROTOCOL bump**, so it was deliberately deferred by a no-wire PB. | diagnosability | filed by PB-DP6 (`scutemob-154`) |
| **OOS-DP6-5** | **`PendingTriggerKind::TurnFaceUp` never re-checks its intervening-if at resolution.** It routes to `StackObjectKind::TurnFaceUpTrigger`, whose resolution path (`rules/resolution.rs:7369`) has no CR 603.4 second-sentence check, unlike the `TriggeredAbility` path. PB-DP6 gave the site a *queue-time* gate (its A12); the resolution half stays open, so the two halves of CR 603.4 are asymmetric here. Latent: no corpus `WhenTurnedFaceUp` def carries an `intervening_if`. | correctness, latent | filed by PB-DP6 (`scutemob-154`) |
| **OOS-DP6-6** | **`WasBargained` / `EvidenceWasCollected` / `GiftWasGiven` / `WasOverloaded` / `WasCleaved` are never propagated into a triggered ability's resolution context.** `rules/resolution.rs`'s trigger-context builder populates eight `EffectContext` fields and none of these — even though `GameObject.was_bargained` / `.evidence_collected` are set earlier specifically so "ETB triggers can check" them (the comments say so). Any def using one as an ETB intervening-if would read **false at both ends**. PB-DP6 classifies all five **non-evaluable** at queue time (so they default to "queue anyway") rather than making it worse. Zero corpus exposure today. Fix is a small ctx extension at the resolution site plus removing them from `condition_is_queue_time_evaluable`'s `false` set. **Same root-cause family as PB-DP6's own headline `WasKicked` finding** — close them together. | correctness, latent | filed by PB-DP6 (`scutemob-154`) |
| **OOS-DP6-7** | **A triggered *mana* ability that resolves inline has no resolution-time intervening-if check.** `rules/mana.rs:843-852` executes the effect directly rather than using the stack (CR 605.4a). PB-DP6 gates it at trigger time (its A6b); because it never goes on the stack there is no second moment at which to re-check, which is arguably correct — nothing can change in between. Recorded so a future reader does not mistake the asymmetry with every other trigger path for an omission. | documentation / known divergence | filed by PB-DP6 (`scutemob-154`) |
| **OOS-DP6-8** | **This audit's §4.8 site cites were stale by 300-400 lines, and the staleness propagated into the card corpus.** `replacement.rs:1446-1456` was really `:1834-1839`; `abilities.rs:6910-6916` was really `:6949-6955`; `resolution.rs:2119-2135` was really `:2139-2156`. Two card-def caveats had copied the stale numbers verbatim (`loyal_apprentice.rs:22`, `siege_gang_lieutenant.rs:19`). PB-DP6 corrected the two caveats and this audit's rows. Recorded as a **method** note, not a one-off: a site cite in this document is a snapshot, and any PB that opens with "the audit says line N" should re-derive N before trusting it. Same class as OOS-DP1-3 and OOS-DP4-6 (stale CR cites). | documentation / method | filed by PB-DP6 (`scutemob-154`) |
| **OOS-DP6-10** | **`WasKicked` / `XValueAtLeast` are classified queue-time-evaluable but read `0` at the cast-trigger site — the one suppression hazard PB-DP6 introduced.** `rules/abilities.rs:3764` (the `WhenYouCastThisSpell` gate, PB-DP6's site A9) is the only Category-A site whose `source` is a **stack object** rather than a permanent. `kicker_times_paid` and `x_value` are `GameObject` fields written once, when the spell *resolves into a permanent* (`resolution.rs:619`/`:628`), so at cast time they are still 0. A hypothetical *"When you cast this spell, if it was kicked, …"* def would therefore read the condition **false** and be **wrongly suppressed** — the hard-constraint-3 direction, i.e. the failure mode PB-DP6 exists to avoid, not the over-firing one it fixes. **Zero corpus exposure today**: no def pairs `WhenYouCastThisSpell` with any `intervening_if` (verified across all 24 `intervening_if: Some` defs), and PB-DP6 documented it in-source at the site rather than special-casing it. Unlike `SourceOnBattlefield` at the same site — which answers `false` *correctly*, because the permanent genuinely is not on the battlefield yet — this one is simply the engine storing the fact on the wrong object at this moment. Real fix is either `StackObject.kicker_times_paid` / `x_value` (**HASH bump**) or writing those fields onto the spell's `GameObject` at cast time (no bump, but it moves a field's meaning). Same root-cause family as **OOS-DP6-6** and as PB-DP6's own headline `WasKicked` zero-fill — close all three together. | correctness, latent (introduced by PB-DP6, documented in-source) | filed by PB-DP6 (`scutemob-154`) closing `/review` |
| **OOS-DP7-2** | **Two `*ChoiceRequired` doc comments assert a pause the engine does not implement.** `rules/events.rs:848-850` says of `DredgeChoiceRequired` that *"the engine pauses until a `Command::ChooseDredge` is received"*, and `rules/replacement.rs:619` repeats it. **It does not pause.** `perform_one_draw` (`replacement.rs:828`) returns `(vec![event], DredgeOffered)` and records **no** pending state; the `DrawStepOutcome` doc at `:764-767` says outright that the caller does **not** stop. `MiracleRevealChoiceRequired` has the same shape and the same suspicion (not verified). Confirmed by execution-path reading during PB-DP7 planning. This is the §9.3 "the reusable asset is the event+command+pending triple; the missing piece is the gate" claim made concrete — and it means a doc comment is currently the *only* thing asserting the property. Fix is either to wire these onto `blocking_decision` (see OOS-DP7-1) or to correct the comments; **do not leave them claiming a guarantee the code does not make.** Related but distinct from **OOS-DP5-7**, the live free-card exploit on the same command. | correctness (doc-vs-code), latent | filed by PB-DP7 (`scutemob-155`) planning |
| **OOS-DP7-1** | **Retrofit `pending_commander_zone_choices` onto `blocking_decision`.** PB-DP7 built the gate; §4.4 and **DP-32** record that the CR 903.9a commander-zone choice is honoured but "does not gate progress" — it is now a one-variant addition to `BlockingDecision` plus a `handle_commander_zone_return` clear, with no new consult site. **Deliberately narrowed to this one entry, not all six.** The three payment vectors should **stay** deadlines (PB-DP4's CR 118.12a reasoning is correct and PB-DP7's §1.1 block-vs-deadline test agrees: block only where the CR supplies no default); the dredge/miracle pair is OOS-DP7-2's problem first. | correctness, ranked candidate | filed by PB-DP7 (`scutemob-155`) |
| **OOS-DP7-3** | **Two hidden-information gaps around the cleanup discard, one pre-existing and one newly created.** (a) `GameEvent::DiscardedToHandSize.reveals_hidden_info()` returns `false` while sibling `CardDiscarded` returns `true` — both move a card from a hidden zone to a public one and both name the `ObjectId`. Pre-existing; found reading the event's neighbours while adding the new event. (b) `GameEvent::CleanupDiscardChoiceRequired` carries the **whole hand's `ObjectId` set** (it has to — that is the choice) and PB-DP7 shipped it with a doc-note rather than a mechanism: M10's filter must make this event **private to `player`**, and today nothing enforces that. Architecture Invariant 7 makes both **pre-alpha networking correctness**, not cosmetic. Check the entire `reveals_hidden_info` match for siblings before fixing either one; the PB-DP7 review's Finding 9 is (b). | correctness (hidden info), (a) pre-existing / (b) introduced-and-documented | filed by PB-DP7 (`scutemob-155`), (b) added by its review |
| **OOS-DP7-4** | **The cleanup-discard subset's order is deliberately discarded, and should carry CR 603.3b trigger order once DP-14 lands.** `handle_discard_to_hand_size` sorts to ascending `ObjectId` before discarding, so a client cannot use the subset's order as a hidden channel into same-controller madness-trigger ordering. That is the right interim call — but it also means that when **DP-14** (same-controller trigger ordering, CR 603.3b) gets a real answer, discarding two madness cards at once should let the player order the resulting triggers, and the subset order is the natural carrier. Revisit **with** DP-14, not before. | design, deferred | filed by PB-DP7 (`scutemob-155`) |
| **OOS-DP7-5** | **`PendingDecision.actions: Vec<LegalAction>` is out of runway.** §9.4 rec 2 asked for a `payload: DecisionPayload` reshape; PB-DP7's cleanup discard is the **last** decision class that still fits the flat action list (it fits only because the whole subset is packed into one `LegalAction`). A trigger-ordering decision is a permutation and a scry decision is a partition — neither is an index into an action list. **M11-local Session 3/5's call**, and it should be made before PB-DP8 rather than after. | design, ranked candidate | filed by PB-DP7 (`scutemob-155`) |
| **OOS-DP7-6** | **The TUI's cleanup-discard answer is the deterministic default, not a picker.** `tools/tui/src/play/input.rs`'s new `'d'` key submits `default_cleanup_discard`'s subset verbatim — the `count` highest `ObjectId`s, i.e. **exactly the pre-PB-DP7 auto-pick**. So a human at the TUI gets the old behaviour with an extra keystroke; the agency the engine now offers is real but unreachable from that surface. The real picker is M11-local Session 7's job (§9.4 rec 8's "engine chose this for you" annotation applies too). Not a regression — the TUI never had a choice to make before. | UX gap, deferred | filed by PB-DP7 (`scutemob-155`) |
| **OOS-DP7-7** | **§10's re-audit triggers are now DUE, and this row is the reminder.** A first pending-decision-that-blocks has landed, so §3.1's 277-def sweep must be re-derived (exclude `defs/mod.rs`; the union must include the modal-triggered-ability row). A new `Command` has landed, so the **DP-24** "accepted-and-discarded field" check applies to it — **answered, and the honest answer is "yes, narrowly"**: every *value* in `DiscardToHandSize.cards` is validated and used, but the **order** the client supplies is accepted and then thrown away (the handler sorts ascending, plan §2.3). That is deliberate and CR-argued — it stops the subset order becoming a hidden channel into CR 603.3b trigger ordering — and **OOS-DP7-4** tracks giving the order meaning once DP-14 lands. A blanket "no accepted-and-discarded fields" would have been the wrong entry. And a new `GameEvent` has landed, so the `reveals_hidden_info` sweep is due (see OOS-DP7-3). | bookkeeping | filed by PB-DP7 (`scutemob-155`); **CLOSED by PB-DP10 (`scutemob-158`)** — the 277-def re-derivation is now `crates/engine/tests/core/decision_gate.rs::decision_site_reconciliation_report`, computed and printed on every `cargo test`, ratcheted against silent growth by T4/T6 |
| **OOS-DP7-11** | **The SR-19 hashed-field gate silently skips any `HashInto` impl written path-qualified, and 5 structs are currently outside it.** `crates/engine/tests/core/hash_schema.rs`'s `every_hashed_struct_field_is_hashed_or_allowlisted` looks up impl bodies by the **bare** struct name, but `hashinto_impl_bodies()` keys them by the exact type token as written — so `impl HashInto for crate::state::stubs::Foo` yields a `None` lookup and the loop `continue`s, taking the struct out of scope with no diagnostic. **Demonstrated, not inferred**: with `PendingCleanupDiscard`'s impl path-qualified, deleting a field's `hash_into` line left the gate reporting `ok`; with the impl written bare, the same deletion made it fail by name. PB-DP7 fixed its own struct that way (the rename moves no fingerprint — PROTOCOL 28 / HASH 65 confirmed unmoved) and **left the other five**, which are pre-existing: `MergedComponent` (`game_object.rs`), `FlashGrant` / `PlayFromTopPermission` / `PlayFromGraveyardPermission` (`stubs.rs`), `SacrificedCreatureLki` (`types.rs`). A further 9 path-qualified impls are enums and out of this gate's scope by construction. Real fix is to **normalise the scanner's key on the bare name** rather than rename five call sites, so the hole cannot reopen. **This one is worth ranking**: it is a gate that reports success while checking nothing, which is the failure mode the whole SR programme exists to prevent — and it was found only because a fix cycle asserted the gate's coverage in a source comment and the closing `/review` checked the claim. **↻ `scutemob-159` (2026-07-27): re-verified live, and the counts are now exact.** The skip is at `crates/engine/tests/core/hash_schema.rs:1540-1541`. All 15 path-qualified `HashInto` impls were classified: **5 structs** outside the gate — `MergedComponent`, `FlashGrant`, `PlayFromTopPermission`, `PlayFromGraveyardPermission`, `SacrificedCreatureLki` (the seed's count is exact) — and **10 enums**, not 9, which are outside it for the *separate* reason recorded in **OOS-DP9-13**. Ranked **PB-DX7**, bundled with OOS-DP9-13 and OOS-DP10-1: one test-only batch, and the fix must normalise the scanner key on the bare name rather than rename the five call sites, so the hole cannot reopen. | correctness (gate integrity), pre-existing, **ranked PB-DX7** | filed by PB-DP7 (`scutemob-155`) closing `/review` |
| **OOS-DP7-12** | **`LocalGame` halts rather than auto-answers if a provider offers nothing while blocked.** `crates/simulator/src/local_game.rs`: an empty `legal` list falls through to `PassPriority`, which the new admission gate rejects, producing `Halted(NoLegalActions)`. Correct-by-construction today because `StubProvider` always offers the discard while blocked — but **M11-local Session 3 replaces the provider**, and at that point a provider that forgets the blocking branch turns a recoverable state into a dead game. A defined halt is better than a silent wrong answer, so this is a note-and-watch rather than a defect; the durable lesson is that **every blocking kind adds an obligation to every provider**, and the provider contract should say so. | design, latent (activates in M11-local S3) | filed by PB-DP7 (`scutemob-155`) closing `/review` |
| **OOS-DP8-1** | **`RandomBot` answers a CR 603.3d choice with the engine's default, not randomly.** `crates/simulator/src/random_bot.rs::action_to_command`'s `LegalAction::ChooseTriggerTargets` arm submits the offered `targets` verbatim. Correct for PB-DP8 (a random answer would change every fuzzer seed's outcome and destroy what is left of the pre/post A/B oracle) and wrong for bot quality: the whole point of DP-6 is that first-match is a bad pick, and the bot now makes it deliberately. A randomising `RandomBot` and a target-evaluating `HeuristicBot` belong together, with a fuzzer-baseline re-pin. | simulator quality, deferred | filed by PB-DP8 (`scutemob-156`) |
| **OOS-DP8-2** | **The TUI answers with the default, not a picker** — the sibling of OOS-DP7-6. `tools/tui/src/play/input.rs`'s new `'n'` key submits `default_trigger_targets` verbatim, i.e. exactly the pre-PB-DP8 auto-pick, so a human at the TUI seat gets the old behaviour plus a keystroke. The real per-slot picker is M11-local Session 7, which per §9.4 rec 9 must also stop believing `TargetPicker.svelte` covers trigger targets. (Note the key is `'n'`, not the plan's `'t'`: `'t'` is bound to tap-for-mana and `'g'` to the graveyard browser.) | UX gap, M11-local | filed by PB-DP8 (`scutemob-156`) |
| **OOS-DP8-3** | **Modular's trigger target is still auto-picked.** `rules/abilities.rs` (the `PendingTriggerKind::Modular` arm of the `kind` match, CR 702.43a "target artifact creature", lowest `ObjectId` on the battlefield). It sits in the `kind` match with its own `continue`, not in `trigger_targets_opt`, so PB-DP8's machinery does not reach it. One more `trigger_target_candidates` call site; cheap now that the batch has landed. | agency loss, narrow | filed by PB-DP8 (`scutemob-156`) |
| **OOS-DP8-4** | **`TargetPermanentDistinctFrom` distinctness is unenforced for triggered abilities and the comment says otherwise.** `rules/abilities.rs` returns `true` for the requirement in the battlefield scan and says distinctness is "enforced at declaration validation (casting.rs)" — true for spells, false for triggers, where nothing validates a declaration, so two such slots on one trigger get the same object. Zero corpus exposure (no `Triggered` def uses it). PB-DP8 added only the narrow cross-slot rejection in `handle_choose_trigger_targets`; the AUTO-fallback kept the defect. **Narrowed by the closing review (Finding 3)**: the two slots' *defaults* had also collided, so `default_trigger_targets` emitted an answer the engine's own check (8) rejected -- every consumer that submits the default verbatim (both bots, `StubProvider`, the harness pump, the TUI announce key) took the refusal, and `LocalGame` turns a refused fallback into a `Halted`. `make_distinct_slot_defaults` now reconciles colliding defaults at offer time and `default_trigger_targets`' doc comment states the one remaining exception rather than over-promising. **Narrowed again by the SECOND closing review (Finding 2)**: the doc's "one exception" named the wrong failure mode for half its cases. A refusal only happens when at least one slot has ≥2 candidates; when **both** slots have exactly one, `trigger_target_choice_is_forced` was satisfied (it read `candidates[0]`, never `default`, and never consulted check (8)), so the engine placed the trigger with the same permanent in both slots and **silently violated CR 601.2c** rather than refusing. That sub-case is now closed at the root: `forced_trigger_target_answer` returns the answer instead of a `bool`, `forced_answer_breaks_distinctness` applies check (8)'s exact predicate to it, and a forced answer that breaks distinctness means *no legal announcement exists*, so CR 603.3d removes the ability from the stack. **Residual, now the whole seed**: the *default-quality* half — a slot list where some slot has ≥2 candidates always HAS a legal answer, and `make_distinct_slot_defaults`' in-order greedy is not always it (assign the most-constrained slot first and it would be), so the engine can still offer a default it refuses; plus the battlefield-scan comment's spell/trigger asymmetry, unchanged. Aspirationally-wrong comment ⇒ correctness hazard per `memory/conventions.md`. | correctness, latent | filed by PB-DP8 (`scutemob-156`) |
| **OOS-DP8-5** | **CR 800.4d is unimplemented for the auto-fallback path.** A trigger controlled by a player who has *already* left the game is still put on the stack; PB-DP8 only declines to ASK such a player (`T10b` pins that), and does drop the trigger on the concede-while-blocked path where the departure is simultaneous with the announcement (`T10` pins that). Root cause is **OOS-DP7-9** (CR 800.4a object removal unimplemented; a dead player's objects persist). Closing it is a behaviour flip on every dead-player trigger and needs its own roster. | correctness, deferred | filed by PB-DP8 (`scutemob-156`) |
| **OOS-DP8-6** | **`GameEvent::private_to()` does not exist.** CLAUDE.md's Architecture Invariant 7 names it as the mechanism by which private events reach only the relevant player; `rg 'private_to' crates/` returns zero. Today the only hidden-info surface is the boolean `reveals_hidden_info()`, which cannot express "this event is for one seat". M10's filter needs the method, and **OOS-DP7-3(b)** (`CleanupDiscardChoiceRequired` broadcasting a hand's `ObjectId` set) is already waiting on it. `TriggerTargetChoiceRequired` itself is genuinely public (battlefield/graveyard objects and players only) so it owes nothing. Minimum action: correct the invariant's wording so it stops asserting a surface that does not exist. **↻ `scutemob-159` (2026-07-27): the headline premise is now STALE — `private_to()` exists**, added by PB-DP9 at `rules/events.rs:1503`. The residual is unchanged and real: it is a *declaration*, not an enforcement point — nothing consumes it, so **OOS-DP7-3(b)** is still waiting. Re-scope the seed to "`private_to()` has no consumer" before ranking it. | documentation-vs-code / M10-gated | filed by PB-DP8 (`scutemob-156`) |
| **OOS-DP8-7** | **OOS-DP3-4 (modal *triggered* abilities) was deliberately NOT bundled with PB-DP8 — rank it as PB-DP8b.** ~5 effectively-`Complete` defs. Reasons, from `pb-plan-DP8.md` §9: it is CR 700.2b/603.3c not 603.3d; CR 700.2c orders modes *before* targets and derives the target requirements *from* the chosen mode, which the ability-level `targets` field cannot express, so designing the mode round trip now would bake in a shape that must be redesigned once per-mode targets exist; and its "no mode chosen ⇒ removed from the stack" half is an unimplemented *rule* rather than an agency gap, mixing classes. On top of PB-DP8's machinery it costs one `BlockingDecision` variant, one `Command`, one `LegalAction` arm and **zero new consult sites**. | scope call, ranked candidate | filed by PB-DP8 (`scutemob-156`) |
| **OOS-DP8-8** | **§10's re-audit triggers are due again.** A new `Command` fires DP-24's accepted-and-discarded-field check — the answer is **not** one: `choice_id` is validated, every `Target` is validated against the offered candidate set, per-slot cardinality is validated, and `zone_at_cast` is deliberately taken from the engine's own candidate rather than from the wire. A new `GameEvent` fires the `reveals_hidden_info` sweep (OOS-DP7-3), answered in `pb-plan-DP8.md` §12: `false`, because every id it carries names a public-zone object or a player. Also still owed from OOS-DP7-7: §3.1's 277-def re-derivation. | bookkeeping | filed by PB-DP8 (`scutemob-156`) |
| **OOS-DP8-9** | **CLOSED IN BATCH (PB-DP8 fix cycle, review Finding 5).** `handle_concede` advanced priority/turn even while ANOTHER player's CR 603.3d announcement was outstanding. The original disposition ("deliberately left ungated; gating risks a hang") was **wrong on both halves**. It is not merely a CR-ordering wrinkle: the priority-advance block reaches `handle_all_passed` -> `resolve_top_of_stack` -> `flush_pending_triggers`, firing the `debug_assert!` that guards re-entrancy (a panic in every test and fuzzer build; in release, a spell resolving with the CR 603.3b batch half-placed), and the turn-advance block runs `enter_step`'s turn-based actions for the new step before PB-DP7's progress gate stops it. And the hang argument does not hold *for the answerer*: the outstanding entry's player is alive by construction (`flush_sorted` never asks a departed controller), so the block always has one. **Fixed**: `handle_concede`'s priority-advance and turn-advance blocks are gated on `blocking_decision(state).is_none()`. Tests `test_dp8_foreign_concede_does_not_step_over_the_suspended_batch`, `test_dp8_resume_routes_past_a_dead_active_player`. **The closing `/review` then found that this fix introduced a HIGH of its own, and the correction is recorded here because the wrong half of the argument is instructive**: the gate's justification also claimed "the resume grants priority itself (`finish_resumed_flush`)", which is FALSE for `FlushResumeSite::None` -- the resume site of all 30 in-match `check_and_flush_triggers` calls, i.e. the common case, where `finish_resumed_flush` returns without touching `priority_holder`. A conceding **priority holder** therefore left the field naming a player who can never act again (`PassPriority` from them is `PlayerEliminated`, from anyone else `NotPriorityHolder`), which is an unrecoverable game-ending deadlock for every driving loop. Closed by `abilities::repair_departed_priority_holder`, called at the end of `resume_trigger_flush`: the successor is the next player in APNAP order who has not passed, or -- if all have -- the CR 603.3b active-player grant. Tests `test_dp8_concede_under_a_suspended_batch_does_not_strand_priority` plus the assertion the original test omitted. **Lesson for DP-9: when a gate SKIPS work, enumerate what that work was going to do and say where each part is picked up.** | correctness | **CLOSED** by PB-DP8 fix cycle (`scutemob-156`) |
| **OOS-DP8-10** | **CLOSED IN BATCH, AND WIDER THAN FILED (PB-DP8 fix cycle, review Finding 4).** The seed named only the Cleanup branch's `cleanup_sba_rounds` ratchet. The CR 726 mandatory-loop check is skipped in **both** `enter_step` branches, not just the Cleanup one -- so a mandatory infinite loop involving a targeted triggered ability produced no `LoopDetected` draw at all and the engine simply cycled forever. Both halves turn a bounded pathological state into an unbounded one. **Fixed**: `PendingTriggerTargets.grant_priority_on_resume: bool` became `resume_site: FlushResumeSite` (`None` / `GrantPriority` / `EnterStepPriority` / `EnterStepCleanup`), and `finish_resumed_flush` reproduces the ratchet and the loop check per site. Tests `test_dp8_suspended_cleanup_batch_still_advances_the_ratchet`, `test_dp8_resume_runs_the_cr726_loop_check`. **Residual, not re-seeded:** the Cleanup resume ratchets unconditionally rather than under `cleanup_sba_rounds < MAX_CLEANUP_SBA_ROUNDS`, because the fall-through-at-max is `enter_step`'s to make and the next non-suspending cleanup round makes it; the CR 726 check is the real bound. | correctness | **CLOSED** by PB-DP8 fix cycle (`scutemob-156`) |
| **OOS-DP8-11** | **The spell path has PB-DP8's Finding 6 index shift too, and it was NOT fixed here.** `rules/abilities.rs`'s trigger path now pads an under-filled `TargetRequirement::UpToN` slot to its declared width (`SpellTarget::unchosen_slot`), so `EffectTarget::DeclaredTarget { index }` keeps naming the clause its card def meant. `rules/casting.rs`'s `validate_targets_inner` builds a spell's `Vec<SpellTarget>` by a two-pass best-fit map and returns it **compacted**, so a SPELL with two parallel `UpToN` slots that declines the first and fills the second still resolves the first clause against the second slot's target. Corpus exposure: no `Complete` spell today has two parallel `UpToN` slots with heterogeneous clauses (Sword of Sinew and Steel, the shape that motivates this, is a *triggered* ability). Fix is the same `flatten_slot_answers` shape applied to the casting validator, plus a decision about whether the sentinel should also appear on spell stack objects. | correctness, latent | filed by PB-DP8 fix cycle (`scutemob-156`) |
| **OOS-DP8-12** | **`SpellTarget::unchosen_slot()` is a real value in the target stream, and no display surface knows about it.** The padding sentinel is `Target::Object(ObjectId::SENTINEL)` — never a real object, so `resolve_effect_target_list_indexed` and `is_target_legal` both contribute nothing for it, which is the intended no-op. But it *is* hashed into `public_state_hash`, serialized into replay logs, and rendered by `tools/replay-viewer` and `tools/tui`, none of which special-case it: a stack object's target list will show an unresolvable object id. Only interior holes are padded (a trailing un-taken slot is omitted, so an all-empty announcement still yields an empty list and cannot trip CR 608.2b's "all targets illegal" fizzle), so exposure is limited to multi-slot `UpToN` abilities — one `Complete` def today. Add a display arm, or replace the flat `Vec<SpellTarget>` with a slot-structured carrier. | display / modelling | filed by PB-DP8 fix cycle (`scutemob-156`) |
| **OOS-DP8-13** | **A reaped entry's `FlushResumeSite` debt is dropped, not transferred.** Closing-review Finding 2 showed `flush_pending_triggers`' CR 800.4d reap discharging the departed entry's debt *inside* the current caller's flush, which then granted priority a second time (two `PriorityGiven` for one step entry, two `players_passed` resets). The fix zeroes the reaped entry's `resume_site` on the principle that *the debt belongs to a call site whose moment has passed; the current caller's own obligation is what is owed now* — which is right for the priority grant (the six guards grant themselves, and the 30 `check_and_flush_triggers` sites already hold correct priority per PB-DP1) but silently drops the other two obligations `FlushResumeSite` carries: an `EnterStepCleanup` debt loses its `cleanup_sba_rounds` bump and both `EnterStep*` debts lose their CR 726 loop check. Reachable only when a player is eliminated by a route other than `handle_concede` (a CR 704.5a/b SBA) *while* their announcement is outstanding *and* the original suspension was at a guarded site. Test `test_dp8_reap_does_not_double_grant_priority_at_a_guarded_site` pins the grant half only. Proper fix: merge the reaped debt into the caller's own rather than choosing one. **MOSTLY CLOSED by the SECOND closing review (Finding 3)**: the asymmetry was the point — losing a *bound* is a different severity class from emitting a duplicate event, and the CR 726 check is what OOS-DP8-10's own closure called "the real bound on a genuinely repeating position". The merge rule is now implemented: `run_flush_resume_obligations` was split out of `finish_resumed_flush` (ratchet + CR 726 check, returning whether it ended the game), the reap still zeroes the priority half, and it calls the obligations half explicitly with the reaped site. It was not literally the three lines the review estimated — the two halves were interleaved in one function body and the CR 726 branch can end the game, so it had to be extracted with a `bool` return rather than gated in place. Pinned by `test_dp8_reap_keeps_the_cleanup_ratchet_and_drops_only_the_priority_debt` (fail-before: `cleanup_sba_rounds` `left: 0, right: 1`). **Residual**: a reap whose continuation immediately re-suspends still loses the reaped site's ratchet bump for that round — the obligations can only run once the batch is complete (CR 726 cannot be evaluated against a half-placed CR 603.3b batch), and the current caller's own `mark_flush_resume_site` then carries its own copy of both obligations, so the bound returns on the next completing round. **Widened by the second closing review, and the widening is the opposite defect from the one this row was filed for.** Now that the reap runs `run_flush_resume_obligations(reaped_site, …)`, it can run `check_for_mandatory_loop` — which is a **counting** detector (`loop_detection.rs:67-83`, `LOOP_DETECTION_THRESHOLD = 3`), not an idempotent predicate — and `flush_pending_triggers` then returns to `enter_step`, which runs it **again** in the same step entry. If the mandatory-state hash is unchanged between the two calls the counter advances twice per round, so the CR 726 draw fires after ~2 recurrences instead of 3; `cleanup_sba_rounds` can be double-bumped the same way. So the seed now carries a **dropped** bound on one path and a **double-counted** bound on the other, and any real fix must make the obligations transfer exactly once — not merely "run somewhere". Both halves tighten a safety bound rather than break correctness, and both need the reap to fire from an `EnterStep*` guard site, which is unreachable today for the reason recorded in `repair_departed_priority_holder`'s doc block (no SBA sweep can run while a blocking decision is outstanding). **That unreachability is a scheduling accident, not a stated invariant** — if it ever stops holding, this row and the reap's missing `repair_departed_priority_holder` call activate together. | correctness, narrow, latent | filed by PB-DP8 closing review (`scutemob-156`), widened by its second closing review |
| **OOS-DP8-14** | **The golden-script pump-skip predicate was step-local and kind-blind, and nothing in the corpus would have caught it.** `crates/engine/tests/scripts/script_replay.rs`'s `next_answers_the_block` read only `step.actions[action_idx + 1]` and matched either answering action string regardless of which decision was outstanding, so (a) a script whose `choose_trigger_targets` opens the *next* step had its decision eaten by the pump and its own answer silently downgraded to `ActionNotTranslated`, and (b) a `discard_to_hand_size` following a `TriggerTargets` block suppressed the pump and left the next command `BlockedByPendingDecision`. Fixed (closing-review Finding 4) by matching on the `BlockingDecision` kind and searching forward across step boundaries; unit test `test_pump_skip_is_cross_step_and_kind_aware`. **The general point for PB-DP9, which adds three more answering action strings: the harness's skip predicate has to be keyed on the decision, not on the vocabulary.** **CORRECTED by PB-DP9 (`scutemob-157`): it adds ONE**, `"answer_effect_choice"`, because CR 608.2d is a single rule and search / scry / surveil are three instances of it, so the answering `Command` is unified. The predicate is keyed on the `BlockingDecision` kind either way, as this seed prescribed, so the fix held — but the growth it worried about did not materialise. | test-harness integrity | filed by PB-DP8 closing review (`scutemob-156`); prediction corrected by PB-DP9 |
| **OOS-DP7-10** | **"Whenever you discard a card" can never fire off a cleanup discard, and PB-DP7's own dispatch arm is the second half of the trap.** `abilities::check_triggers` has **no `GameEvent::DiscardedToHandSize` arm** — so a CR 514.1 discard is invisible to the trigger system — *and* PB-DP7's `Command::DiscardToHandSize` dispatch arm does not route its events through `check_triggers`/`check_and_flush_triggers` at all. Today those two gaps cancel out and the omission is harmless, which is exactly what makes it dangerous: **whoever adds the discard-trigger arm to `check_triggers` will fix nothing**, because the cleanup path still won't call it. Both halves must move together. Live corpus exposure to check when it does: `Waste Not`, `Bone Miser`, `Containment Construct` and the rest of the "whenever you discard" family. PB-DP7 shipped an in-source comment at `rules/engine.rs` naming this row so the trap is visible at the site — **this row is what that comment points at, so do not delete it without deleting the comment.** | correctness, latent (two-sided) | filed by PB-DP7 (`scutemob-155`) fix cycle, cited from source |
| **OOS-DP7-9** | **`has_madness` reads base `characteristics.keywords`, not layer-resolved characteristics** (`rules/turn_actions.rs::handle_discard_to_hand_size`). This is the W3-LC pattern the layer-correctness audit swept 55 sites for; the read was moved **verbatim** out of the deleted auto-discard loop, so it is pre-existing rather than introduced, and PB-DP7 left it as an in-source note per scope discipline. Exposure is currently empty — nothing in the corpus *grants* Madness via `LayerModification::AddKeyword` — so this is latent, and it is latent in the direction that a granted-Madness card would go to the graveyard instead of exile. Same class as the PB-AC8 `NoMaxHandSize` fix a few lines above it in the same function, which is the worked precedent for the conversion. | correctness, latent (pre-existing) | filed by PB-DP7 (`scutemob-155`) review Finding 8 |
| **OOS-DP7-8** | **The scattered per-PB wire-sentinel tests SR-17 was meant to retire have instead kept growing, and every future bump pays the tax.** PB-DP7's PROTOCOL 27→28 / HASH 64→65 required re-pinning **~40** `assert_eq!(PROTOCOL_VERSION, …)` / `assert_eq!(HASH_SCHEMA_VERSION, …)` copies scattered across `crates/engine/tests/primitives/*.rs` and elsewhere — up from the "~29" SR-17's doc comment describes as the disease it was designed to cure. SR-17 fixed the two *headline* sentinels and never claimed to eliminate the per-PB copies each batch leaves behind, so this is growth, not regression — but it is now a standing, unbounded mechanical tax on every wire change, and each copy is a place the value can drift. Fix: delete them in favour of the canonical gate tests (`protocol_version_sentinel` / `hash_schema_version_sentinel` and their fingerprint siblings), or derive them from one source of truth. **A test-hygiene item, not a correctness one** — but it is the kind that silently makes the next PROTOCOL bump look expensive. | maintenance / test hygiene | filed by PB-DP7 (`scutemob-155`) implementation |
| **OOS-DP6-9** | **Haunt's `HauntedCreatureDies` reads the card-def ability and ignores `intervening_if` at both ends.** `rules/resolution.rs:5351` destructures `AbilityDefinition::Triggered` for the haunt resolution path but never consults the intervening-if, and the corresponding queue site is keyword machinery (PB-DP6 Category B1) with no gate either. Same shape as OOS-DP6-5, found by the PB-DP6 review while re-deriving the plan's destructure partition — the plan's §2 claimed the mechanical `rg` returned 15 non-`testing` destructures; it actually returns **24**. No Category-A site was missed (the partition's *conclusion* held), but the extra nine include this genuine latent gap. Latent: no corpus haunt def carries an `intervening_if`. | correctness, latent | filed by PB-DP6 (`scutemob-154`) review |
| **OOS-DP9-1** | **`RandomBot` answers a CR 608.2d choice with the engine's default, not randomly.** Correct for this batch (a random answer would change every fuzzer seed and destroy any baseline), wrong for bot quality — and it now bites harder than OOS-DP8-1's sibling did, because the scry/surveil default is the IDENTITY: a bot's scry is a **no-op**, not even a self-mill. A randomising `RandomBot` + a library-aware `HeuristicBot` + a fuzzer-baseline re-pin belong together. | simulator quality | filed by PB-DP9 (`scutemob-157`) |
| **OOS-DP9-2** | **"Look at the top N and put them back in any order" has no DSL representation.** `halimar_depths.rs:24` and `wrenn_and_seven.rs:36` both say so in their own source and both lower to `Effect::Scry`, which wrongly permits bottoming. With PB-DP9's choice surface built this is one `Effect` variant (or one `bottom_allowed: bool` on `Scry`) plus a question flag — no new plumbing. Found by the mandatory pre-existing-TODO sweep; not a PB-DP9 forced add because it needs a different primitive. | DSL gap, cheap follow-on | filed by PB-DP9 (`scutemob-157`) |
| **OOS-DP9-3** | **`Effect::SearchLibrary` finds exactly ONE card.** ~7 `partial` defs say so in their own source (`tooth_and_nail`, `buried_alive`, `myriad_landscape`, `sarkhan_unbroken`, `goblin_recruiter`, `protean_hulk`, `tiamat`). CR 701.23 searches for "one or more cards". On PB-DP9's machinery this is a `count: EffectAmount` on the effect and `found: Vec<ObjectId>` on the answer, with **zero** new plumbing — one command, one event, one `LegalAction`, all already there. ~~**The largest card-yield item adjacent to this batch and the strongest candidate to rank next.**~~ **↻ `scutemob-159` (2026-07-27): the machinery claim holds; the yield claim does not.** Per-def audit of all seven: `protean_hulk` is **`inert`**, not `partial`; and **five of the seven carry a second, independent blocker** the count field does not touch — `myriad_landscape` needs a "share a land type" cross-card constraint, `goblin_recruiter` needs "put them on top in any order" (that is **OOS-DP9-2**), `sarkhan_unbroken` needs "any number" *plus* an interactive add-one-mana-of-any-colour, `tiamat` needs a "different names" uniqueness constraint *plus* a name-**exclusion** filter, `protean_hulk` needs a total-mana-value budget across the found set. **A count-only fix flips 2** (`tooth_and_nail`, `buried_alive`), not 7. Cite **CR 701.23d** ("a quantity of cards, such as 'a card' or 'three cards'") rather than the bare 701.23 — 701.23a is written in the singular. Ranked **PB-DX9**, still cheap, no longer high-yield. | DSL gap, card yield **2** | filed by PB-DP9 (`scutemob-157`); yield corrected by `scutemob-159` |
| **OOS-DP9-4** | **`EffectTarget::LastSearchResult` does not exist** (`scion_of_the_ur_dragon.rs:6`). PB-DP9's `AnsweredEffectChoice` is the natural carrier: the found card is now a first-class recorded value on `GameState` rather than a local inside one match arm. | DSL gap, narrow | filed by PB-DP9 (`scutemob-157`) |
| **OOS-DP9-5** | **`may_fail_to_find` is a judgement about CR 701.23b's "stated quality", and it is made on TWO axes the engine cannot decide from the filter alone.** *(Axis 1, as filed.)* The predicate ignores the **zone**: CR 701.23b is scoped to a *hidden* zone, so with `also_search_graveyard: true` the search spans a hidden zone and a public one and strictly the player may decline to find from the library but must find from the graveyard. `Effect::SearchLibrary` always searches the library, so the conservative reading (allow the decline whenever the filter states a quality) is taken and stated in the source rather than left silent. *(Axis 2, added by the closing review, LOW-5 — **half fixed**.)* The predicate was `*filter != TargetFilter::default()`, i.e. **every** non-default field counted as a stated quality. `TargetFilter` is shared with battlefield/target contexts, so several of its fields are runtime **board properties** rather than card qualities: a card in a library has no controller, is not a token, is not attacking or blocking and is not "the source". Those six (`controller`, `exclude_self`, `is_token`, `is_nontoken`, `is_attacking`, `is_blocking`) are also exactly the fields `matches_filter` cannot see, so setting one narrowed **nothing** and yet bought a CR 701.23d-forbidden decline over the whole library. They are now subtracted before the comparison (`effects::filter_states_a_quality`), pinned by `test_dp9_may_fail_to_find_ignores_non_quality_filter_axes`; no `all_cards()` def sets one on a search filter, so the change is behaviour-neutral over the corpus. **The residual is why this stays open:** "is field X a stated quality?" is a per-field human judgement with **no gate**, and the predicate is a subtraction, so a field added to `TargetFilter` tomorrow silently defaults to *allowing* the decline. That default is the safe one (it never forces a find the CR does not force) but it is still a claim nobody re-checks. | correctness, narrow | filed by PB-DP9 (`scutemob-157`); axis 2 added + half-fixed by the closing-review fix cycle |
| **OOS-DP9-6** | **`GameEvent::Scried` and `GameEvent::Surveilled` return `false` from `reveals_hidden_info()`** while committing to hidden library order — the same class as OOS-DP7-3. Not fixed in PB-DP9 to keep the batch's hidden-info story to the one new event it adds. | correctness, M10-gated | filed by PB-DP9 (`scutemob-157`) |
| **OOS-DP9-7** | **The TUI answers with the default, not a picker** — sibling of OOS-DP7-6 / OOS-DP8-2, and the most valuable of the three: a scry picker is the one widget a human meets **every game**. M11-local Session 7. | UX gap, M11-local | filed by PB-DP9 (`scutemob-157`) |
| **OOS-DP9-8** | **Multi-player resolution-time choices are made in ascending `PlayerId` order, not APNAP, and the cards do not move simultaneously.** CR **701.22c** / **701.23i** / **608.2e** all require APNAP, and 701.22c additionally requires the moved cards to move at the same time. `effects::resolve_player_target_list` (`effects/mod.rs:6926-6937`) iterates `state.players.keys()`, an `imbl::OrdMap`. Pre-existing and far wider than this roster — it governs **every** `ForEach::EachPlayer` effect — which is why PB-DP9 did not fix it; but PB-DP9 makes it **observable for the first time**, because the questions are now asked in that order. Pinned as the engine's actual behaviour by `test_dp9_choice_inside_for_each_each_player`. | correctness, wide | filed by PB-DP9 (`scutemob-157`) |
| **OOS-DP9-9** | **`Effect::SearchLibrary`'s `reveal` field is inert** (`effects/mod.rs`, destructured `reveal: _`) and **CR 701.23h ("search a library more than once before shuffling is a SINGLE search") is unmodelled** — the engine treats each `Effect::SearchLibrary` as an independent search, so a two-search effect now asks **twice**. Both were invisible before PB-DP9; the second is now visible as two questions. | correctness, narrow | filed by PB-DP9 (`scutemob-157`) |
| **OOS-DP9-10** | **Unordered-iteration-to-outcome breaks PB-DP9's replay premise — and the premise is RESOLUTION-scoped, not effect-scoped.** Originally filed as "`EffectContext.target_remaps` is a `HashMap`"; that part audited **clean** (three `insert` sites at `effects/mod.rs:1975`/`:2020`/`:2847`, one `get` at `:6699`, nothing iterates it). **The scoping was the error** (closing-review Finding 4): the replay re-executes the *whole resolution*, so ANY statement anywhere in it that reaches an outcome through `HashMap`/`HashSet` iteration order can diverge between passes — and Rust's `RandomState` re-keys per map, so this diverges *within one process*, not merely across processes. The widened workspace audit ran in the fix cycle and found and FIXED five sites: `effects/mod.rs`'s `Effect::ChooseCreatureType` and `replacement.rs`'s ETB twin (both `max_by_key` over a `HashMap`, and a tie is the common case — now `BTreeMap`); `abilities.rs`'s `AnyCreatureYouControlBatchCombatDamage` map, which **queued CR 603.3b triggers in map order**, i.e. stack order (now `BTreeMap`); `turn_actions.rs`'s CR 603.7b delayed-trigger map, same class (now `BTreeMap`); and `replacement.rs`'s `PendingZoneChange.already_applied`, built from a `HashSet` **without the sort its own sibling site in `pending_draws` documents as "load-bearing, not cosmetic"** — and that field is fed element-by-element into the state hash. Everything else enumerated is `contains`/`get`-only or sorted before use. `replacement.rs`'s `ChooseColor` was listed by the review and was **already deterministic** (unique max discriminant); converted anyway. **Residual, and the reason this stays open:** there is no gate for the shape. A new unordered-iteration-to-outcome site is a live hazard the moment a card co-locates it with a search/scry/surveil. | determinism, rankable | filed by PB-DP9 (`scutemob-157`); widened + 5 sites fixed in the fix cycle |
| **OOS-DP9-11** | **`GameState::move_object_to_zone` / `move_object_to_bottom_of_zone` renumber on a SAME-ZONE move (CR 400.7).** Both call `next_object_id()` unconditionally, so a card that never leaves its zone becomes a new object — and consumes a `timestamp_counter` value, which is the seed source for every `Zone::shuffle` and coin flip. PB-DP9 fixed it for scry with `Zone::reposition_within`; **every other same-zone caller of either helper should be swept for the same defect.** This is the OOS-RS-1 method applied to a different axis. | correctness, sweep | filed by PB-DP9 (`scutemob-157`) |
| **OOS-DP9-13** | **SR-19's `every_hashed_struct_field_is_hashed_or_allowlisted` gate covers STRUCT fields only — a hashed ENUM variant can silently drop a field.** Discovered while performing the plan's mandatory delete-a-field demonstration. Deleting `PendingEffectChoice.index` or `AnsweredEffectChoice.answer` from their `HashInto` impls fails the gate **by name**, exactly as designed. But rewriting `EffectChoiceQuestion::SearchLibrary { candidates, may_fail_to_find }` as `{ candidates, .. }` and dropping the `may_fail_to_find.hash_into(hasher)` feed **passes every gate in the suite green** — the struct-field scan does not see enum variants, and `-D warnings` only catches it while the field is still *named* in the pattern (a `..` silences that too). The `stream_fingerprint` would catch it only if the canonical fixture happens to populate that variant with a discriminating value. Same family as **OOS-DP7-11** (path-qualified impls fall out of the same gate with no diagnostic) and the same lesson: *a gate cited in a comment is a claim like any other*. Extending the scan to enum variants is the fix. | gate integrity, rankable | filed by PB-DP9 (`scutemob-157`); demonstrated, not theorised |
| **OOS-DP9-12** | **The §10 re-audit triggers are due again.** A new `Command` ⇒ DP-24's accepted-and-discarded-field check (**answered: no** — `choice_id`, the answer variant, every id and the partition multiset are all validated, and a rejected answer is proven not to move the state hash). A new `GameEvent` ⇒ the `reveals_hidden_info` sweep (**answered in-batch**, and it found OOS-DP9-6). Still owed from OOS-DP7-7: §3.1's 277-def re-derivation. | bookkeeping | filed by PB-DP9 (`scutemob-157`) |
| **OOS-DP9-14** | **`pending_effect_choice` with a dead owner is a trap state, and nothing clears it outside `handle_concede`.** `discharge_effect_choice_on_concede`'s only caller is `handle_concede`, so §1.5's exit 4 (elimination by an SBA rather than a concede) is **unreachable rather than handled**: while the block stands `process_command`'s admission gate rejects every command except the answer and `Concede`, so no SBA runs. `blocking_decision`'s liveness filter is defence in depth — it stops a dead owner's entry blocking the game, but it does **not** clear the field. If the admission gate is ever widened, or an SBA path ever reaches here, the residue is unrecoverable: `blocking_decision` returns `None` so `PassPriority` is admitted, `resolve_top_of_stack`'s entry `debug_assert!` fires, and `ask_or_consume_effect_choice`'s "already suspended" guard short-circuits on the pre-existing entry so the wrapper re-emits the stale question forever. The fix is a reap at the top of `resolve_top_of_stack`, mirroring `drop_departed_trigger_flush`'s placement. | latent trap, defensive | filed by PB-DP9 fix cycle (`scutemob-157`); argued unreachable in-source |
| **OOS-DP9-15** | **`Scried.count` is the REQUESTED N; `Surveilled.count` is the ACTUAL number looked at.** Neither is CR-wrong (CR 701.22d / 701.25d both fire the event regardless of how many cards were actually seen, and no "whenever you scry/surveil" trigger in the corpus reads the count), and the asymmetry has one real virtue — reporting the requested N keeps `Scry 3` with an empty library distinguishable from `Scry 0`, which emits nothing at all under CR 701.22b. Documented in-source at both arms rather than unified, because unifying one side in isolation is the trap. | hygiene | filed by PB-DP9 fix cycle (`scutemob-157`) |
| **OOS-DP9-17** | **`Command::AnswerEffectChoice` resets loop detection, and the new identity scry/surveil default turns that into a missed draw.** `rules/engine.rs`'s arm calls `loop_detection::reset_loop_detection` under CR **104.4b** ("a meaningful player choice"), which is defensible on its face — CR **726.1** only declares a draw for a loop of *mandatory* actions, and a resolution-time announcement is by definition not mandatory. The interaction is what is new: PB-DP9 made the scry and surveil defaults the **identity**, so a bot (or any client) that answers with the default repeats a genuinely identical, genuinely no-op answer forever and the loop is never recognised. Pre-PB-DP9 the same loop self-terminated, because bottom-everything / mill-everything changed the library every pass. The game now runs to `MaxTurnsReached` where CR 726.1 would have declared a draw. **Behaviour deliberately unchanged** — the CR 726.1 argument for the reset is sound and the right fix is on the other side (a repeated *identical* answer is not a meaningful choice). Ranks with **OOS-DP9-1**, which is the same root cause seen from the bot's end. | correctness, ranks with OOS-DP9-1 | filed by the PB-DP9 closing review (`scutemob-157`) |
| **OOS-DP9-18** | **`has_conceded` and `has_lost` are not interchangeable, and the effect pipeline reads a different one at each end.** `ask_or_consume_effect_choice` treats `has_lost \|\| has_conceded` as "announces nothing, apply the default" (CR 104.3a), but `effects::resolve_player_target_list` filters the effect's player list on **`has_lost` only**. A player who *conceded* is therefore still in the list: their library is still searched and a card still lands in their hand. CR **800.4a** says all objects owned by a player who leaves the game leave with them, so there should be no library to search and no hand to put a card into. The gap predates this batch and is far wider than it — the engine has **no CR 800.4a object sweep at all**, it marks the flag and leaves the board alone — but PB-DP9 codified and now *tests* the deviation, so it is recorded rather than left implicit: `test_dp9_foreign_concede_invalidates_a_non_empty_bank`'s final assertion says in its own message that it pins a known deviation. Fixing it means implementing CR 800.4a's zone sweep (and CR 800.4b/c/d/n's exceptions), which is its own batch. | correctness, wide | filed by the PB-DP9 closing review (`scutemob-157`) |
| **OOS-DP9-19** | **The engine's priority-grant sites, and which of them answer CR 800.4j.** The closing review's HIGH-1 root cause was `resolve_top_of_stack_inner` granting priority to `turn.active_player` unconditionally; its fix routed **both** of that function's grant sites (the CR 117.3b tail and the CR 608.2b fizzle path) through a CR 800.4j-aware helper. That fix shipped with a **false completeness claim** in two places -- the source comment said those two were "the only place in the engine" that granted unconditionally, and this row called `rules/engine.rs`'s cleanup-SBA-round grant "the last unconditional one in the engine", which also contradicted the source comment written in the same commit. **The second closing review enumerated the real set mechanically** with `grep -rn 'priority_holder = ' crates/*/src tools/*/src` (every production write of the field in the workspace) and this row now carries that inventory: **(a) FIXED, routed through `rules::priority::grant_priority_to_active_player`** -- `resolution.rs`'s two `resolve_top_of_stack_inner` grants (closing review), plus two more found by the second closing review: **`combat.rs`'s `handle_declare_blockers` CR 509.1 / 117.3a tail**, which was **live-reachable on a production path with no PB-DP9 machinery on it at all** (active player attacks, dies to a CR 704.5a SBA during its own combat phase, the surviving defender declares blockers, priority lands on the departed seat -- probe `test_509_declare_blockers_grant_skips_a_departed_active_player`, pre-existing on `main`), and `resolution.rs`'s `counter_stack_object` tail, which is **structurally the exact bug** (unconditional grant preceded by `check_and_apply_sbas`) and latent only because the function has no production caller today -- routed anyway so a future caller does not inherit a shipped deadlock. **(b) STILL UNCONDITIONAL, open** -- `rules/engine.rs:2427`, `enter_step`'s cleanup-SBA-round grant. Reachability **not** proven; needs its own fail-before probe. **(c) UNCONDITIONAL BUT BENIGN, deliberately not routed** -- the cipher copy cast mid-resolution in `resolution.rs` (`StackObjectKind::KeywordTrigger` / `KeywordAbility::Cipher`): the write is dead by the time the command returns because the arm falls through to the CR 117.3b tail, which overwrites it, and it pushes no `PriorityGiven`, so routing it would add an event for no behavioural gain. Argued in-source at the site. **(d) GRANT NOTHING AT ALL** -- three early returns inside `resolve_top_of_stack_inner`, verified accurate as filed at `resolution.rs:4352` (a stack object with no recorded target -- a *defensive* branch, consistent with "reachability not proven"), `:5066` and `:5316` (the CR 603.4 Offspring / Gift intervening-if failures). They `return Ok` leaving `priority_holder == None` with `players_passed` full -- the same deadlock shape from the other direction. Left alone. **Not in scope of this row:** the ~20 `= Some(player)` grants to the *actor* (PB-DP1's CR 117.3c rule -- the actor just issued a legal command and holds by construction) and the sites that were already conditional (`handle_all_passed`'s three, `enter_step`'s two mid-turn ones, `abilities.rs`'s forced-payment grants). **The classification is a snapshot of a grep, not a machine-checked invariant:** an unconditional grant added tomorrow fails no gate. That, and (b) and (d), are why this stays open. | correctness, partly fixed | filed by the PB-DP9 closing review fix cycle (`scutemob-157`); inventory corrected + 2 sites fixed by its second closing review |
| **OOS-DP9-16** | **CR 603.7b delayed triggers are keyed by `target_object`, so two delayed triggers sharing a target COLLAPSE.** `turn_actions.rs`'s end-step sweep builds a map from `target_object` to `(action, controller, source)` and queues one `PendingTrigger` per key. Two `AtNextEndStep` delayed triggers on the same object therefore fire once. Found while making that map's iteration order deterministic (OOS-DP9-10's widened audit); the ordering was fixed, the collapse deliberately was not — it is a behaviour change with its own test surface. | correctness, narrow | filed by PB-DP9 fix cycle (`scutemob-157`) |
| **OOS-DP10-1** | **`pb_dp9_effect_choice.rs::roster`'s walk matches object keys only and is blind to a unit variant.** Harmless today — `SearchLibrary`/`Scry`/`Surveil` are all struct variants — but a live hazard the moment that copy is reused for a unit-variant row. `crates/engine/tests/core/decision_site_walk.rs::json_contains_variant` is the canonical, unit-variant-aware version; it cannot be shared directly across the SR-9a integration-test-target boundary (a group `main.rs` may declare only bare `mod x;` lines, no `#[path]`), so the copy is kept, documented in-source, and cross-checked BY VALUE (not by text) against the canonical walk's own PB-DP9 rosters (`decision_gate::canonical_walk_reproduces_pb_dp9_rosters`). Promoting the walk to a shared home (the engine crate's `testing` module) would be an engine change and is out of scope for a test-only batch. | gate integrity | filed by PB-DP10 (`scutemob-158`) |
| **OOS-DP10-2** | **A token's `TriggeredAbilityDef` (`state/game_object.rs:884`) is a DIFFERENT type from `AbilityDefinition::Triggered` and carries its own `targets`.** Neither the audit's original regex, PB-DP8's typed walk, nor PB-DP10's `"Triggered"`-node predicate covers it — a granted CR 603.3d target choice on a token's own triggered ability is an uncounted class-B site. Deliberately NOT widened in PB-DP10 (it would change PB-DP8's published `triggered_targets` roster of 77 on no fail-before evidence). | correctness, uncounted | filed by PB-DP10 (`scutemob-158`) |
| **OOS-DP10-3** | **`Effect::AddManaFilterChoice`'s 0-`Complete` count was hand-maintained with NOTHING holding it** until PB-DP10's `T7` — the SR-33 gate bars `AddManaChoice`, a different serde key. The underlying CR 605.1a 3-way filter-land choice is still unimplemented (`effects/mod.rs` always adds one of each colour); the 7 filter lands stay `known_wrong` (PB-RS2). **↻ `scutemob-159` (2026-07-27): this is the same gap as the legacy seed `OOS-AC9-FILTERMANA`** ("3-way filter-land choice, M10", `oos-retriage-plan-2026-07-18.md` §1d), filed ~9 months earlier and parked as dormant. Verified at `effects/mod.rs:2823-2845`: the arm adds one of `color_a` **and** one of `color_b`, so a filter land makes two mana instead of one chosen one. Treat the legacy ID as an alias of this row. | correctness, deferred | filed by PB-DP10 (`scutemob-158`); aliased to `OOS-AC9-FILTERMANA` by `scutemob-159` |
| **OOS-DP10-4** | **§10's "new `Command` variant → accepted-and-discarded field (DP-24)" re-audit trigger is UNMECHANIZED.** An SR-15-style source scan for `_`-bound `Command` fields in handlers is feasible test-only and unbuilt; PB-DP10 scoped its own T15/T15b differently (a whole-roster digest of the DSL enum declarations, not this scan) for time. | gate coverage | filed by PB-DP10 (`scutemob-158`) |
| **OOS-DP10-5** | **`Effect::LookAtTopThenPlace.optional` is inert by construction** (`effects/mod.rs` destructures `optional: _` and says so) yet is serialized and hashed — the same DP-24 "accepted-and-discarded" class, on an `Effect` rather than a `Command`. `SearchLibrary.reveal` is the other known sibling (OOS-DP9-9). Sweep for others not yet found. | correctness, narrow | filed by PB-DP10 (`scutemob-158`) |
| **OOS-DP10-6** | **The successor queue's input, ranked by measured `Complete` count.** `proliferate` 25, `discard_cards` 13, `sacrifice_permanents` 11, `may_pay_then_effect` 10, `choose_color_or_type` 10, `look_at_top_or_route` 10, `counter_unless_pays` 7, `modal_trigger` 4, `change_targets` 3, `bolster_amass` 3, `connive` 1, `discover` 1, `put_on_library` 1 (`decision_gate::decision_site_reconciliation_report`'s live output, 2026-07-27 — these numbers are `>=`-floor-checked, not pinned, and will drift as the corpus grows; re-read the report before ranking; `put_on_library` was omitted from this row's first draft — closing fix-review caught it). PB-DP9's `AnswerEffectChoice` channel generalises to most of these "for the cost of one `EffectChoiceQuestion`/`Answer` variant pair and zero new plumbing" per its own §4.9 note — so rank by def count, not by machinery. **Caveat on `look_at_top_or_route`'s 10**: this row's count is an UPPER BOUND, not an exact roster of real decisions — `RevealAndRoute` also matches defs whose routing the card itself determines with no choice at all (Chaos Warp, Coiling Oracle), alongside the genuine CR 608.2d/401.4 order choices (Goblin Ringleader). Whoever ranks this row should re-derive the real subset before scoping work, not take 10 at face value. **↻ CONSUMED by `scutemob-159` (2026-07-27).** The report was re-run on that branch before ranking, as this row instructs; its live output is transcribed in `memory/primitives/seed-rerank-2026-07-27.md` §2.9 with the ranking consequences. Three: (i) **`proliferate` 25 is the largest auto-chosen class** and is named there as the designated successor to the PB-DX queue — it is agency restoration on otherwise-correct cards, so it ranks below the live-wrong block, not above it; (ii) **`modal_trigger` 4** is PB-DP8b's real roster, correcting OOS-DP8-7's "~5"; (iii) `look_at_top_or_route`'s upper-bound caveat was honoured — that row is **not** ranked. | queue input | filed by PB-DP10 (`scutemob-158`); **consumed by `scutemob-159`** |
| **OOS-DP10-7** | **`GameEvent::reveals_hidden_info()` and `private_to()` both end in a catch-all `_ =>` arm**, so a new `GameEvent` variant silently answers `false` / `None` rather than forcing a deliberate decision. §10's trigger exists in prose but nothing enforces it mechanically. A roster digest over the `GameEvent` declaration (the same twenty lines as a `ROWS`-style whole-enum digest, pointed at a different enum) is the cheap fix if not taken before the next `GameEvent` variant lands. | gate coverage | filed by PB-DP10 (`scutemob-158`) |
| **OOS-DP10-8** | **`BASELINE`'s 97 entries were populated mechanically; the plan §5.3 class-B/class-D triage was never performed against oracle text**, and a closing-review spot-check of 5 of the 97 found TWO class-D members. **Smuggler's Copter** — oracle "Whenever this Vehicle attacks or blocks, you **may** draw a card. If you do, discard a card" (CR 118.12) is modelled as an unconditional `Effect::Sequence(vec![DrawCards, DiscardCards])`, `Completeness::Complete`; the controller is FORCED to loot every attack and block. This is the 20th instance of the class audit §5's DP-12 row already owns ("costless 'you may' on a trigger has no DSL representation; such cards are authored mandatory and marked `known_wrong` — 19 marked") — Smuggler's Copter was simply left `Complete` instead, so the fix is a one-line marker change for whoever ranks it. **Shambling Ghast** — oracle "When this creature dies, choose one — Target creature an opponent controls gets **-1/-1 until end of turn**. — Create a Treasure token" (keywords: `["Treasure"]` only) has THREE deviations, not two: (i) mode 1 is authored as a PERMANENT `CounterType::MinusOneMinusOne` counter, not an until-end-of-turn P/T change (persists past end of turn, is proliferate-able, interacts with CR 122.3 counter annihilation); (ii) its stored `oracle_text` says "When Shambling Ghast **enters**" while `trigger_condition` is `WhenDies`; (iii) it grants `KeywordAbility::Decayed` (a creature that can't block and sacrifices itself after attacking), which the printed card does not have at all. Both are `Complete`, deck-legal. Per plan §5.3: **file, do not demote** — `BASELINE`'s doc comment and `T4`'s failure message now say explicitly that an entry asserts only "hits these rows", nothing about oracle-correctness. **The remaining 95 entries have not been triaged**; the full 97-entry sweep is the highest-value remaining work named by this seed. **↻ `scutemob-159` (2026-07-27): both spot-check findings re-verified against oracle text via MCP and RANKED (PB-DX4).** Shambling Ghast's printed text is "When this creature dies, choose one — • Target creature an opponent controls gets **-1/-1 until end of turn**. • Create a Treasure token", keywords `["Treasure"]` **only** — so all three deviations hold, including the phantom `Decayed`. Smuggler's Copter's is "Whenever this Vehicle attacks or blocks, **you may** draw a card. If you do, discard a card" against an unconditional `Effect::Sequence(vec![DrawCards, DiscardCards])` on a `Complete` def. Two cautions for the sweep: 2-of-5 is a very noisy sample, and per plan §5.3 the default is **file, do not demote** — a marker flip needs an oracle citation. | correctness, uncounted, **ranked PB-DX4** | filed by PB-DP10 review/fix cycle (`scutemob-158`) |
| **OOS-DP10-9** | **Un-encoded decisions are invisible to PB-DP10's gate, and this is the gate's most severe blind spot, not its mildest.** Every one of the 22 `ROWS` is a predicate over a DSL *variant name*; a card whose "you may X. If you do, Y" or "choose one" was dropped at authoring time — never expressed as `MayPayThenEffect`, `Choose`, a mode, or anything else — hits zero rows and passes T4/T6 forever. Smuggler's Copter (OOS-DP10-8) is the live example: its `may` clause is a CR 118.12 optional cost authored as a bare `Sequence`, and it appears in `BASELINE` at all only because of the incidental `DiscardCards`. A recorded auto-choice (what this gate's 97 entries are) is at least a legal outcome; a dropped "may" is not. Detecting this class needs a DIFFERENT instrument from a variant walk — an oracle-text-vs-DSL cross-check (`'may'`/`'choose'`/`'up to'` in `oracle_text` with no decision-bearing variant anywhere in the effect tree); `oracle_text` is already on the def and already parsed by `effect_choose_gate.rs`, so this is feasible test-only. | gate coverage | filed by PB-DP10 review/fix cycle (`scutemob-158`) |
| **OOS-DP10-10** | **CR 404.3's simultaneous-graveyard-order choice is engine-chosen everywhere and is not one of §3.1's 22 rows.** CR 404.3: "If an effect or rule puts two or more cards into the same graveyard at the same time, the owner of those cards may arrange them in any order." `discard_cards`, `WheelHand`'s whole-hand discard, mill effects, and sacrifice batches all move multiple cards/permanents to a graveyard by ascending `ObjectId` with no player choice offered — an uncounted class-B site with a roster at least as wide as `discard_cards`'s own. Found while narrowing `wheel_hand`'s NO-DECISION `why` text, which had (correctly, on the question §3.1 asks) conflated "no 'which card' pick" with "no choice of any kind" — the CR 404.3 choice is real, just not the one that row counts. | correctness, uncounted | filed by PB-DP10 review/fix cycle (`scutemob-158`) |
| **OOS-DP10-11** | **The DSL-enum roster digest (PB-DP10's own dropped `T15`) has no owning seed.** T15 was a count + blake3 digest of the sorted variant-name lists of `Effect`, `AbilityDefinition` and `ReplacementModification`, dropped for budget (§10's ledger records the reasoning). Neither OOS-DP10-4 (a `Command`-field scan, a different enum and a different instrument) nor OOS-DP10-7 (the `GameEvent` digest, `T15b`) owns T15's actual subject, so it was untracked prose inside §10's ledger only. Failure message on landing: "a new variant landed; classify it in `decision_site_walk.rs::ROWS` and update audit §3.1/§10." The notice is already free (`Effect`/`AbilityDefinition`/`ReplacementModification` are all inside the SR-8/SR-17 wire closures, so a new variant already forces a PROTOCOL and a HASH bump); the digest supplies the missing **obligation** message. ~20 lines, test-only. Sibling of OOS-DP10-7, the same instrument pointed at `GameEvent`. | gate coverage | filed by PB-DP10 review/fix cycle (`scutemob-158`) |

---

## 9. M11-local decision-loop extensibility assessment

Against `/home/skydude/projects/scutemob/.worktrees/scutemob-147/memory/m11-session-plan.md`
(read-only cross-worktree reference; nothing under `.worktrees/` was modified).

### 9.1 The question

The plan's §2(a) adopts a steppable driver:

```
    pub enum AdvanceOutcome { AwaitingHuman(PendingDecision), GameOver(..), Halted(..) }
    pub enum DecisionKind { Priority, Mulligan, CommanderZoneChoice,
                            DeclareAttackers, DeclareBlockers }
    pub struct HumanChoice { pub action_index: usize, pub params: ActionParams }
```

Does that generalise to trigger-time and resolution-time choices without redesign?

### 9.2 Answer: **no — and the plan's own reasoning shows why**

§2(a) justifies the design like this:

> **Sub-decisions**: the five non-`choose_action` `Bot` methods are *not* driver callbacks
> (verified: zero call sites), so there is nothing to intercept. Attackers, blockers,
> mulligan-bottoms and targets are all fields of the `Command` the seat returns. They become
> fields of `ActionParams` (§3), which is a server-side type — **no new `Command` or
> `GameEvent` variant.**

That is **exactly right for the five decisions it enumerates, and exactly wrong as a general
claim** — because it is true only of choices that are announced *at command-submission time*.
This audit's §4.6 through §4.11 are all choices that happen *inside* `process_command`, while
the engine flushes triggers or resolves a stack object. Concretely:

- `LocalGame::advance()` can only yield `AwaitingHuman` **between** `process_command` calls.
- `execute_effect_inner` returns `()` and cannot suspend (§4.9). **Still true after PB-DP9 —
  and that is why PB-DP9's mechanism is an ABORT, not a pause: the resolution is rolled back
  wholesale and re-run from the top once the answer arrives, so nothing has to suspend.**
- `flush_pending_triggers` runs to completion inside whatever command triggered it (§4.6/4.7).
- `ActionParams` is a *command-assembly* struct. There is no `Command` for a trigger target,
  a scry split, a search pick, or a cleanup discard, so there is nothing for `ActionParams` to
  assemble.

So `DecisionKind`'s five variants are not an early cut of a longer list — they are **the
complete set of decisions reachable by this architecture**, and they will stay complete until
the *engine* grows pending-decision state for the other classes. That is a wire change, which
the plan's §7 constraint 6 explicitly forbids:

> **OVERTAKEN BY EVENTS (PB-DP7/DP8/DP9, `scutemob-155`/`156`/`157`).** The engine grew exactly
> that pending-decision state, three times, outside M11-local and therefore outside constraint
> 6's scope. `DecisionKind` now has **seven** variants, three of which are out-of-band blocking
> decisions (`CleanupDiscard`, `TriggerTargets`, `EffectChoice`), and PROTOCOL went 27 → **31**
> in the process. The paragraph's *analysis* was right — it correctly predicted that reaching
> the trigger-time and resolution-time classes needs engine pending state and a wire change —
> and its *conclusion* ("they will stay complete") was a scoping assumption, not a structural
> one. Read the numbers below as of 2026-07-26, not as current.

> **No new `Command` / `GameEvent` / `Effect` variant.** If one seems necessary, stop and
> flag: it is a wire change (SR-8) requiring a PROTOCOL bump, and for this milestone it
> signals a design error.

**This is not a criticism of the plan.** Constraint 6 is right for M11-local, and the audit
confirms the plan's own §1 fact 6 (optional payments are "not stalls; just silently
unavailable"). The problem is only that a closed 5-variant `DecisionKind` and a flat
`actions: Vec<LegalAction>` will *look* complete to session 5 and 7 authors, and the DTO
shapes they lock in will have to be reshaped later. The fix is cheap and belongs in the
sessions that define those shapes.

### 9.3 The engine already has the right pattern — it just doesn't block

Seven existing round-trips prove the shape generalises at the engine level:
`ReplacementChoiceRequired`/`OrderReplacements`, `CommanderZoneReturnChoiceRequired`,
`DredgeChoiceRequired`/`ChooseDredge`, `MiracleRevealChoiceRequired`/`ChooseMiracle`,
`EchoPaymentRequired`/`PayEcho`, `CumulativeUpkeepPaymentRequired`, `RecoverPaymentRequired`.
Each is: emit a `*Required` event → record a pending entry → accept an answering `Command`.

But §4.4 and §4.11 show that **only `pending_zone_changes` actually gates anything**. The
other five pending vectors are inert queues that nothing consults. So the reusable asset is
the *event + command + pending-entry triple*; the missing piece is the gate. That is why
PB-DP7 in §8 is proposed as a deliberate pilot.

> **UPDATED by PB-DP7 (`scutemob-155`).** The gate now exists —
> `rules::engine::{BlockingDecision, blocking_decision}`, consulted in `enter_step` (progress)
> and at the top of `process_command` (admission), with a clear-on-concede in `handle_concede`.
> **None of the seven existing round-trips was retrofitted onto it** (OOS-DP7-1), and the
> paragraph above needs one correction: the dredge round-trip does not merely fail to block —
> its own doc comments *claim* it does. `events.rs:848-850` and `replacement.rs:619` both say
> "the engine pauses"; `perform_one_draw` records no pending state and `DrawStepOutcome`'s doc
> at `:764-767` says the caller does not stop. See **OOS-DP7-2**.

### 9.4 Concrete recommendations

**Session 3** — *Action parameterization + engine target queries*

1. Make `DecisionKind` `#[non_exhaustive]` and document, in the enum's doc comment, that it
   enumerates **command-submission-time** decisions only, with a pointer to this audit for the
   trigger-time and resolution-time classes it structurally cannot reach. One comment now
   prevents a later reader assuming the surface is complete.
   **DONE by PB-DP7 (`scutemob-155`)** — the attribute is on, and `DecisionKind::CleanupDiscard`
   is the first variant that is *not* a priority window, which is the concrete proof the
   recommendation was needed. Note the premise has moved: the enum is no longer
   command-submission-time-only.
   **AND THE STRUCTURAL CLAIM IS NOW FALSIFIED (PB-DP9, `scutemob-157`).** This item said the
   enum "structurally cannot reach" the trigger-time and resolution-time classes. PB-DP8 added
   `DecisionKind::TriggerTargets` and PB-DP9 added `DecisionKind::EffectChoice`, so it reaches
   both — and reaching the resolution-time class needed no reshaping of `PendingDecision`
   either, because the abort-and-replay design hands the client an ordinary
   `Vec<LegalAction>` on a state that has been rolled back to look exactly like a state where
   nothing has started. Item 2 below is therefore still un-done and still un-needed.
2. Give `PendingDecision` an explicit shape for its payload rather than only
   `actions: Vec<LegalAction>` — e.g. `payload: DecisionPayload` with an `Actions(Vec<LegalAction>)`
   variant today. A future `TriggerOrder`/`ResolutionChoice` then adds a variant instead of
   reshaping the struct that sessions 5 and 7 will have built DTOs against.
3. **Surface the decisions that are already reachable with zero wire change.** The plan's §1
   fact 6 lists them; this audit adds why they matter. `advance()` should yield `AwaitingHuman`
   for a non-empty `pending_echo_payments` / `pending_cumulative_upkeep_payments` /
   `pending_recover_payments`, and `OrderBlockers` should be offered whenever an attacker has
   ≥2 blockers. These need new `LegalAction` variants (simulator-internal, **not** a wire
   change) and are the cheapest agency the milestone can buy. Note **DP-11**: because nothing
   enforces the pending payments, an M11 game today lets every echo permanent stay for free.

   > **UPDATED by PB-DP4 (`scutemob-152`).** The `LegalAction` half is **DONE** for all three
   > payments; the enforcement gap is closed; and **the `advance()` half turned out to be
   > unnecessary**. Because the deadline makes the payment a choice inside a normal priority
   > window rather than an out-of-band pause, `LegalAction::PayEcho` / `PayCumulativeUpkeep` /
   > `PayRecover` arrive inside the **existing** `PendingDecision` and are classified
   > `DecisionKind::Priority` by `decision_kind_for` — **no new `DecisionKind` variant, no
   > `PendingDecision` reshaping, no `local_game.rs` edit at all.** Cheaper than this
   > recommendation predicted. The `OrderBlockers` half (**DP-13**) remains open and is still
   > the only decision in this list with no `LegalAction`.
4. ~~Session 3 item 5 already forbids re-deriving `hybrid_choices` / `phyrexian_life_payments`.
   Extend the same rule to `modes_chosen`: **`action_to_command_with_params` must reject an
   empty `modes_chosen` on a modal action whose `min_modes >= 1`**, because the engine will not
   (**DP-4**). Otherwise the first human to cast Cryptic Command through the play server gets
   half a spell at full price and no error.~~
   **SUPERSEDED by PB-DP3 (`scutemob-151`)** — the engine now rejects it, at cast time and
   before costs are paid, so the play server needs no compensating check. What it needs instead
   is a mode-selection **UI**: `crates/simulator/src/legal_actions.rs`'s `spell_default_modes` /
   `ability_default_modes` supply the first `min_modes` legal indices as a bot/placeholder
   default, and that placeholder is exactly what session 7 must replace with a human choice.
   Note also **OOS-DP3-7**: the ~28 alt-cost cast paths cannot announce modes at all, so a
   modal card cast via flashback/escape/etc. is unreachable through them today.

**Session 5** — *play-server REST API*

5. Make `DecisionView` a **tagged union keyed on decision kind**, not a flat
   `{seq, kind, actions}`. `POST /api/game/action` should carry the same discriminator. A
   trigger-ordering decision is a permutation, a scry decision is a partition, a cleanup
   discard is a subset — none of them is an index into an action list, and a client written
   against a flat action list will need a breaking change to accept any of them.
   **SHARPENED by PB-DP7 (`scutemob-155`), and now urgent.** The cleanup discard shipped, and
   the subset shape this recommendation predicted is exactly what `Command::DiscardToHandSize
   { player, cards: Vec<ObjectId> }` carries. It fits the flat `actions: Vec<LegalAction>` only
   because the whole subset is packed inside one `LegalAction` — i.e. it is **the last decision
   class that fits at all**. Make the reshape (rec 2's `payload: DecisionPayload`) *before*
   PB-DP8 rather than after; seeded as **OOS-DP7-5**.
   **"The last decision class that fits at all" is now falsified twice over.** PB-DP8's
   per-slot target announcement and PB-DP9's scry/surveil **partition** -- precisely the shape
   this recommendation named as not fitting -- both packed into one `LegalAction` the same way:
   `AnswerEffectChoice { choice_id, source, question, answer }` carries the full legal answer
   space in `question` and an engine-accepted default in `answer`. The reshape is still worth
   doing for the *client*'s sake (a picker needs the discriminator), but the flat action list
   has not yet been the thing that blocks a decision class from shipping. OOS-DP7-5 stands,
   with its urgency downgraded.
6. On the R4 `--dev` raw-command escape hatch the plan defers to this session: **recommend
   yes**, and note that it is also the only way a playtester can currently exercise
   `PayEcho`, `PayCumulativeUpkeep`, `PayRecover` and `OrderBlockers` (no `LegalAction`
   exists for any of them, §4.11 / DP-11 / DP-13).
   **SUPERSEDED IN PART by PB-DP4 (`scutemob-152`)** — `LegalAction::PayEcho`,
   `PayCumulativeUpkeep` and `PayRecover` now exist and reach a human seat through the normal
   priority decision, so the raw-command hatch is no longer the only way to exercise them. The
   `--dev` hatch is still recommended, and **`OrderBlockers` is now the only decision in the
   audit with no `LegalAction`**.
7. `GET /api/game/report` (session 8 item 5) should include the auto-choice annotations from
   recommendation 8 below, so a "that seemed wrong" report distinguishes an engine bug from an
   engine *choice*.

**Session 7** — *Targeting, combat and choice UIs*

8. **Ship an "engine chose this for you" annotation in the event feed.** This is the single
   highest-value item the milestone can add for this audit's findings, it needs no wire change,
   and it converts an invisible wrongness into a visible one. Minimum viable set, all
   detectable from existing `GameEvent`s plus the seat view: triggered-ability targets
   (**DP-6**), scry/surveil disposition (**DP-8/DP-9**), search picks (**DP-7**), sacrifice and
   discard picks (**DP-16**, ~~**DP-3**~~ — DP-3 no longer needs an annotation, PB-DP7 made it a
   real choice; but see **OOS-DP7-6**, the TUI answers it with the old auto-pick), and combat
   damage assignment (**DP-13**).
9. `TargetPicker.svelte` is the **cast-time** picker (CR 601.2c). Name it accordingly and
   document that trigger targets (CR 603.3d) never reach it. Otherwise session 7 will close
   believing targeting is done.
10. The attacker picker should not imply the engine will enforce attack requirements
    maximally — §4.5 shows validation is per-declaration only, so a client that offers an
    under-satisfying attack set gets a rejection, not a correction. And per **DP-10**, if a
    Propaganda-class effect is out, the UI must not tell the human they paid: nothing is
    debited.
11. Session 7's acceptance is "the human can attack, block, and cast targeted/X/modal spells".
    Add: **and can see, in the feed, every choice the engine made on their behalf.**

### 9.5 What does *not* need to change

The plan's crate layout, its async boundary, `Viewer`-based redaction, the steppable-driver
decision over the channel-backed bot, and constraint 6 are all sound and this audit does not
touch them. `LocalGame` is the right home for a decision loop; it just needs to be honest
about which decisions it can currently reach.

---

## 10. Re-audit triggers

> **PB-DP10 mechanization ledger (`scutemob-158`).** Of the eight triggers below, **2 are now
> MECHANIZED** (a test computes and enforces the answer on every `cargo test`), **2 are
> feasible-but-not-built and now carry a seed** (the DSL-enum roster digest, **OOS-DP10-11**;
> the `GameEvent` roster digest, **OOS-DP10-7**), and **4 stay human** (DP-24's
> accepted-and-discarded-`Command`-field check, **OOS-DP10-4**; a new `BlockingDecision`
> variant's seven obligations; a new same-zone `move_object_to_zone` caller, **OOS-DP9-11**;
> a `Zone` API change). Write that ratio down rather than a claim of coverage — this suite's
> recurring lesson (OOS-DP7-11; PB-DP8's meta-lesson iii) is that *a gate cited as covering
> something is a claim like any other*, and **the first version of this very ledger said
> "3 of 8" while its own body concluded the third one "stays NOT mechanized"** — caught by
> the closing `/review`. Counting your own coverage is the same kind of claim.
> **MECHANIZED**: the §3.1 re-derivation (below, `decision_site_reconciliation_report`); the
> `docs/authoring-status.md` material-jump trigger (the same test prints the live
> denominator and percentage every run, and `T6`'s exact ratchet catches the numerator).
> **A third, narrower one**: a gate over the CORPUS cannot see a new `Effect`/
> `AbilityDefinition`/`ReplacementModification` variant no def yet uses — but that variant
> ALREADY forces a PROTOCOL and a HASH bump (both are in the SR-8/SR-17 wire closures), so
> the *notice* was never the missing half; the missing half is the *obligation* to classify
> it in `ROWS` and update §3.1/§10, which nothing states today and which a roster digest
> (count + blake3 of the sorted variant-name list) could supply — **not built in PB-DP10**
> (budget; see **OOS-DP10-11**, filed by the closing review after the WIP's own record of
> this drop was found to point at the wrong seeds), so this one stays **NOT mechanized**
> below — **feasible and seeded, not mechanized**. The `GameEvent` sibling-answer sweep is the
> same instrument pointed at another enum and is in the same state (**OOS-DP10-7**).
> **STAYS HUMAN**: DP-24's accepted-and-discarded-`Command`-field check (**OOS-DP10-4**); any
> new `BlockingDecision` variant's seven obligations (only 2 of 7 are compile-forced, per
> PB-DP9's own count); any new same-zone `move_object_to_zone` caller (OOS-DP9-11 already owns
> that sweep, unbuilt); and a `Zone` API change, whose roster §10 already prescribes deriving
> from `Zone::push_front` call sites by hand.

Per [methodology.md](methodology.md) "When to Re-Audit":

- Any new `Effect` variant that carries a `choices`, `optional`, `may`, or filter-selection
  field — check whether it joins §3.1 or is gated.
  > **PARTIALLY MECHANIZED, AND THE FRAMING WAS THE WRONG ONE (PB-DP10, `scutemob-158`).** A
  > gate over card defs cannot see a variant no def uses — correct, and why a roster digest
  > has to scan the DSL *declaration*, not the corpus. But the honest marginal value is
  > smaller than the trigger implies: `Effect` is already in both the SR-8 and SR-17
  > closures, so a new variant already forces a PROTOCOL and a HASH bump — the **notice** is
  > free; what a digest would add is the **obligation** message ("classify it in `ROWS`"). A
  > needle scan for choice-shaped fields would also be worse than a whole-roster digest: the
  > DSL has only **four** such fields (`prompt`/`choices` on `Choose`, `optional` on
  > `LookAtTopThenPlace`, and the mode-target machinery), while `Proliferate`, `SearchLibrary`,
  > `Scry`, `DiscardCards`, `SacrificePermanents`, `Bolster`, `Connive`, `PutOnLibrary`,
  > `Discover`, `ChangeTargets` and `TheRingTemptsYou` carry **no** decision-shaped field at
  > all — the choice is inherent in the CR keyword action, not in the DSL shape. Not built
  > this batch (budget; owning seed **OOS-DP10-11**, filed by the closing review — the WIP's
  > own record of the drop pointed at two seeds that own a DIFFERENT enum's digest, not this
  > one); recommendation for whoever builds it: ship a whole-roster digest, not a needle scan,
  > and do not let the trigger's wording imply the needle set is the population.
- Any new `Command` variant — check whether it closes a §5 finding or is another
  accepted-and-discarded field (**DP-24**).
  > **NOT mechanized (PB-DP10, `scutemob-158`).** Feasible test-only as an SR-15-style scan
  > for `_`-bound `Command` fields in handlers; unbuilt. See **OOS-DP10-4**.
- After **PB-DP7** or any first pending-decision-that-blocks lands — re-run §3.1's sweep and
  re-derive the 277 figure. Exclude `defs/mod.rs`, and remember the union must include the
  modal-triggered-ability row — omitting it is how the first pass of this audit undercounted by 5.
  > **FIRED 2026-07-26** — PB-DP7 (`scutemob-155`) landed the first blocking pending decision,
  > and `Command::DiscardToHandSize` is the new `Command` the trigger above also names (that
  > one is already answered: **not** an accepted-and-discarded field, every field is validated).
  > **The §3.1 re-derivation is still owed** — tracked as **OOS-DP7-7**. A third trigger also
  > fired that this list did not have: a new `GameEvent` needs a `reveals_hidden_info` decision,
  > and checking it turned up a pre-existing wrong answer on a sibling (**OOS-DP7-3**). Add
  > "any new `GameEvent` variant" to this list.
  > **MECHANIZED — CLOSES OOS-DP7-7 (PB-DP10, `scutemob-158`).** The 277-def re-derivation is
  > now `crates/engine/tests/core/decision_gate.rs::decision_site_reconciliation_report`
  > (`T9`): it enumerates all 22 rows from `all_cards()` (SR-36, not grep), prints the live
  > all-rows union (**267**) and still-auto union (**97**), and prints the live denominator
  > and percentage — recomputed and asserted (`>=` floors) on every `cargo test`, not merely
  > owed. `T4`/`T6` additionally ratchet the still-auto figure against silent growth, which
  > was the invariant-level defect this whole trigger existed to catch.
- Any new `GameEvent` variant — decide its `reveals_hidden_info()` answer deliberately and
  check its siblings (Architecture Invariant 7). Added by PB-DP7; see **OOS-DP7-3**.
  > **FIRED AGAIN 2026-07-27** — PB-DP9 (`scutemob-157`) added
  > `GameEvent::EffectChoiceRequired` (`reveals_hidden_info() == true`) and, because a boolean
  > cannot say "for this seat", also added the `GameEvent::private_to() -> Option<PlayerId>`
  > method Architecture Invariant 7 had always named but which did not exist (**OOS-DP8-6**'s
  > declaration half, now closed). The sibling sweep it forces turned up
  > `Scried`/`Surveilled` answering `false` while committing to hidden library order —
  > **OOS-DP9-6**. **`private_to()` is a declaration, not an enforcement point**: nothing
  > consumes it until M10's server exists. Extend this trigger accordingly: a new `GameEvent`
  > now owes BOTH answers.
- **Any new `BlockingDecision` variant** — added by PB-DP9. Discharge the (now) **seven**
  obligations listed on the enum's doc comment in `rules/engine.rs`, of which only two are
  compile-forced. Obligation (7) is PB-DP9's own addition: state whether the new pending state
  belongs in `loop_detection.rs`'s mandatory-state fingerprint, **and argue it** — PB-DP7 and
  PB-DP8 both said yes, PB-DP9 says no, and the difference is that PB-DP9's state grows
  between successive replays of one resolution.
- **Any new same-zone caller of `move_object_to_zone` / `move_object_to_bottom_of_zone`** —
  added by PB-DP9. Both mint a fresh `ObjectId` unconditionally, which is a CR 400.7 deviation
  for a card that never leaves its zone, and consumes a `timestamp_counter` value (the shuffle
  seed source). Use `Zone::reposition_within` instead. See **OOS-DP9-11**.
- After a `Zone` API change — **DP-2** is a top/bottom inversion of exactly the kind PB-RS1
  swept; the sweep's roster did not include the mulligan, and a future roster should be
  derived from `Zone::push_front` call sites rather than hand-listed.
- When `docs/authoring-status.md` shows a material jump in `Complete` count — the §3.1
  percentage moves with it.
  > **MECHANIZED (PB-DP10, `scutemob-158`).** `decision_site_reconciliation_report` (`T9`)
  > prints the live effectively-`Complete` denominator and percentage on every run; `T6`'s
  > exact-equality ratchet reddens on the numerator (the still-auto union), not merely the
  > percentage, so a material authoring jump that also moves the numerator is caught by
  > construction.

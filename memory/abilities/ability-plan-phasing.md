# Ability Plan: Phasing

**Generated**: 2026-03-06
**CR**: 702.26
**Priority**: P4
**Similar abilities studied**: Decayed (EOC flag pattern, `game_object.rs:L420`), untap step (`turn_actions.rs:L681`)

## CR Rule Text

702.26. Phasing

702.26a Phasing is a static ability that modifies the rules of the untap step. During each player's untap step, before the active player untaps permanents, all phased-in permanents with phasing that player controls "phase out." Simultaneously, all phased-out permanents that had phased out under that player's control "phase in."

702.26b If a permanent phases out, its status changes to "phased out." Except for rules and effects that specifically mention phased-out permanents, a phased-out permanent is treated as though it does not exist. It can't affect or be affected by anything else in the game. A permanent that phases out is removed from combat. (See rule 506.4.)

702.26c If a permanent phases in, its status changes to "phased in." The game once again treats it as though it exists.

702.26d The phasing event doesn't actually cause a permanent to change zones or control, even though it's treated as though it's not on the battlefield and not under its controller's control while it's phased out. Zone-change triggers don't trigger when a permanent phases in or out. Tokens continue to exist on the battlefield while phased out. Counters and stickers remain on a permanent while it's phased out. Effects that check a phased-in permanent's history won't treat the phasing event as having caused the permanent to leave or enter the battlefield or its controller's control.

702.26e If a continuous effect generated by the resolution of a spell or ability modifies the characteristics or changes the controller of any objects, a phased-out permanent won't be included in the set of affected objects. This includes continuous effects that reference the permanent specifically, unless they also specifically refer to the permanent as phased out.

702.26f Continuous effects that affect a phased-out permanent may expire while that permanent is phased out. If so, they will no longer affect that permanent once it's phased in. In particular, effects with "for as long as" durations that track that permanent (see rule 611.2b) end when that permanent phases out because they can no longer see it.

702.26g When a permanent phases out, any Auras, Equipment, or Fortifications attached to that permanent phase out at the same time. This alternate way of phasing out is known as phasing out "indirectly." An Aura, Equipment, or Fortification that phased out indirectly won't phase in by itself, but instead phases in along with the permanent it's attached to.

702.26h If an object would simultaneously phase out directly and indirectly, it just phases out indirectly.

702.26i An Aura, Equipment, or Fortification that phased out directly will phase in attached to the object or player it was attached to when it phased out, if that object is still in the same zone or that player is still in the game. If not, that Aura, Equipment, or Fortification phases in unattached. State-based actions apply as appropriate. (See rules 704.5m and 704.5n.)

702.26j Abilities that trigger when a permanent becomes attached or unattached from an object or player don't trigger when that permanent phases in or out.

702.26k Phased-out permanents owned by a player who leaves the game also leave the game. This doesn't cause zone-change abilities to trigger. See rule 800.4.

702.26m If an effect causes a player to skip their untap step, the phasing event simply doesn't occur that turn.

702.26n In a multiplayer game, game rules may cause a phased-out permanent to leave the game or to be exiled once a player leaves the game. (See rules 800.4a and 800.4c.) If a phased-out permanent phased out under the control of a player who has left the game, that permanent phases in during the next untap step after that player's next turn would have begun.

702.26p Multiple instances of phasing on the same permanent are redundant.

## Key Edge Cases

- **Indirect phasing (CR 702.26g)**: Auras/Equipment attached to a phasing permanent phase out WITH it. They don't phase in independently -- they return when the host phases in, still attached.
- **Direct vs indirect (CR 702.26h)**: If something would phase out both directly (has Phasing keyword) and indirectly (attached to a phasing permanent), it phases out indirectly only.
- **NOT a zone change (CR 702.26d)**: Phasing does NOT cause zone-change triggers. Object identity is preserved. Counters, damage, attachments all persist. Tokens don't cease to exist.
- **Continuous effects exclusion (CR 702.26e)**: Phased-out permanents are excluded from continuous effect sets (e.g., "all creatures get +1/+1" does not apply to phased-out creatures).
- **Removed from combat (CR 702.26b, 506.4)**: If a permanent phases out during combat, it is removed from combat.
- **Multiplayer controller tracking (CR 702.26a/n)**: Must track which player controlled the permanent when it phased out, for phase-in. If that player leaves the game, special rules apply.
- **Skip untap = skip phasing (CR 702.26m)**: If a player's untap step is skipped, no phasing occurs.
- **Redundant instances (CR 702.26p)**: Multiple Phasing on same permanent is redundant (auto-deduped by OrdSet).
- **No attach/unattach triggers (CR 702.26j)**: Phasing in/out does not trigger attach/detach abilities.

## Current State (from ability-wip.md)

- [ ] Step 1: Enum variant
- [ ] Step 2: Rule enforcement
- [ ] Step 3: Trigger wiring (n/a -- phasing doesn't use triggers/stack)
- [ ] Step 4: Unit tests
- [ ] Step 5: Card definition
- [ ] Step 6: Game script

## Infrastructure Already Present

- `ObjectStatus::phased_out: bool` exists at `crates/engine/src/state/game_object.rs:L288`
- Already initialized to `false` in `builder.rs:L927`
- Already hashed in `hash.rs:L688`
- Comment in `turn_actions.rs:L676` says "For M2 we skip phasing and just untap"

## Implementation Steps

### Step 1: Enum Variant

**File**: `crates/engine/src/state/types.rs`
**Action**: Add `KeywordAbility::Phasing` variant after `Forecast` (line ~1081)
**Discriminant**: 118 (Forecast is 117)
**Doc comment**:
```
/// CR 702.26a: Phasing -- static ability modifying untap step rules.
/// "During each player's untap step, before the active player untaps permanents,
/// all phased-in permanents with phasing that player controls phase out.
/// Simultaneously, all phased-out permanents that had phased out under that
/// player's control phase in."
///
/// CR 702.26p: Multiple instances are redundant (auto-deduped by OrdSet).
/// Phasing does NOT use the stack (CR 702.26d -- no zone change, no triggers).
///
/// Infrastructure: `ObjectStatus::phased_out` (already exists at game_object.rs:L288).
Phasing,
```

**Match arms**: Grep for exhaustive `KeywordAbility` match expressions and add `Phasing` arm. Key locations:
- `crates/engine/src/state/hash.rs` -- `HashInto` for `KeywordAbility` (likely uses `#[derive]`, verify)
- `crates/engine/src/rules/combat.rs` -- evasion/keyword checks (if exhaustive match exists)
- Any `match kw { ... }` patterns

### Step 2: New Fields on GameObject

**File**: `crates/engine/src/state/game_object.rs`
**Action**: Add two fields to `GameObject`:

```rust
/// CR 702.26a/g: If true, this permanent phased out indirectly (was attached
/// to another permanent that phased out). Indirectly-phased permanents don't
/// phase in by themselves -- they phase in only when their host phases in.
///
/// Set during the phasing-out step in turn_actions.rs. Reset when the permanent
/// phases back in.
#[serde(default)]
pub phased_out_indirectly: bool,

/// CR 702.26a: The player who controlled this permanent when it phased out.
/// Used to determine which player's untap step phases it back in.
/// Only meaningful when `status.phased_out` is true.
#[serde(default)]
pub phased_out_controller: Option<PlayerId>,
```

**Hash**: Add both to `state/hash.rs` in the `HashInto` impl for `GameObject`.

**Init sites**: Initialize `phased_out_indirectly: false` and `phased_out_controller: None` in:
- `builder.rs` (object construction)
- `effects/mod.rs` (token creation -- search for `is_token: true`)
- `resolution.rs` (if tokens are created there)

### Step 3: GameEvent Variants

**File**: `crates/engine/src/rules/events.rs`
**Action**: Add two new `GameEvent` variants:

```rust
/// CR 702.26a: Permanents phased out during the untap step.
/// Includes both directly-phased (Phasing keyword) and indirectly-phased
/// (attached Auras/Equipment).
PermanentsPhasedOut {
    player: PlayerId,
    objects: Vec<ObjectId>,
},

/// CR 702.26a: Permanents phased in during the untap step.
PermanentsPhasedIn {
    player: PlayerId,
    objects: Vec<ObjectId>,
},
```

**Hash**: Add both to `state/hash.rs` in the `HashInto` impl for `GameEvent`.

### Step 4: Untap Step -- Phase Out / Phase In

**File**: `crates/engine/src/rules/turn_actions.rs`
**Function**: `untap_active_player_permanents` (line 681)
**Action**: BEFORE the existing untap logic, insert phasing logic per CR 702.26a:

1. **Phase IN** all phased-out permanents where `phased_out_controller == Some(active)`:
   - Collect all objects where `obj.status.phased_out && obj.phased_out_controller == Some(active) && !obj.phased_out_indirectly`
   - For each, set `obj.status.phased_out = false`, `obj.phased_out_controller = None`
   - Also phase in all indirectly-phased objects attached to them: find objects where `obj.phased_out_indirectly && obj.attached_to == Some(host_id)` and clear their phased_out status too
   - Emit `PermanentsPhasedIn` event
   - Do NOT fire any ETB triggers (CR 702.26d)

2. **Phase OUT** all phased-in permanents with Phasing keyword controlled by active player:
   - Collect all objects where `obj.zone == ZoneId::Battlefield && obj.controller == active && !obj.status.phased_out` AND the object has `KeywordAbility::Phasing` in its characteristics (use `calculate_characteristics` to check layer-resolved keywords)
   - For each, check CR 702.26h: if it would phase out directly AND indirectly (attached to another Phasing permanent), only phase out indirectly
   - Set `obj.status.phased_out = true`, `obj.phased_out_controller = Some(active)`
   - **Indirect phasing (CR 702.26g)**: For each permanent that phases out, find all attached Auras/Equipment (`obj.attachments`) that are NOT already phasing out directly, and phase them out too with `phased_out_indirectly = true`
   - Emit `PermanentsPhasedOut` event
   - Remove phased-out permanents from combat if combat is active (CR 506.4)

3. **Then proceed with existing untap logic** -- but SKIP phased-out permanents (they don't untap)

**Pattern**: The existing function collects `ids_on_battlefield` and iterates. Add phasing before this collection. Modify the collection filter to exclude `obj.status.phased_out`.

**CR citations**: CR 702.26a (ordering: phase in, then phase out, then untap), CR 702.26g (indirect), CR 702.26h (direct+indirect = indirect)

### Step 5: Battlefield Visibility Filter

**Concept**: A phased-out permanent is "treated as though it does not exist" (CR 702.26b). This requires adding `!obj.status.phased_out` filters to MANY locations. The recommended approach is a helper function:

**File**: `crates/engine/src/state/game_object.rs` (or a new helper in `state/mod.rs`)
**Action**: Add a helper method:
```rust
impl GameObject {
    /// CR 702.26b: Returns true if this permanent is visible (not phased out).
    /// Phased-out permanents are treated as though they don't exist.
    pub fn is_phased_in(&self) -> bool {
        !self.status.phased_out
    }
}
```

Then add `&& obj.is_phased_in()` (or `&& !obj.status.phased_out`) to these locations:

#### 5a. Untap step (turn_actions.rs)
- `untap_active_player_permanents`: filter to `!obj.status.phased_out` in the untap loop (line ~689)
- All other battlefield filters in turn_actions.rs (upkeep triggers, end step, cleanup)

#### 5b. SBA checks (sba.rs)
- Lines 111, 127, 158, 987: every `obj.zone == ZoneId::Battlefield` filter must also check `!obj.status.phased_out`
- Phased-out permanents are immune to ALL SBAs (zero toughness, lethal damage, unattached Auras, legend rule, etc.)

#### 5c. Combat (combat.rs)
- Lines 183, 711, 784, 901: declare attackers/blockers -- phased-out creatures can't attack or block
- Damage assignment: phased-out creatures shouldn't deal or receive combat damage

#### 5d. Continuous effects / Layers (layers.rs)
- CR 702.26e: phased-out permanents excluded from continuous effect sets
- Line 95: already filters `obj.zone == ZoneId::Battlefield` -- add `&& !obj.status.phased_out`
- Lines 183-187: source check -- phased-out source means effect doesn't apply
- Lines 214+: `AllCreatures`, `AllCreaturesOfType`, `AllPermanents` etc. filters in `effect_applies` must exclude phased-out

#### 5e. Triggered abilities (abilities.rs)
- Lines 2300, 2442, 2465, 2487, 2594, 2781, 2826, 2984, 3236, 3257, 3358, 3444, 3523, 3613, 3656: all battlefield scans for trigger sources/targets
- A phased-out permanent can't be a trigger source and shouldn't trigger from events affecting it

#### 5f. Targeting (casting.rs / resolution.rs)
- Lines 2364, 4122, 4341: target validation -- phased-out permanents can't be targeted

#### 5g. Effects (effects/mod.rs)
- Line 2202: effect resolution filters -- phased-out permanents excluded from "each creature" effects

#### 5h. Replacement effects (replacement.rs)
- Line 231: source check for replacement effects

#### 5i. Engine command handlers (engine.rs)
- Lines 561, 727: command validation -- can't interact with phased-out permanents

**IMPORTANT**: Do NOT add phased-out filtering to:
- `calculate_characteristics()` itself -- characteristics are preserved while phased out (CR 702.26d)
- Zone tracking -- phased-out objects remain on the battlefield (zone doesn't change)

### Step 6: Unit Tests

**File**: `crates/engine/tests/phasing.rs`
**Tests to write**:

1. `test_phasing_basic_phase_out_on_untap` -- CR 702.26a
   - Creature with Phasing on battlefield. Pass through untap step. Assert `status.phased_out == true`.
   - Verify creature is excluded from battlefield count.

2. `test_phasing_basic_phase_in_on_next_untap` -- CR 702.26a
   - Setup: creature already phased out. Pass through untap step. Assert `status.phased_out == false`.
   - Verify creature is back in battlefield count.

3. `test_phasing_indirect_aura_phases_out_with_host` -- CR 702.26g
   - Creature with Phasing has an Aura attached. Both phase out on untap.
   - Assert both are phased out. Assert Aura's `phased_out_indirectly` is true.

4. `test_phasing_indirect_equipment_phases_out_with_host` -- CR 702.26g
   - Same as above but with Equipment instead of Aura.

5. `test_phasing_indirect_phases_in_together` -- CR 702.26g
   - Creature + attached Aura both phased out. On controller's next untap, both phase in together.
   - Assert Aura is still attached after phase-in.

6. `test_phasing_no_etb_triggers_on_phase_in` -- CR 702.26d
   - Creature with an ETB trigger phases in. Assert NO ETB trigger fires.

7. `test_phasing_no_zone_change_on_phase_out` -- CR 702.26d
   - Creature phases out. Assert ObjectId is unchanged (no new object created).
   - Assert counters are preserved.

8. `test_phasing_excluded_from_sba` -- CR 702.26b
   - Creature with 0 toughness (from counters) is phased out. Run SBAs. Assert it is NOT destroyed.

9. `test_phasing_excluded_from_combat` -- CR 702.26b
   - Phased-out creature cannot be declared as attacker.

10. `test_phasing_excluded_from_continuous_effects` -- CR 702.26e
    - "All creatures get +1/+1" continuous effect. Phased-out creature should NOT get the bonus.
    - When it phases back in, it should then get the bonus.

11. `test_phasing_tokens_survive` -- CR 702.26d
    - Token with Phasing phases out. Assert token still exists (NOT destroyed by SBA 704.5d).
    - When it phases in, assert it's a functional creature.

12. `test_phasing_removed_from_combat` -- CR 506.4
    - Creature declared as attacker. Something causes it to phase out mid-combat.
    - Assert it's removed from combat.

13. `test_phasing_multiplayer_controller_tracking` -- CR 702.26a
    - Player A's creature with Phasing phases out during A's untap.
    - Verify it phases in during A's NEXT untap, not during player B's or C's untap.

14. `test_phasing_redundant_instances` -- CR 702.26p
    - Creature with two instances of Phasing. Verify behavior is identical to one instance.

**Pattern**: Follow tests for Decayed in `tests/decayed.rs` (EOC flag pattern) and combat evasion tests.

### Step 7: Card Definition (later phase)

**Suggested card**: Teferi's Isle
- Legendary Land, "Phasing. Teferi's Isle enters tapped. {T}: Add {U}{U}."
- Simple land with Phasing -- minimal complexity for testing the core mechanic.

**Alternative card**: Weatherseed Treefolk (or any simple creature with Phasing)

**Card lookup**: use `card-definition-author` agent

### Step 8: Game Script (later phase)

**Suggested scenario**: Teferi's Isle phasing cycle
- Turn 1: Play Teferi's Isle (enters tapped)
- Turn 2: Untap step -- Teferi's Isle phases out. Verify it's not available for tapping.
- Turn 3: Untap step -- Teferi's Isle phases in. Tap for {U}{U}.

**Subsystem directory**: `test-data/generated-scripts/baseline/`

## Interactions to Watch

- **Layer system**: Phased-out permanents must be excluded from `effect_applies` filters but their characteristics are still calculable (for "last known information" purposes)
- **SBA system**: Every SBA that iterates battlefield permanents needs the phased-out exclusion
- **Combat system**: Phase-out during combat means removal from combat (506.4)
- **Attachment tracking**: Auras/Equipment don't fall off on phase-out -- they phase out indirectly. This is fundamentally different from leaving the battlefield.
- **Object identity**: Phasing preserves ObjectId (CR 702.26d) -- this is the OPPOSITE of a zone change. No new ObjectId is created. This is critical and different from almost every other mechanic.
- **Multiplayer turn order**: Phase-in happens on the controller's untap step. Must track which player's control the permanent was under when it phased out.
- **Skip untap (CR 702.26m)**: If a player's untap step is skipped entirely (e.g., Stasis), phasing doesn't happen. The engine already has step-skipping infrastructure to check.
- **Humility interaction**: If Humility removes the Phasing keyword after a creature has already phased out, the creature still phases back in (phase-in doesn't require the keyword -- CR 702.26a says "all phased-out permanents that had phased out under that player's control phase in")

## Scope Notes

This implementation covers the CORE phasing mechanic:
- KeywordAbility::Phasing enum variant
- Phase in/out during untap step
- Indirect phasing for attachments
- Battlefield visibility filtering (~30+ filter sites)
- GameEvent emission
- Comprehensive unit tests

**Deferred** (address in future batches if needed):
- CR 702.26n: Multiplayer player-leaves-game phasing rules (complex, rarely relevant)
- CR 702.26i: Direct phase-out of Auras/Equipment (only matters for cards that explicitly phase out an Aura, not for the Phasing keyword itself)
- "Phase out target permanent" effects (Teferi's Protection, Slip Out the Back) -- these are card-specific effects, not the Phasing keyword

# Authoring worklist from the marker sweep — 2026-07-16 (scutemob-88)

Cards whose stated blocker **has shipped** but whose def never used it. These are
ready to author: the primitive exists and the chain was traced. Grouped by blocker so
one worker can clear a group in one pass.

**Not authored in scutemob-88** — this task audits and corrects markers; authoring is
W-PB2 / W-EMPTY work.

Cards whose note was stale but which remain un-authorable for a *different* reason are
**deliberately excluded** — their notes were rewritten instead, so this list does not
send a worker at a wall.

**116 ready cards across 16 blocker groups.**

## EffectAmount::PermanentCount / count-scaled amounts & tokens  (18)

- **Access Denied** (`access_denied.rs`) — Note says 'Stripped until MV-tracking + variable token count is in DSL'. Both shipped: TokenSpec.count is an EffectAmount (card_definition.rs:3687) and EffectAmount::ManaValueOf(EffectTarget) exists (:2493), resolving via state.fizzle_object so the countered spell's MV is still readable after it hits the graveyard (effects/mod.rs:6686). Authorable as Sequence[CounterSpell(DeclaredTarget 0), CreateToken{count: ManaValueOf(DeclaredTarget 0)}].
- **Avenger of Zendikar** (`avenger_of_zendikar.rs`) — Note claims 'EffectAmount lacks count of lands you control variant'. FALSE: EffectAmount::PermanentCount { filter, controller } resolves at effects/mod.rs:6749, and Effect::CreateToken resolves spec.count through resolve_amount at effects/mod.rs:669 (CR 608.2h) before token-doubling replacements. ashaya_soul_of_the_wild.rs:34-39 uses exactly PermanentCount { has_card_type: Land, controller: Controller }. WRONG STATE TODAY: EffectAmount::Fixed(5) at line 31 hardcodes 5 Plant tokens regardless of land count — this is an approximation that produces incorrect board state, not an omission. Second clause (landfall: +1/+1 counter on each Plant) also looks expressible via ForEachTarget::EachPermanentMatching + AddCounter, but was not traced end-to-end.
- **Blackblade Reforged** (`blackblade_reforged.rs`) — The headline blocker is FALSE: `LayerModification::ModifyBothDynamic { amount, negate }` exists and is live in layers.rs:1269 and effects/mod.rs:3008, and `EffectAmount::PermanentCount` + `EffectFilter::AttachedCreature` cover '+1/+1 for each land you control'. That clause is authorable today. Two things still block Complete: (a) the second 'Equip legendary creature {3}' alternative equip cost has no representation, and (b) ADDITIONAL FINDING — this def has NO working equip ability at all: `AbilityDefinition::Keyword(KeywordAbility::Equip)` is a bare marker; the real ability is an `Activated { cost, effect: Effect::AttachEquipment }` (see skullclamp.rs:55-60). So the card is more incomplete than its note says, but the pump clause is real rewiring work.
- **Canopy Crawler** (`canopy_crawler.rs`) — The note's stated blocker is FALSE: it says 'Requires EffectAmount::CountersOnSelf or similar variant (not yet in DSL)', but EffectAmount::CounterCount { target: EffectTarget::Source, counter: PlusOnePlusOne } exists at card_definition.rs:2511-2514 and its doc comment literally reads 'Used for "draw cards equal to the number of +1/+1 counters on this creature"'. Targeted activated abilities exist (PB-5) and LayerModification::ModifyBothDynamic exists. NOT an upgrade — the def has no ability body at all, and the rewiring author must resolve one real semantic question: CR 608.2 locks the pump amount in at RESOLUTION, whereas ModifyBothDynamic recalculates continuously, so a naive ModifyBothDynamic(CounterCount{Source}) would keep growing if Crawler gains counters later. Flag for the authoring worklist, not a mechanical flip.
- **Commissar Severina Raine** (`commissar_severina_raine.rs`) — The note's first blocker is FALSE: EffectAmount::AttackingCreatureCount { controller, filter } exists at card_definition.rs:2697-2700 (PB-AC3, hash disc 19) and its doc comment at 2689-2690 says verbatim '`filter` with `exclude_self: true` gives "other attacking creatures" (Commissar Severina Raine)'. Wired at effects/mod.rs resolve_amount and layers.rs resolve_cda_amount. With TriggerCondition::WhenAttacks, the attack trigger is authorable NOW. The second blocker is still real, though for a different reason than the note gives: Cost::Sacrifice(TargetFilter) EXISTS (card_definition.rs:1226), but the lowering to the runtime SacrificeFilter (replay_harness.rs:3743-3767) reads only has_chosen_subtype / has_subtype / has_card_type and DISCARDS exclude_self — SacrificeFilter's 6 variants have no self-exclusion — so 'Sacrifice another creature' would silently permit sacrificing Severina herself. Author the trigger; leave the activated ability blocked.
- **Crown of Skemfar** (`crown_of_skemfar.rs`) — Note's stated blocker — 'No EffectAmount variant for counting permanents of a subtype' — is FALSE. EffectAmount::PermanentCount { filter: TargetFilter, controller: PlayerTarget } exists and TargetFilter::has_subtype (card_definition.rs:2833) expresses 'Elf'; destiny_spinner.rs:52-67 already uses PermanentCount with a type filter. Route: AbilityDefinition::Static stores modification verbatim (replacement.rs:2051-2073) and layers.rs:1269-1279 live-evaluates ModifyBothDynamic via resolve_cda_amount, so Static + ModifyBothDynamic + EffectFilter::AttachedCreature satisfies CR 611.3a. NOT an upgrade — the def has no P/T ability at all. CAVEAT for the worker: AbilityDefinition::CdaModifyPowerToughness is NOT usable here (replacement.rs:2192 hard-codes filter: SingleObject(new_id) = the Aura itself); and resolve_cda_amount is passed the AFFECTED object's controller (layers.rs:1270-1274), so 'Elves you control' will resolve to the enchanted creature's controller, which is wrong when the Aura is on an opponent's creature. Verify that before flipping to Complete.
- **Dragon Tempest** (`dragon_tempest.rs`) — Both cited blockers are STALE. (1) 'WheneverCreatureEntersBattlefield has a filter but no has-flying attribute filter' — TargetFilter.has_keywords: OrdSet<KeywordAbility> exists (:2813), so controller=You + has_keywords={Flying} expresses it. (2) 'EffectAmount::CountCreaturesYouControl with subtype filter not present' — EffectAmount::PermanentCount{filter, controller} exists and TargetFilter.has_subtype covers Dragon. EffectTarget::TriggeringCreature exists for the 'it gains haste' / 'it deals X damage' source binding.
- **Elenda, the Dusk Rose** (`elenda_the_dusk_rose.rs`) — Oracle (MCP) matches def; lifelink and the 'another creature dies' counter trigger (:24-38) are correct. The note's 'EffectAmount lacks power-based count' is STALE — EffectAmount::SourcePowerAtLastKnownInformation shipped in PB-LKI-Power (hash.rs:103) for exactly this shape ('when this dies, X = its power'). Full chain verified: variant card_definition.rs:2611-ff region; resolution at effects/mod.rs:7003 (ctx.lki_power.unwrap_or(0)); ctx populated for SelfDies triggers at abilities.rs:4165-4167 ('CR 603.10a: capture the dying creature's layer-resolved power into LKI so EffectAmount::SourcePowerAtLastKnownInformation can resolve'); TokenSpec.count is an EffectAmount (card_definition.rs:3687). NOT upgradeable as written: elenda_the_dusk_rose.rs:52 hardcodes count: EffectAmount::Fixed(3), which is active wrong game state — with the counter trigger feeding her power, X is almost never 3. Arguably known_wrong until rewired.
- **Elenda's Hierophant** (`elendas_hierophant.rs`) — Oracle (MCP) matches def; flying and the WheneverYouGainLife counter trigger (:20-33) are correct. The note's 'power-based token count not in DSL' is STALE for the same chain as Elenda, the Dusk Rose: EffectAmount::SourcePowerAtLastKnownInformation resolves at effects/mod.rs:7003 from ctx.lki_power, which is populated on SelfDies triggers at abilities.rs:4165-4167 (CR 603.10a), and TokenSpec.count is an EffectAmount (card_definition.rs:3687). Cleaner than Elenda: the death trigger is simply absent (:34-35) rather than approximated, so no wrong game state is live today — the card is silently missing an ability, which partial correctly describes.
- **Elven Ambush** (`elven_ambush.rs`) — Note claims 'count-based token creation not in DSL'. FALSE on every link: TokenSpec.count is typed `EffectAmount` (card_definition.rs:3687); Effect::CreateToken resolves it dynamically via `resolve_amount(state, &spec.count, ctx)` (effects/mod.rs:669, CR 608.2h); EffectAmount::PermanentCount { filter, controller } has a resolver arm at effects/mod.rs:6749. Working precedent for exactly this shape: howlsquad_heavy.rs:43-49 (PermanentCount with has_subtype Goblin as a token count). Def currently ships EffectAmount::Fixed(3) (line 23) — a wrong-game-state approximation, not a gap.
- **Elvish Guidance** (`elvish_guidance.rs`) — Note says 'Not expressible — same gap as Wild Growth plus count scaling.' Both halves are false. Wild Growth itself ships fully today (wild_growth.rs:18-31, no completeness marker) using TriggerCondition::WhenTappedForMana { source_filter: ManaSourceFilter::EnchantedLand } — shipped by PB-E (docs/card-authoring-operations.md:274). The count-scaling half is Effect::AddManaScaled { player, color, count: EffectAmount } (card_definition.rs:1490-1494), and 'for each Elf on the battlefield' is EffectAmount::PermanentCount with controller: PlayerTarget::EachPlayer — precedent brightstone_ritual.rs:17-23 ('each Goblin on the battlefield'). Def currently has only the Enchant keyword.
- **Elvish Promenade** (`elvish_promenade.rs`) — Same false claim as elven_ambush.rs: TokenSpec.count is EffectAmount (card_definition.rs:3687), resolved at effects/mod.rs:669, PermanentCount arm at effects/mod.rs:6749, token-count precedent howlsquad_heavy.rs:43-49. Def ships Fixed(3) (line 23). SECOND, UNSTATED defect: oracle type line is 'Kindred Sorcery — Elf' (MCP lookup) but the def is types_sub(&[CardType::Sorcery], &["Elf"]) (line 10) — the Kindred/Tribal card type is missing.
- **Éomer, King of Rohan** (`eomer_king_of_rohan.rs`) — Note claims 'AddCounters does not yet support dynamic EffectAmount (only Fixed)'. FALSE twice over: (a) ReplacementModification::EntersWithCounters { counter, count: Box<EffectAmount> } (replacement_effect.rs:141-144) — the correct primitive, since oracle says 'Éomer ENTERS WITH a +1/+1 counter on it for each other Human you control', which is a replacement effect (CR 614.1c), not the trigger the note assumes; and (b) Effect::AddCounterAmount { target, counter, count: EffectAmount } (card_definition.rs:1543-1547) exists for the trigger shape anyway. 'Each OTHER Human you control' is EffectAmount::PermanentCount with TargetFilter { has_subtype: Human, controller: You, exclude_self: true }. Clauses 1 and 3 are correct: DoubleStrike (line 29) and the two-target ETB (BecomeMonarch on target player + DealDamage PowerOf(Source) to any target, lines 35-60). Oracle text field matches MCP exactly.
- **Inspiring Call** (`inspiring_call.rs`) — The note's claim ('EffectAmount lacks PermanentCountWithCounter') is FALSE: TargetFilter.has_counter_type exists (card_definition.rs:2970) and IS checked on the PermanentCount path — see effects/mod.rs check_has_counter_type call sites and the reference impl armorcraft_judge.rs:35, which is exactly PermanentCount + has_counter_type. The draw count is rewirable NOW. The indestructible grant is still blocked (no EffectFilter variant for 'creatures you control with a +1/+1 counter'). ENGINE-FINDING: as written the def is not merely partial — it DRAWS THE WRONG NUMBER OF CARDS (counts every creature you control, per the line 21 PermanentCount with no counter filter). Until rewired this is known_wrong behavior, not an omission.
- **Regal Force** (`regal_force.rs`) — Note says "'green creature' filter — PermanentCount with color filter" is missing. It is not: TargetFilter has `colors: Option<OrdSet<Color>>` (card_definition.rs:2815) and matches_filter honors it (effects/mod.rs:7962-7966); PermanentCount routes through matches_filter (effects/mod.rs:6763). The def currently counts ALL creatures you control (line 22-29) — a live wrong-game-state approximation. Fix is a one-line filter addition: colors: Some({Green}).
- **Scourge of Valkas** (`scourge_of_valkas.rs`) — Both stated blockers are FALSE. (1) 'this creature OR another Dragon you control enters' — TriggerCondition::WheneverCreatureEntersBattlefield{filter, exclude_self} covers it with filter{controller: You, has_subtype: Dragon}, exclude_self: false; the SIBLING def satoru_the_infiltrator.rs:26 uses exactly this shape. (2) 'EffectAmount::CountCreaturesYouControl(filter) not in DSL' — EffectAmount::PermanentCount{filter: TargetFilter, controller: PlayerTarget} EXISTS at card_definition.rs:2502 and takes a full TargetFilter (has_subtype: Dragon). Targeted triggered abilities shipped in PB-5, so 'deals X damage to any target' is a Triggered with targets: vec![TargetRequirement::TargetAny]. Real authoring work remains, so NOT an upgrade — and a rewiring worker must note the deviation that 'IT deals X damage' means the ENTERING Dragon is the damage source, not Scourge (matters for lifelink/damage triggers). The {R} pump ability (lines 25-41) is already correct.
- **Shaman of the Pack** (`shaman_of_the_pack.rs`) — The note's blocker is FALSE: it asks for 'EffectAmount::PermanentCountWithSubtype(SubType("Elf"), TargetController::You)', and EffectAmount::PermanentCount{filter: TargetFilter, controller: PlayerTarget} EXISTS at card_definition.rs:2502 and takes a full TargetFilter, so filter{has_subtype: SubType("Elf"), controller: TargetController::You} + controller: PlayerTarget::Controller expresses 'the number of Elves you control' precisely. The ETB is then TriggerCondition::WhenEntersBattlefield + Effect::LoseLife{player: DeclaredTarget{index:0}, amount: PermanentCount{...}} with targets: [TargetRequirement::TargetPlayer]. Not an upgrade — abilities is still vec![], this is real authoring work. Rewiring caveat: oracle says 'target OPPONENT', and TargetRequirement::TargetPlayer is unqualified, so opponent-only restriction needs checking before authoring (same shape as KI-1).
- **Shamanic Revelation** (`shamanic_revelation.rs`) — Note claims EffectAmount lacks a 'count permanents matching power filter' variant. FALSE: TargetFilter.min_power exists (card_definition.rs:2809) and IS enforced by matches_filter (effects/mod.rs:7947-7951), which EffectAmount::PermanentCount calls at effects/mod.rs:6749-6761. The Ferocious clause is expressible today as Effect::ForEach{ target: ForEachTarget::EachPermanentMatching(TargetFilter{min_power:Some(4), has_card_type:Creature, controller:You}), effect: GainLife{Controller, Fixed(4)} }. The def does NOT do this — the Sequence has only the DrawCards arm plus a TODO comment (line 27), so real authoring work remains.

## TargetFilter fields that shipped (legendary / nonbasic / max_cmc / is_attacking / card-types / subtypes / colors)  (18)

- **Baron Bertram Graywater** (`baron_bertram_graywater.rs`) — The ENGINE-BLOCKED comment's central claim — 'is_token ... is NOT confirmed wired into the creature-ETB trigger dispatch' — is FALSE. abilities.rs:6256-6263 reads trigger_def.triggering_creature_filter on the ETB path and explicitly guards BOTH `if creature_filter.is_token && !entering_obj.is_token { continue; }` and the is_nontoken mirror, with a comment stating it mirrors the death-path guards; enrich_spec_from_def forwards the full carddef TargetFilter as triggering_creature_filter (replay_harness.rs:2701, PB-AC0). Combined with once_per_turn (PB-AC1), the trigger is authorable. Two caveats for the rewiring worker: (1) oracle says 'one or more TOKENS', not creature tokens — use WheneverPermanentEntersBattlefield, not WheneverCreatureEntersBattlefield, or Treasures/Clues will be missed; (2) 'one or more ... enter' is a batched trigger, but once_per_turn: true makes the batching difference unobservable in practice. SEPARATE DEFECT in the shipped ability: Cost::Sacrifice(TargetFilter::default()) at line 38 accepts ANY permanent — oracle requires 'another creature or artifact', so Baron can currently sac a land, or himself.
- **Goblin Cratermaker** (`goblin_cratermaker.rs`) — Note claims the 'colorless' constraint 'is not expressible in TargetFilter' — FALSE. TargetFilter::exclude_colors exists (card_definition.rs:2818) with 'object must share none' semantics, enforced in matches_filter at effects/mod.rs:7967-7971, reached for TargetPermanentWithFilter via casting.rs:6233-6237. 'Colorless' = exclude_colors: Some({W,U,B,R,G}). Def uses only non_land (goblin_cratermaker.rs:50-53), so mode 1 can currently destroy any colored nonland permanent. Real authoring work; not done here.
- **Jagged-Scar Archers** (`jagged_scar_archers.rs`) — BOTH of the note's claims are false. (1) 'no EffectAmount::PowerOf(Source)' — EffectAmount::PowerOf(EffectTarget) exists at card_definition.rs:2489, and its docs (2642-2647) explicitly bless 'PowerOf for abilities that fire while the source is still on the battlefield (e.g. {T}: deal damage equal to this creature\'s power)' — Jagged-Scar Archers exactly. (2) 'no TargetFilter for with flying' — TargetFilter.has_keywords: OrdSet<KeywordAbility> exists at card_definition.rs:2813, usable via TargetRequirement::TargetCreatureWithFilter. The CDA is already correct. Rewiring: Activated { cost: Cost::Tap, effect: DealDamage { target: DeclaredTarget{0}, amount: PowerOf(EffectTarget::Source) }, targets: vec![TargetCreatureWithFilter(TargetFilter { has_card_type: Some(Creature), has_keywords: {Flying}, ..default })] }.
- **Junji, the Midnight Sky** (`junji_the_midnight_sky.rs`) — Note says the Dragon exclusion is not expressible — FALSE. TargetFilter::exclude_subtypes exists (card_definition.rs:2887) and is checked in matches_filter at effects/mod.rs:8058-8066 ('reject if object has ANY of the excluded subtypes'), reached for TargetCardInGraveyard via casting.rs:6319-6340. Fix: exclude_subtypes: vec![SubType("Dragon")] on the filter at junji_the_midnight_sky.rs:41-44. Note that mode 1 is also currently unreachable (Effect::Choose auto-picks mode 0), which limits the practical impact but not the correctness of the marker.
- **Keeper of Fables** (`keeper_of_fables.rs`) — The note ('No such trigger condition exists in the DSL') is FALSE, and the shipped variant's own docstring names this card's clause. Full chain verified: TriggerCondition::WhenOneOrMoreCreaturesYouControlDealCombatDamageToPlayer { filter: Option<TargetFilter> } at card_definition.rs:3245 (docs: 'Optional filter restricts which creatures count (e.g., "non-Human creatures")'); builder arm replay_harness.rs:3057 → TriggerEvent::AnyCreatureYouControlBatchCombatDamage threading combat_damage_filter: filter.clone() (:3065); dispatch rules/abilities.rs:4959-5066, which correctly fires ONCE per (controller, damaged_player) pair per combat damage step (CR 510.3a/603.2c) — the batch semantics 'one or more' requires. 'non-Human' is covered by TargetFilter.exclude_subtypes: Vec<SubType> (card_definition.rs:2887). Rewiring: Triggered { trigger_condition: WhenOneOrMoreCreaturesYouControlDealCombatDamageToPlayer { filter: Some(TargetFilter { has_card_type: Some(Creature), exclude_subtypes: vec![SubType("Human")], ..default }) }, effect: DrawCards { player: Controller, count: Fixed(1) } }. Not upgraded here only because the def body is vec![].
- **Kor Haven** (`kor_haven.rs`) — Note says the 'attacking creature' restriction 'is not enforced' — the capability now exists. TargetFilter::is_attacking was added by PB-XA2 (card_definition.rs:2888-2906) and is enforced for TargetCreatureWithFilter in casting.rs via passes_combat_role (:6294-6306, reading state.combat.attackers). Def still uses bare TargetRequirement::TargetCreature (kor_haven.rs:37), so any creature is a legal target. Fix: TargetCreatureWithFilter(TargetFilter { is_attacking: true, .. }).
- **Maze of Ith** (`maze_of_ith.rs`) — The def's header comment claims 'no TargetAttackingCreature variant exists' — stale. TargetFilter::is_attacking (card_definition.rs:2888-2906, PB-XA2) is enforced for TargetCreatureWithFilter via passes_combat_role in casting.rs:6294-6306. Def still uses bare TargetRequirement::TargetCreature (maze_of_ith.rs:31), so Maze can currently untap/protect any creature at any time — a significant behavioral deviation, not the harmless one the header comment suggests. Fix: TargetCreatureWithFilter(TargetFilter { is_attacking: true, .. }).
- **Minamo, School at Water's Edge** (`minamo_school_at_waters_edge.rs`) — Note claims 'TargetFilter lacks supertype constraint' — STALE. TargetFilter.legendary: bool exists (card_definition.rs:2855-2858, doc cites CR 205.4a) and IS enforced in matches_filter at effects/mod.rs:8045 (checks chars.supertypes for Legendary); hashed at hash.rs. The def passes TargetPermanentWithFilter(TargetFilter { ..Default::default() }) (lines 35-37), i.e. an unrestricted filter, so it can currently untap ANY permanent. Setting legendary: true is a one-field fix and appears to complete the card (both mana ability and untap ability otherwise match oracle). Not upgrading here per the strict bar — the fix is unwritten, so this is authoring work, not marker lag.
- **Mind Games** (`mind_games.rs`) — Note claims "TargetFilter doesn't have a compound 'artifact OR creature OR land' filter" — STALE. TargetFilter.has_card_types: Vec<CardType> exists with explicit OR semantics (card_definition.rs:2850-2854, doc cites 'instant or sorcery card' / 'artifact or enchantment' use cases) and IS enforced in matches_filter at effects/mod.rs:8035. The def uses bare TargetRequirement::TargetPermanent (line 30), which illegally allows targeting enchantments and planeswalkers. Rewire to TargetPermanentWithFilter(TargetFilter { has_card_types: vec![Artifact, Creature, Land], ..Default }). Buyback and TapPermanent are correct. LOW: the note's first sentence ('this is a tap, not a choice between tap/untap') describes a bug the def does not have — the def taps.
- **Oko, Thief of Crowns** (`oko_thief_of_crowns.rs`) — Both filters the note calls omitted are expressible today. has_card_types has OR semantics ('artifact or creature') and is checked by matches_filter, reached for TargetPermanentWithFilter at casting.rs:6233-6237; TargetController::You/Opponent is enforced at casting.rs:6238-6246. The note also UNDERSELLS the defect: on the -5 the def uses bare TargetPermanent for target_a (oko_thief_of_crowns.rs:91) where oracle says 'artifact or creature YOU CONTROL', and TargetCreatureWithFilter{max_power:3} for target_b (:92-95) where oracle says 'creature an OPPONENT controls with power 3 or less' — so the controller constraints are missing on both halves, not just the type filters. The +1 likewise uses bare TargetPermanent (:78) and can currently Elk a land or a planeswalker.
- **Otawara, Soaring City** (`otawara_soaring_city.rs`) — Oracle: 'Return target artifact, creature, enchantment, or planeswalker to its owner's hand.' has_card_types (OR semantics, card_definition.rs:2850-2854) expresses this exactly and is checked by matches_filter via casting.rs:6233-6237. Def uses non_land: true (otawara_soaring_city.rs:39-42), which additionally admits Battles (CardType::Battle exists, types.rs:37) — the only nonland permanent type outside the printed four. Real but narrow divergence.
- **Patriar's Seal** (`patriars_seal.rs`) — Note claims 'TargetFilter lacks has_supertype field for legendary filtering'. FALSE: TargetFilter.legendary: bool exists (card_definition.rs:2858, doc-commented 'Must have the Legendary supertype') and IS enforced in matches_filter at effects/mod.rs:8045 ('if filter.legendary'); it is already used in shipped defs (eiganjo_seat_of_the_empire.rs:53, otawara_soaring_city.rs:54, boseiju_who_endures.rs:97). Oracle verified: '{1}, {T}: Untap target legendary creature you control.' Authorable now as Activated{ cost: Cost::Sequence([Mana{generic:1}, Cost::Tap]), effect: Effect::UntapPermanent{DeclaredTarget{0}}, targets: vec![TargetCreatureWithFilter(TargetFilter{legendary: true, controller: TargetController::You, ..})] }. Not an upgrade — the ability is absent from the def today.
- **Perilous Forays** (`perilous_forays.rs`) — Note says the search filter must require the Basic supertype. False: TargetFilter.has_subtypes (card_definition.rs:2837, OR semantics) is honored by matches_filter (effects/mod.rs:7999-8006), and Effect::SearchLibrary matches candidates via matches_filter (effects/mod.rs:2806). 'A land card with a basic land type' is expressible today as has_card_type: Land + has_subtypes: [Plains, Island, Swamp, Mountain, Forest]. The def still uses basic_land_filter() (helpers.rs:88, basic: true), so this is real authoring work, not a marker flip.
- **Ruthless Winnower** (`ruthless_winnower.rs`) — BOTH stated blockers are stale. (1) 'SacrificePermanents has no subtype exclusion': SacrificePermanents.filter is Option<TargetFilter> (card_definition.rs:1747), TargetFilter has exclude_subtypes: Vec<SubType> (2887), and matches_filter enforces it (effects/mod.rs:8059-8065); SacrificePermanents routes through sacrifice_permanents_for_player with that filter (effects/mod.rs:3260). (2) 'AtBeginningOfYourUpkeep only fires for controller': TriggerCondition::AtBeginningOfEachUpkeep exists AND is dispatched (turn_actions.rs:294, hash.rs:5210). Def currently fires on controller's upkeep only and sacrifices unfiltered — live wrong game state on both axes.
- **Shifting Woodland** (`shifting_woodland.rs`) — Note says TargetFilter lacks a 'permanent card' constraint. There is no dedicated field, but TargetFilter.has_card_types (Vec<CardType>, OR semantics, card_definition.rs:2854) IS enforced by matches_filter (effects/mod.rs:8035-8042) and expresses 'permanent card' as [Artifact, Creature, Enchantment, Land, Planeswalker, Battle]. The def currently passes TargetFilter::default() to TargetCardInYourGraveyard (line 58-60), so instants/sorceries are still legal targets — real rewiring work. ETB-tapped-unless-Forest and the {T}:Add{G} + Delirium activation condition are all correctly wired.
- **Shizo, Death's Storehouse** (`shizo_deaths_storehouse.rs`) — Note says 'add legendary filter when available' and the inline comment claims TargetFilter lacks has_supertype. STALE: TargetFilter.legendary exists (card_definition.rs:2858) and matches_filter enforces it against chars.supertypes (effects/mod.rs:8045-8051). The def still uses bare TargetRequirement::TargetCreature (line 43), so it remains over-permissive; swapping to TargetCreatureWithFilter(TargetFilter{legendary:true, ..}) would complete the card (both oracle clauses — {T}:Add{B} at line 13-24 and the fear grant at line 28-47 — are otherwise present).
- **Unearth** (`unearth.rs`) — Note claims 'TargetCardInYourGraveyard lacks mana value filter (<=3)'. FALSE, and I traced the whole chain: `TargetFilter.max_cmc: Option<u32>` exists (card_definition.rs:2843-2846, doc cites 'search for a card with mana value N or less'); `matches_filter` implements it via ManaCost::mana_value(), CR 202.3 (effects/mod.rs:8013-8020); and the TargetCardInYourGraveyard arm of validate_object_satisfies_requirement calls `crate::effects::matches_filter(&chars, filter)` (casting.rs:6312). Urza's Saga already uses max_cmc: Some(1) in this same batch. NOT an upgrade: the def as written (line 24-27) passes only `has_card_type: Some(CardType::Creature)` with no max_cmc, so it currently permits reanimating a creature of ANY mana value — a live illegal-target bug, not a downgrade. One-line rewiring: add `max_cmc: Some(3)`. Oracle confirmed via MCP ('mana value 3 or less'); Cycling {2} is already correctly wired (Keyword + AbilityDefinition::Cycling).
- **Wasteland** (`wasteland.rs`) — Note says 'TargetFilter lacks non_basic exclusion field'. STALE — TargetFilter HAS `pub nonbasic: bool` (card_definition.rs:2829), documented with the exact citation this card needs: 'Distinct from `basic: false` which is the default no-restriction state. CR 205.4a — nonbasic land requires absence of the Basic supertype.' The def still uses TargetRequirement::TargetLand (line 35), which lets Wasteland destroy basic lands — currently a wrong-state card. Rewiring to TargetPermanentWithFilter{ has_card_type: Land, nonbasic: true } should make it Complete, but that is authoring work. Until rewired, arguably known_wrong rather than partial (it does something illegal, not something incomplete).

## No blocker left — fully authorable today  (14)

- **Anafenza, Unyielding Lineage** (`anafenza_unyielding_lineage.rs`) — Note's first claim ('WhenDies with nontoken + controller filter not in DSL') is FALSE. Chain traced: TriggeredAbilityDef.death_filter carries controller_you (abilities.rs:4388), exclude_self (4396) and nontoken_only (4400), all dispatched off TriggerEvent::AnyCreatureDies in check_triggers. Oracle 'Whenever another nontoken creature you control dies' is fully expressible today. Residual: Endure 2 (choose: two +1/+1 counters on Anafenza OR create a 2/2 white Spirit token) — Effect::Choose exists (card_definition.rs:1696) so this is likely authorable, but no card def currently uses Choose for a non-mana modal at resolution, so verify before authoring. Def has abilities: vec![] for the trigger — not close to Complete.
- **Boseiju, Who Endures** (`boseiju_who_endures.rs`) — The note is FALSE: `TargetFilter.has_card_types: Vec<CardType>` with OR semantics exists (card_definition.rs:2854, doc-comment cites exactly this 'artifact or enchantment' use case) and `nonbasic: bool` exists (card_definition.rs:2829, cites CR 205.4a); both are live in matches_filter (effects/mod.rs:8035 and :7986). 'Artifact, enchantment, or nonbasic land an opponent controls' is expressible exactly as TargetFilter { has_card_types: vec![Artifact, Enchantment, Land], nonbasic: true, controller: Opponent } — nonbasic is a no-op on artifacts/enchantments (they never carry the Basic supertype) and correctly excludes basic lands. The rest of the card is already complete and correct: Channel cost, DestroyPermanent, ControllerOf-scoped search for a land with a basic land type (with the right CR 305.8 note), Shuffle, and the per-legendary-creature cost reduction. Not upgrading to Complete because the target filter as written is currently OVERBROAD (any opponent permanent) — that is a live legal-but-wrong bug, and fixing it is a one-line rewiring, not a marker flip.
- **Brokers Ascendancy** (`brokers_ascendancy.rs`) — The note is FALSE on its central claim ('TriggerCondition::AtBeginningOfYourEndStep not available for CardDef triggered abilities'): that variant is used by SEVEN other card defs today — atraxa_praetors_voice.rs, jadar_ghoulcaller_of_nephalia.rs, searslicer_goblin.rs, thaumatic_compass.rs, throne_of_the_god_pharaoh.rs, contaminant_grafter.rs, case_of_the_locked_hothouse.rs — and is dispatched from turn_actions.rs. The mass-counter clause is also expressible: felidar_retreat.rs:67-71 is the exact pattern (Effect::ForEach { over: ForEachTarget::EachCreatureYouControl, effect: AddCounter{PlusOnePlusOne} }), and the planeswalker half maps to ForEachTarget::EachPermanentMatching + AddCounter{CounterType::Loyalty}. No blocker remains; this is pure authoring work. Highest-value stale marker in the batch.
- **Cavern-Hoard Dragon** (`cavern_hoard_dragon.rs`) — The note's stated blocker — 'EffectAmount/ForEach variant for artifacts controlled by the damaged player does not exist' — is FALSE on the full chain. EffectAmount::PermanentCount { filter, controller: PlayerTarget } is declared at card_definition.rs:2502 and evaluated at effects/mod.rs:6749-6771; `controller` is resolved through resolve_player_target_list, whose PlayerTarget::DamagedPlayer arm reads ctx.damaged_player (effects/mod.rs:6402-6417). TokenSpec.count is an EffectAmount (see chandra_flamecaller.rs:33), and TriggerCondition::WhenDealsCombatDamageToPlayer exists. So `CreateToken { spec: treasure, count: PermanentCount { filter: artifact, controller: DamagedPlayer } }` on a combat-damage trigger is fully expressible today. NOT an upgrade — the trigger is simply absent from `abilities` (only the three keywords are present).
- **Dockside Extortionist** (`dockside_extortionist.rs`) — Note claims 'count-based EffectAmount not in DSL' — FALSE. EffectAmount::PermanentCount{filter, controller} exists (card_definition.rs:2502-2505) and dispatches at effects/mod.rs:6749-6768. TokenSpec.count is an EffectAmount, not a u32 (card_definition.rs:3687, 'CR 608.2h: resolved dynamically at execution time via resolve_amount'). 'artifacts and enchantments' is TargetFilter.has_card_types (OR-semantics, :2854, doc cites 'artifact or enchantment (Enlightened Tutor)'); 'your opponents control' is PlayerTarget::EachOpponent. So X is fully expressible. NOT upgradeable as written: dockside_extortionist.rs:21 hardcodes treasure_token_spec(3), which is active wrong game state — arguably known_wrong until rewired.
- **Dwynen, Gilt-Leaf Daen** (`dwynen_gilt_leaf_daen.rs`) — Oracle (MCP) matches def; Reach and the Elf anthem (:23-31) are correct. The note asks for 'EffectAmount::AttackingCreatureCountWithSubtype' — the equivalent SHIPPED as AttackingCreatureCount{controller, filter: Option<TargetFilter>} in PB-AC3 (card_definition.rs:2682-2714), dispatching at effects/mod.rs:7010-7032. Its own doc at :2688-2689 names this exact pattern: "controller = PlayerTarget::Controller + filter = Some(<subtype>) gives 'attacking [Dragons] you control'". TriggerCondition::WhenAttacks exists. Dwynen counts herself (she is an attacking Elf), so exclude_self stays false. Fully expressible; def just has not been rewired.
- **Elderfang Venom** (`elderfang_venom.rs`) — Oracle (MCP) matches def; the 'Attacking Elves you control have deathtouch' static (:15-25) is correct. The note's 'Blocked on PB-26: WheneverCreatureDies needs subtype filter (Elf only)' is STALE — the filter shipped in PB-N: TriggerCondition::WheneverCreatureDies now carries filter: Option<TargetFilter> (card_definition.rs:3058-3063), whose doc names the exact pattern ("Used for 'Whenever a Vampire you control dies' (Teysa Orzhov Scion)") and confirms it is evaluated against PRE-DEATH characteristics preserved on the graveyard object per CR 603.10a. Dispatch verified at abilities.rs:4400-4416. Sibling def elenda_the_dusk_rose.rs:26 already constructs the 4-field form. Effects are trivial (LoseLife to EachOpponent + GainLife to Controller). Fully expressible.
- **Gnarlroot Trapper** (`gnarlroot_trapper.rs`) — The note's stated blocker ('EffectTarget has no AttackingCreatureWithFilter variant') is FALSE — the ability TARGETS, so it needs a TargetRequirement, not an EffectTarget. Full chain traced: TargetRequirement::TargetCreatureWithFilter exists; TargetFilter carries is_attacking (:2906, doc says it is enforced at casting::validate_object_satisfies_requirement for TargetCreatureWithFilter AND at the abilities.rs auto-target picker), has_subtype (:2833), and controller: TargetController::You (:2831); AbilityDefinition::Activated carries targets (PB-5); Effect::ApplyContinuousEffect + LayerModification::AddKeyword(Deathtouch) + EffectFilter::DeclaredTarget + EffectDuration::UntilEndOfTurn completes it — the exact shape the sibling ability in goro_goro_disciple_of_ryusei.rs uses. Real authoring work (the ability is absent), so NOT an upgrade — this is a worklist item that should un-mark the card once wired.
- **Hallowed Spiritkeeper** (`hallowed_spiritkeeper.rs`) — Note claims 'count-based token amount (EffectAmount::CreatureCardsInYourGraveyard) not in DSL' — stale. `TokenSpec.count` is an `EffectAmount` resolved dynamically at execution time (card_definition.rs:3684-3687: 'CR 608.2h: resolved dynamically at execution time via resolve_amount'), and `EffectAmount::CardCount { zone: ZoneTarget::Graveyard, player: PlayerTarget::Controller, filter: Some(TargetFilter{ has_card_type: Some(CardType::Creature), .. }) }` exists (card_definition.rs:2495-2498). A dies-trigger needs `trigger_zone` set to Graveyard (CR 603.10a) as other dies-triggers do. NOT an upgrade: the def's abilities are only `Keyword(Vigilance)` (line 17) — the whole dies-trigger is absent and must be authored.
- **Myrel, Shield of Argive** (`myrel_shield_of_argive.rs`) — BOTH stated blockers are stale, and the def is actively wrong. (1) 'Opponents can't cast/activate during your turn stax restriction not in DSL' — GameRestriction::OpponentsCantCastOrActivateDuringYourTurn exists and its doc comment literally names this card: 'Your opponents can't cast spells or activate abilities of artifacts, creatures, or enchantments [during your turn].' (Grand Abolisher, Myrel — superset of OpponentsCantCastDuringYourTurn)' (card-types/src/state/stubs.rs:566-569). It is enforced at casting.rs:6571, mana.rs:95, and simulator/legal_actions.rs:939+993. The variant was built for Myrel and the def never picked it up. (2) 'X = number of Soldiers — count-based EffectAmount not in DSL' — EffectAmount::PermanentCount { filter, controller } exists and is used with a subtype filter across the corpus (e.g. multani_yavimayas_avatar.rs:36). The def instead ships count: EffectAmount::Fixed(2) as an 'approximation' — Myrel makes exactly 2 tokens in every game regardless of board, which is legal-but-wrong. Oracle verified via lookup: matches def's oracle_text exactly. Rewire = AbilityDefinition::StaticRestriction with the GameRestriction + swap Fixed(2) for PermanentCount{has_subtype: Soldier, controller: You}. Until rewired the marker should be known_wrong, not partial.
- **Nether Traitor** (`nether_traitor.rs`) — BOTH stated blockers are stale. (1) 'triggered ability from graveyard zone not expressible' — AbilityDefinition::Triggered carries a `trigger_zone` field (present as trigger_zone: None throughout this corpus) and TriggerZone::Graveyard is explicitly handled: replay_harness.rs:2732 skips those abilities precisely because 'they are handled by collect_graveyard_carddef_triggers at dispatch time... these triggers fire only from the graveyard.' (2) "mana-payment conditional ('you may pay {B}') not supported" — Effect::MayPayThenEffect exists and, unlike MayPayOrElse, is CORRECTLY implemented (effects/mod.rs:3203-3219: try_pay_optional_cost, `then` runs only if the cost was actually paid, CR 118.12 pay-when-able). (3) The trigger itself: TriggerCondition::WheneverCreatureDies { controller: Option<TargetController>, exclude_self: bool, nontoken_only, filter } (card_definition.rs:3048-3056) covers 'whenever ANOTHER creature is put into YOUR graveyard from the battlefield' via controller: Some(You) + exclude_self: true. Rewire = WheneverCreatureDies{controller: Some(You), exclude_self: true} + trigger_zone: Some(TriggerZone::Graveyard) + Effect::MayPayThenEffect{cost: Mana({B}), then: MoveZone(Source -> Battlefield)}. Haste + Shadow already implemented. No engine work needed.
- **Radha, Heart of Keld** (`radha_heart_of_keld.rs`) — The note's stated blocker is false: it claims 'ModifyBoth only supports fixed i32' and that a ModifyBothDynamic variant is needed — but LayerModification::ModifyBothDynamic EXISTS (confirmed in the capability inventory and documented in card_definition.rs:1002 and :1015, with ModifyPowerDynamic/ModifyToughnessDynamic alongside it), and EffectAmount::PermanentCount supplies 'number of lands you control'. The {4}{R}{G} activated ability is simply not written yet — real authoring work, not a DSL gap. Oracle (MCP) confirms three clauses; the conditional first strike (Condition::IsYourTurn) and StaticPlayFromTop are already correctly implemented, so this def is one ability away from Complete.
- **Turn // Burn** (`turn.rs`) — Note 'no abilities implemented' is vague and the card is now authorable. Fuse is fully wired: AbilityDefinition::Fuse (card_definition.rs:782), cast path casting.rs:1291/7723-7755, resolution.rs:181, registry ability_definition_registry.rs:278, tests/mechanics_e_l/fuse.rs, and reference def connive.rs:50 shows the split-card encoding. Turn's effect needs LayerModification::RemoveAllAbilities + SetColors + SetCreatureTypes + SetPowerToughness — all present. Burn ('deals 2 damage to any target') is Effect::DealDamage. NOTE for the rewiring worker: the def's header comment is truncated mid-sentence, mana_cost carries only the Turn half ({2}{U}) and no Fuse/Burn data, so this is real authoring work, not a marker flip.
- **Wolverine Riders** (`wolverine_riders.rs`) — Note claims 'EffectAmount lacks toughness-of-entering-creature variant'. FALSE on the full chain: (1) TriggerCondition::WheneverCreatureEntersBattlefield { filter, exclude_self } exists (card_definition.rs:3079, exclude_self added by PB-XS-E); (2) builder arm replay_harness.rs:2650 maps filter -> etb_filter + triggering_creature_filter; (3) dispatch abilities.rs:6202-6300 honors exclude_self (:6206), controller_you (:6222), subtype via triggering_creature_filter (:6256, PB-AC0), and sets entering_object_id = the ENTERING creature (:6298); (4) abilities.rs:7758 copies it to stack_obj.triggering_creature_id, resolution.rs:2199 into ctx, effects/mod.rs:6260 resolves EffectTarget::TriggeringCreature, effects/mod.rs:6665 reads layer-resolved toughness. So Effect::GainLife { player: Controller, amount: ToughnessOf(TriggeringCreature) } under WheneverCreatureEntersBattlefield { filter: has_subtype Elf + controller You, exclude_self: true } is fully expressible today. Upkeep-token clause is already correct (AtBeginningOfEachUpkeep + CreateToken 1/1 green Elf Warrior). NOT upgrade-to-complete: the def simply does not contain the ability yet (TODO at line 44).

## Effect variants that shipped but are unwired (WheelHand, RevealAndRoute, Proliferate, DiscardCards, MoveZone, SacrificePermanents)  (13)

- **Chandra, Flamecaller** (`chandra_flamecaller.rs`) — The note says 'EffectAmount::HandSize not in DSL'. It IS in the DSL — card_definition.rs:2727, discriminant 21, added by PB-AC3. More directly, Effect::WheelHand { player, disposal, draw } (card_definition.rs:2366-2374) is purpose-built for this shape and its doc even explains why the naive DiscardCards{HandSize}+DrawCards{HandSize} draws 0: the count must be snapshotted before disposal. The 0 ability currently resolves as Effect::Nothing (chandra_flamecaller.rs:46). Rewiring: WheelHand { player: Controller, disposal: WheelDisposal::Discard, draw: WheelDraw::ThatMany } followed by DrawCards(Fixed(1)) for the 'plus one' — WheelDraw has only ThatMany and Fixed (2401-2408), no ThatMany+N, so the author must confirm the split-draw is acceptable (CR 121.3 makes drawing N+1 a sequence of individual draws anyway). NOT an upgrade: needs authoring + that judgement call.
- **Encroaching Dragonstorm** (`encroaching_dragonstorm.rs`) — Note claims 'Effect::ReturnToHand does not exist as a DSL variant'. Literally true — and irrelevant: Effect::MoveZone { target: EffectTarget::Source, to: ZoneTarget::Hand, controller_override: None } (card_definition.rs:1594-1603) is exactly this, and the doc explicitly states that with controller_override: None 'controller defaults to owner as per normal zone-change rules' — matching 'return this enchantment to its owner's hand'. The trigger half the def's own comment sketches (TriggerCondition::WheneverCreatureEntersBattlefield { filter: Some(TargetFilter { has_subtype: Dragon, controller: You }) }) also exists. SEPARATE issue found in the implemented clause: oracle says 'search your library for UP TO TWO basic land cards' as ONE search; the def (lines 24-39) issues TWO independent SearchLibrary calls, which is not the same event and cannot express 'up to' (declining, or finding only one). Author should confirm SearchLibrary's count semantics before claiming Complete.
- **Entish Restoration** (`entish_restoration.rs`) — Both cited blockers are STALE. (1) 'SacrificePermanents has no type filter' — it has had filter: Option<TargetFilter> since PB-SFT (:1741-1747, CR 701.21a/109.1), and SacrificeFilter::Land also exists; sacrificing specifically a land is expressible. (2) 'Condition::YouControlCreatureWithPowerAtLeast(N) does not exist' — Condition::YouControlNOrMoreWithFilter covers it, since TargetFilter.min_power: Option<i32> exists (:2809). Effect::Conditional + SearchLibrary with basic/max-count filters (PB-17) complete the card.
- **Frantic Search** (`frantic_search.rs`) — Note claims 'Then discard two cards + untap up to three lands not expressible'. BOTH are stale. (1) Effect::DiscardCards { player: PlayerTarget, count: EffectAmount } exists at card_definition.rs:1361 — 'then discard two cards' is Effect::Sequence([DrawCards Fixed(2), DiscardCards { Controller, Fixed(2) }]). (2) 'Untap up to three lands' is the documented UpToN pattern: TargetRequirement::UpToN { count: 3, inner: Box::new(TargetLand) } (:2798) with Effect::UntapPermanent { target: DeclaredTarget{0/1/2} } (:1449) — card_definition.rs:1405 spells out exactly this idiom ('For Sorin -6 (UpToN 3), use [DeclaredTarget{0}, DeclaredTarget{1}, DeclaredTarget{2}]'). Real authoring work, not a free upgrade — a rewired def needs its target slots validated. Meanwhile the def is wrong game state in the card's favor: it draws 2 and never discards, deleting the card's entire drawback; the marker would be better as known_wrong until rewired.
- **Maestros Theater** (`maestros_theater.rs`) — The def SKIPS the sacrifice entirely (explicit TODO at line 24-26), so the land permanently stays on the battlefield AND ramps a basic — strictly better than the real card, i.e. wrong game state, not merely incomplete. The note's stated blocker is partly stale: Effect::SacrificePermanents exists (inventory) and could sacrifice the source inside the existing Sequence before the search, which is behaviorally equivalent here because the reflexive 'when you do' clause is unconditional (the sacrifice cannot fail). Only the strict reflexive-trigger *timing* (search as a separate trigger on the stack) is not modeled.
- **Ox of Agonas** (`ox_of_agonas.rs`) — Two findings. (1) The note's blocker is stale: EffectAmount::HandSize { player } exists (PB-AC3) and resolves at effects/mod.rs:7073-7083 (delegates to CardCount{zone:Hand}), so 'discard your hand' is exactly DiscardCards{count: EffectAmount::HandSize}. Def hardcodes Fixed(7) (ox_of_agonas.rs:35), which under-discards a hand of 8+ and is not 'up to 7' as the header claims — DiscardCards has no up-to semantics. (2) UNDOCUMENTED CLAUSE: current oracle reads 'This creature escapes with a +1/+1 counter on it.' The def's AltCastDetails::Escape { exile_count: 8 } (ox_of_agonas.rs:24) carries no counter and the def's oracle_text field (:14) predates that clause entirely. This clause is silently dropped and the marker note does not mention it at all.
- **Roalesk, Apex Hybrid** (`roalesk_apex_hybrid.rs`) — Note claims 'WhenThisDies trigger + Effect::Proliferate' are a DSL gap. Both exist and are wired: TriggerCondition::WhenDies (card_definition.rs; wired replay_harness.rs:2289, hash.rs:5162) and Effect::Proliferate (dispatched effects/mod.rs:3467). The clause is a plain Triggered{WhenDies, Effect::Sequence([Proliferate, Proliferate])}. Flying/trample/ETB counters are already correct (lines 21-43, exclude_self: true for 'another target'). Not upgrading — the dies trigger is genuinely absent from the def body, so this is authoring work, not lag.
- **Roiling Dragonstorm** (`roiling_dragonstorm.rs`) — Note says 'forced discard not easily expressible'. Effect::DiscardCards exists (inventory) — 'then discard a card' is Effect::DiscardCards{player: Controller, count: Fixed(1)} inside a Sequence after the draw. The second TODO is likewise stale: TriggerCondition::WheneverPermanentEntersBattlefield takes filter: Option<TargetFilter> + exclude_self (card_definition.rs:3097-3103) and TargetFilter has has_subtype (2833), so 'when a Dragon you control enters' is expressible; Effect::MoveZone handles the self-bounce. Def currently only draws 2 and silently drops the discard — live wrong game state.
- **Sea Gate Restoration // Sea Gate, Reborn** (`sea_gate_restoration.rs`) — The vague note ('no abilities implemented') hides a genuinely stale situation: PB-AC9 shipped exactly this card's front face. 'You have no maximum hand size for the rest of the game' -> Effect::SetNoMaximumHandSize { player } exists and executes at effects/mod.rs:650 (hashed at hash.rs:6393-6394, CR 402.2 permanent designation per hash.rs:312; the rest-of-game flag issue is handled, cf. turn_actions.rs:1240). 'Draw cards equal to the number of cards in your hand plus one' -> EffectAmount::HandSize { player } exists and resolves at effects/mod.rs:7077 and layers.rs:1902. So the front face is authorable now. NOT upgrade-to-complete: abilities is still vec![] (nothing is wired), and back_face is None via ..Default::default() (sea_gate_restoration.rs:13) so the Sea Gate, Reborn land face (MCP type line 'Sorcery // Land') is absent. Real authoring work.
- **Sensei's Divining Top** (`senseis_divining_top.rs`) — Partially stale. FALSE claim: 'the {T} ability requires Effect::PutSelfOnTopOfLibrary which does not exist' — Effect::MoveZone{target: EffectTarget::Source, to: ZoneTarget::Library{owner: PlayerTarget::OwnerOf(..), position: LibraryPosition::Top}, controller_override: None} (card_definition.rs:1594) expresses 'put this artifact on top of its owner's library' exactly, so '{T}: Draw a card, then put this artifact on top of its owner's library' is authorable now as a Sequence[DrawCards, MoveZone] on Cost::Tap. STILL REAL: the {1} ability — no effect looks at the top N and returns them in a chosen order; Effect::Scry is the closest and its documented M9.4 fallback puts cards on the BOTTOM in ObjectId order (card_definition.rs:1605-1614), which is a different card. So the def can gain its {T} ability but stays partial.
- **Sylvan Messenger** (`sylvan_messenger.rs`) — The note's 'No RevealTopN + subtype filter + split-destination effect exists in the DSL' is exactly false — Effect::RevealAndRoute { player, count, filter, matched_dest, unmatched_dest } exists and does precisely this: it takes the top N of the library, partitions by filter, and routes matched/unmatched to separate zones (effects/mod.rs:4584-4614). Live usage: teferi_temporal_archmage.rs:22-31. Sylvan Messenger = count 4, filter subtype Elf, matched_dest Hand, unmatched_dest Library{Bottom}. NOT an upgrade: the def currently has NO ETB ability at all (abilities is Trample only), so this is real authoring work. Minor residual: 'in any order' for the bottomed cards is deterministic here.
- **The Locust God** (`the_locust_god.rs`) — Note claims "'then discard' — forced discard after draw not expressible". This is false: Effect::DiscardCards { player: PlayerTarget, count: EffectAmount } exists at card_definition.rs:1361 and Effect::Sequence is used elsewhere in this very file (line 21 pattern in other defs). The {2}{U}{R} activated ability should be Sequence([DrawCards{Controller,1}, DiscardCards{Controller,1}]). Real authoring work, not a gap. Other clauses (Flying, draw-trigger token, dies-return) are already implemented.
- **Wrenn and Seven** (`wrenn_and_seven.rs`) — Both of the note's stated blockers shipped. (1) '+1: Reveal top four, put all land cards into your hand and the rest into your graveyard' is EXACTLY Effect::RevealAndRoute { player: Controller, count: Fixed(4), filter: TargetFilter { has_card_type: Some(CardType::Land), .. }, matched_dest: Hand, unmatched_dest: Graveyard } (PB-22 S3; live usage in yuriko_the_tigers_shadow.rs:45). The note calls this 'a complex effect. Known DSL gap' — stale. (2) The -8's 'You have no maximum hand size' is Effect::SetNoMaximumHandSize { player } (card_definition.rs:2380, PB-AC9, sets PlayerState.no_max_hand_size_permanent for the rest of the game), which makes the def's own long TODO at lines 83-91 ('INCORRECT APPROXIMATION ... grants NoMaxHandSize to permanents you control as a proxy, which fails when the player controls no permanents ... Correct fix: add player.no_max_hand_size flag to PlayerState') stale — that exact fix shipped. NOT upgrade-to-complete: the def currently does Scry 4 in place of the +1 (a materially different, wrong game state — cards go to the bottom, not to hand/graveyard), the 0 ability is Effect::Sequence(vec![]) (activatable no-op), the -3 token is a plain 0/0 with no CDA (dies to SBA immediately), and '-8: Return all permanent cards from your graveyard to your hand' is unimplemented. Residual real gaps: 'put any number of land cards from hand onto battlefield' (interactive multi-select) and the token CDA. ENGINE-FINDING: none. NOTE on marker kind — a Scry-4-for-a-reveal-and-route substitution and a 0/0 CDA-less token are not omissions, they are wrong behavior; this def arguably belongs at known_wrong rather than partial.

## Condition / intervening-if variants  (7)

- **Arch of Orazca** (`arch_of_orazca.rs`) — The note's stated blocker — 'Proper activation restriction requires legal_actions filter (PB-18 stax framework)' (def comment lines 28-30) — is stale. Full chain verified: AbilityDefinition::Activated carries `activation_condition: Option<Condition>`; it is enforced at rules/abilities.rs:254-261 under the comment 'CR 602.5b: "Activate only if [condition]" — check activation condition'; Condition::HasCitysBlessing exists and the def already uses it (line 37, inside a Conditional effect instead); and 15+ defs use the pattern live (temple_of_the_false_god.rs:24, idol_of_oblivion.rs:24, bloodsoaked_champion.rs:31, glistening_sphere.rs:48, shifting_woodland.rs:61, ...). Current def lets a player pay {5}+{T} without the blessing and draw nothing — real wrong game state. Fix is mechanical: set `activation_condition: Some(Condition::HasCitysBlessing)` on the third ability and unwrap the Conditional to a bare DrawCards.
- **Garruk's Uprising** (`garruks_uprising.rs`) — Blocker is real at the exact-semantics level: the intervening_if field is typed InterveningIf, which has only 2 variants (ControllerLifeAtLeast, SourceHadNoCounterOfType) — neither expresses 'you control a creature with power 4 or greater', so CR 603.4's check-on-trigger-AND-on-resolution cannot be had. However the def as written draws UNCONDITIONALLY (line 20-23, intervening_if: None), which is wrong game state, and a strictly closer approximation is available now: Condition::YouControlNOrMoreWithFilter exists and Effect::Conditional would gate the draw at resolution. Rewiring to Conditional removes the free-draw bug but still misses the trigger-time check, so the marker stays.
- **Inventors' Fair** (`inventors_fair.rs`) — The marker note's headline (upkeep intervening-if 'control 3+ artifacts') is STILL VALID: the InterveningIf enum has only 2 variants (ControllerLifeAtLeast, SourceHadNoCounterOfType) — no permanent-count threshold. But the file's second TODO ('activation condition ... PB-18') is stale: activation_condition: Some(Condition::YouControlNOrMoreWithFilter { .. }) shipped in PB-22 S1 and Condition::YouControlNOrMoreWithFilter exists. ENGINE-FINDING: the search ability sets activation_condition: None (line 62), so the engine currently lets a player activate it with zero artifacts — an illegal activation, i.e. live wrong game state that is fixable today.
- **Ophiomancer** (`ophiomancer.rs`) — Note claims "Condition lacks 'you control no permanents with subtype X' variant". FALSE on the full chain: (1) AbilityDefinition::Triggered.intervening_if is Option<Condition> (card_definition.rs:319), NOT the 2-variant InterveningIf enum (that one is the runtime type at game_object.rs:634); (2) Condition::Not (effects/mod.rs:8176) and Condition::ControlCreatureWithSubtype (effects/mod.rs:8312) are both evaluated by check_condition; (3) resolution.rs:2073-2083 evaluates a card-def trigger's intervening_if through check_condition per CR 603.4. So intervening_if: Some(Condition::Not(Box::new(Condition::ControlCreatureWithSubtype(SubType("Snake"))))) is expressible and dispatched. MARKER KIND ALSO WRONG: as written the def has intervening_if: None and creates a Snake token at EVERY upkeep unconditionally — wrong game state (unbounded token generation), i.e. known_wrong, not partial. Author should also confirm the trigger-time half of CR 603.4 (resolution-time re-check is proven; trigger-time gating was not verified here).
- **Prossh, Skyraider of Kher** (`prossh_skyraider_of_kher.rs`) — The def's second TODO is flatly stale: 'Sacrifice another creature: Prossh gets +1/+0 until end of turn' IS expressible today — Cost::Sacrifice(TargetFilter) (PB-4) plus Effect::ApplyContinuousEffect with LayerModification::ModifyPower/EffectDuration::UntilEndOfTurn, exactly the shape the engine itself builds at builder.rs:530-535. That ability should be authored. The cast trigger stays blocked: TriggerCondition::WhenYouCastThisSpell exists (card_definition.rs:3317) but no EffectAmount expresses 'the amount of mana spent to cast it' (XValue is the X in the cost, which Prossh does not have). Flying is wired.
- **Replicating Ring** (`replicating_ring.rs`) — Note claims 'token creation of named artifact tokens with mana abilities' is unimplementable. TokenSpec has `mana_abilities: Vec<ManaAbility>` (card_definition.rs:3701, explicitly 'Mana abilities to populate on each created token (CR 605)') plus name/supertypes/card_types fields, so the Replicated Ring token is expressible. Upkeep trigger (AtBeginningOfYourUpkeep), CounterType::Custom for night counters, Condition::SourceHasCounters and Effect::RemoveCounter all exist. Medium confidence: did not verify SourceHasCounters supports an 'eight or more' threshold or that RemoveCounter can remove all.
- **Sting, the Glinting Dagger** (`sting_the_glinting_dagger.rs`) — Note's premise 'EffectFilter::EquippedCreature which may not exist' is stale: EffectFilter::AttachedCreature exists (rules/layers.rs:633) and is the standard equipment pattern used by sword_of_vengeance.rs:23,42, lightning_greaves.rs:24 (haste grant), rancor.rs:28,38. So '+1/+1 and has haste' is expressible. 'At the beginning of each combat, untap equipped creature' is also expressible: TriggerCondition::AtBeginningOfCombat + Effect::UntapPermanent + EffectTarget::EquippedCreature (PB-33, hash.rs:4945). Equip {2} is supported (tests/mechanics_e_l/equip.rs). Residual real blocker: 'first strike as long as it's blocking or blocked by a Goblin or Orc' — ContinuousEffectDef.condition takes a Condition, and no Condition variant expresses a combat relationship to a creature of a given subtype.

## Attack / cast triggers that shipped (WhenAttacks, WheneverYouAttack, WheneverYouCastSpell, spell_subtype_filter)  (6)

- **Arixmethes, Slumbering Isle** (`arixmethes_slumbering_isle.rs`) — BOTH stated blockers are false. (1) 'no WheneverYouCastASpell trigger condition' — TriggerCondition::WheneverYouCastSpell exists, has a builder arm at replay_harness.rs:2578, hashes at hash.rs:5213, and is already used by murmuring_mystic.rs:19, alela_cunning_conqueror.rs:29, bontus_monument.rs:27, leaf_crowned_visionary.rs:32. (2) 'ETB-with-counters replacement not in DSL' — ReplacementModification::EntersWithCounters { counter, count } dispatches at replacement.rs:1607 (PB-EWC, hash.rs:111). Both unauthored clauses are now writable. Not upgradable: the def currently has neither clause (5 slumber counters absent means the land-not-creature statics never engage — the card is a live 12/12 for {2}{G}{U}, wrong state). Residual to verify when rewiring: 'you MAY remove a slumber counter' optionality.
- **Edgar Markov** (`edgar_markov.rs`) — First strike and haste implemented (:18-19). Note has two false clauses and one real one. FALSE: 'WheneverYouCastSpell with subtype filter (Vampire only) ... not expressible' — WheneverYouCastSpell.spell_subtype_filter shipped (card_definition.rs:3131-3135, doc cites 'Whenever you cast an Elf spell (Leaf-Crowned Visionary)'). FALSE: 'EffectTarget::AllCreaturesYouControlWithSubtype not in DSL' — EffectTarget::AllPermanentsMatching(Box<TargetFilter>) exists (:2435), so 'put a +1/+1 counter on each Vampire you control' IS authorable now. REAL: Eminence must trigger from the command zone, and TriggerZone has exactly one variant, Graveyard (card_definition.rs:4063-4066); Triggered.trigger_zone (:336) cannot name the command zone. So the attack trigger should be wired now; the card stays partial for Eminence only.
- **Karlach, Fury of Avernus** (`karlach_fury_of_avernus.rs`) — Note claims a 'DSL gap: no WhenYouDeclareAttackers condition' — FALSE, full chain traced. (1) TriggerCondition::WheneverYouAttack exists (hashed at hash.rs:5275, 'CR 508.1 Whenever you attack'). (2) Builder arm converts it: replay_harness.rs:3250 -> TriggerEvent::ControllerAttacks at :3259. (3) Dispatch fires it: abilities.rs:3879-3900 ('fires once when controller declares one or more attackers... per player, not per creature'). Def still uses TriggerCondition::WhenAttacks (karlach_fury_of_avernus.rs:33), so Karlach must attack personally — wrong game state. Rewiring is real work; not done here.
- **Lys Alana Huntmaster** (`lys_alana_huntmaster.rs`) — Note claims "'Elf spell' needs subtype filter on spells (not yet in DSL)" — STALE. spell_subtype_filter: Option<Vec<SubType>> exists (card_definition.rs:3136), is converted in replay_harness.rs:2581-2610 (-> has_subtypes on the runtime filter), is hashed (hash.rs:5218-5225) and has positive/negative/None-regression tests (pb_ac7_type_change_ability_removal.rs:842/903/931 — the test file literally uses SubType("Elf")). The def passes spell_subtype_filter: None (line 26) and filters only on CardType::Creature, so it over-triggers on EVERY creature spell. Rewiring to Some(vec![Elf]) is a real behavior fix. Card still NOT Complete: the 'you may' optionality has no correct expression (Effect::Choose always executes choices.first(), effects/mod.rs:3190).
- **Niv-Mizzet, Parun** (`niv_mizzet_parun.rs`) — The note claims 'no such variant exists ... WheneverYouCastSpell only covers the controller's own casts'. Both TriggerCondition::WheneverOpponentCastsSpell (card_definition.rs:3025) and WheneverYouCastSpell (:3117) carry a `spell_type_filter: Option<Vec<CardType>>`. 'Whenever a player casts an instant or sorcery spell' is exactly the union of the two, expressible as two Triggered abilities each with spell_type_filter Some([Instant, Sorcery]) and DrawCards to Controller. Real authoring work, not an upgrade — the clause is currently absent from the def.
- **Sun Titan** (`sun_titan.rs`) — The note's blocker is false on every link: TriggerCondition::WhenAttacks exists (card_definition.rs, hash.rs:5163), has a builder arm (replay_harness.rs:2327), and is used by ~15 defs (hero_of_bladehold.rs:21, grave_titan.rs:49, world_shaper.rs:19). The ETB trigger at sun_titan.rs:19-38 can simply be duplicated with WhenAttacks. NOT an upgrade: even then the 'you may return' optionality is unexpressible (Effect::Choose is non-interactive), so the def would still force the return.

## WheneverCreatureDies filter / controller / exclude_self (PB-N)  (6)

- **Athreos, God of Passage** (`athreos_god_of_passage.rs`) — Stated blocker ('no mechanic for opponent paying life as an alternative to an effect') is FALSE: Effect::MayPayOrElse { cost, payer: PlayerTarget, or_else } exists (card_definition.rs:1703-1707) with a targetable payer, and Cost::PayLife exists. Residual gaps are different from what the note says: (a) death_filter offers controller_you / controller_opponent (abilities.rs:4388-4392) but NOT owner scoping, and Athreos says 'creature you OWN' — controller_you diverges under gain-control in multiplayer; (b) ENGINE-FINDING: effects/mod.rs:3196 `Effect::MayPayOrElse { or_else, .. }` destructures away `cost` and `payer` and unconditionally executes or_else — the payer never pays. Deliberate M9+/M10 deterministic stub per the inline comment, but `payer` is a silently-unread field on a shipped Effect variant, so any card authored against it gets 'opponent never pays' semantics. Indestructible + the devotion-gated type-change static are correctly authored.
- **Boggart Shenanigans** (`boggart_shenanigans.rs`) — Note claims 'WheneverCreatureDies is overbroad... No filtered variant exists for whenever a creature of subtype X you control dies. Deferred until WheneverCreatureDiesWithFilter or similar is available.' That variant SHIPPED as fields on the existing trigger (:3048): controller: Option<TargetController>, exclude_self: bool, and filter: Option<TargetFilter> (added by PB-N explicitly for 'Whenever a Vampire you control dies'). 'Another Goblin you control dies' = controller: Some(You), exclude_self: true, filter: has_subtype Goblin. Targeted damage also available via Triggered.targets + TargetPlayerOrPlaneswalker.
- **Kalastria Highborn** (`kalastria_highborn.rs`) — Every clause of the note is false. (1) 'trigger with controller + subtype filter' — TriggerCondition::WheneverCreatureDies { controller, exclude_self, nontoken_only, filter: Option<TargetFilter> } exists at card_definition.rs:3048, and its own docs name 'Whenever a Vampire you control dies (Teysa Orzhov Scion)' as the use case; chain verified: builder threads death_filter, dispatch applies it at rules/abilities.rs:4386 using the pre-death controller (CR 603.10a). exclude_self: false gives the inclusive 'this creature or another' reading. (2) 'optional mana payment at resolution' — Effect::MayPayThenEffect { cost, payer, then } shipped in PB-AC2 (card_definition.rs:1714, CR 118.12) and is exactly 'you may pay {B}. If you do, ...'; the shipped precedent leaf_crowned_visionary.rs:39 uses it for the identical shape and is marked Complete. Rewiring: Triggered { trigger_condition: WheneverCreatureDies { controller: Some(You), exclude_self: false, nontoken_only: false, filter: Some(TargetFilter { has_subtype: Some(SubType("Vampire")), ..default }) }, effect: MayPayThenEffect { cost: Cost::Mana({B}), payer: Controller, then: Sequence[LoseLife{DeclaredTarget{0},2}, GainLife{Controller,2}] }, targets: vec![TargetRequirement::TargetPlayer] }. Not upgraded here only because the def body is vec![] — this is authoring work, not marker lag.
- **Patron of the Vein** (`patron_of_the_vein.rs`) — The note's gap 1 is FALSE: TriggerCondition::WheneverCreatureDies carries controller: Option<TargetController> (card_definition.rs:3048-3049) plus exclude_self, nontoken_only, and a characteristics filter (PB-N) — so 'whenever a creature an opponent controls dies' is directly expressible as controller: Some(TargetController::Opponent); it is NOT overbroad and the cited KI-5 no longer applies. The '+1/+1 counter on each Vampire you control' half is expressible via AddCounter over EffectTarget::AllPermanentsMatching. Gap 2 ('exile it') is the only open question: EffectTarget::TriggeringCreature exists, but whether ExileObject resolves it against the already-dead object in the graveyard must be verified by the author before this is marked Complete.
- **Prowess of the Fair** (`prowess_of_the_fair.rs`) — The note's stated blocker is false on every clause: TriggerCondition::WheneverCreatureDies carries `controller: Option<TargetController>`, `exclude_self: bool`, `nontoken_only: bool` AND `filter: Option<TargetFilter>` (card_definition.rs:3048-3062, filter added by PB-N), so subtype + nontoken + controller + another are all expressible. NOT an upgrade: (a) 'you may create' hits the known Choose/MayPayOrElse non-interactivity, so wiring it would create the token unconditionally; (b) oracle (MCP) says 'another nontoken Elf', which includes noncreature Elves, and WheneverCreatureDies is creature-only — a genuine narrowing. Also flagging a type-line bug: oracle type is 'Kindred Enchantment — Elf' but the def uses types_sub(&[Enchantment], &["Elf"]) and omits the Kindred card type.
- **Spiteful Banditry** (`spiteful_banditry.rs`) — The note's sole blocker — "the 'only once each turn' per-turn throttle is not expressible in the current DSL" — is FALSE. AbilityDefinition::Triggered carries once_per_turn: bool (this very def sets it on line 17), and PB-AC1 wired the gate in rules/abilities.rs:6521 ('CR 603.2c/603.2h (PB-AC1): once-per-turn gate'). TriggerCondition::WheneverCreatureDies takes controller: Option<TargetController> (card_definition.rs:3048), covering 'creatures your opponents control'. The batch 'one or more ... die' semantics is neutralized by once_per_turn: true. Not an upgrade — the second ability is still absent from the def.

## Dynamic P/T via LayerModification::ModifyBothDynamic / ModifyPowerDynamic  (5)

- **Aspect of Hydra** (`aspect_of_hydra.rs`) — Note claims 'LayerModification::ModifyBothDynamic(EffectAmount) ... does not exist'. It DOES: declared at continuous_effect.rs:430, substituted at resolution in effects/mod.rs:3008 (CR 608.2h), applied at layers.rs:1269, hashed at hash.rs:1929, and covered by tests/primitives/primitive_pb_x.rs:455-541 (PB-X). EffectAmount::DevotionTo(Color) resolves at effects/mod.rs:6772 and layers.rs:1734. The whole card is one clause and is now expressible as ApplyContinuousEffect { ModifyBothDynamic { amount: DevotionTo(Green), negate: false }, filter: DeclaredTarget{0}, UntilEndOfTurn }. NOT upgradable as written — the def is Effect::Nothing, i.e. a castable 1-mana instant that does nothing.
- **Craterhoof Behemoth** (`craterhoof_behemoth.rs`) — Note claims 'LayerModification::ModifyBoth takes a fixed i32, not an EffectAmount. Needs LayerModification variant with dynamic amount, or snapshot-at-resolution pattern.' Both exist: LayerModification::ModifyBothDynamic { amount: Box<EffectAmount>, negate } (card-types/src/state/continuous_effect.rs) and the snapshot-at-resolution pattern is implemented — effects/mod.rs:3008-3012 substitutes ModifyBothDynamic -> ModifyBoth(v) at execute_effect time, explicitly 'so X is locked in at resolution (not re-evaluated later when creatures enter/leave)' (comment at effects/mod.rs:2990-2992), which is exactly CR 608.2h for this card. Mass grant filter EffectFilter::CreaturesYouControl and LayerModification::AddKeywords(Trample) both exist and are applied at layers.rs:1168-1172. NOT an upgrade: the def has NO ETB trigger at all (abilities is Haste only), so the whole ETB clause is unimplemented — this is real authoring work.
- **Devilish Valet** (`devilish_valet.rs`) — Note claims 'no LayerModification for power doubling'. Doubling is expressible as add-current-power: LayerModification::ModifyPowerDynamic { amount: EffectAmount::PowerOf(EffectTarget::Source), negate: false } inside Effect::ApplyContinuousEffect is substituted to a concrete ModifyPower(v) at resolution (effects/mod.rs:3013-3017), which is exactly CR 608.2h lock-in — so P becomes 2P and repeated triggers compound correctly. EffectAmount::PowerOf resolves through layer-resolved P/T (effects/mod.rs:6643-6649, 'CR 613.1: Use layer-resolved P/T'). The trigger also exists: TriggerCondition::WheneverCreatureEntersBattlefield { filter, exclude_self: true } (card_definition.rs:3079-3085) whose doc names the 'another ... you control' case explicitly (enforced at collect_triggers_for_event). NOT an upgrade: the def has no trigger at all — Trample/Haste only.
- **Empyrial Plate** (`empyrial_plate.rs`) — Note claims 'LayerModification::ModifyBoth takes fixed i32, not EffectAmount. Needs dynamic LayerModification.' FALSE — LayerModification::ModifyBothDynamic { amount: Box<EffectAmount>, negate: bool } exists (continuous_effect.rs:430-433) with a live, continuously re-evaluating arm at layers.rs:1269-1283 (CR 611.3a, routed through resolve_cda_amount). 'For each card in your hand' is EffectAmount::HandSize { player: PlayerTarget } (card_definition.rs:2727). EffectFilter::AttachedCreature exists and is used by paradise_mantle.rs:16-18. So an AbilityDefinition::Static { ContinuousEffectDef { layer: PtModify, modification: ModifyBothDynamic { HandSize { Controller } }, filter: AttachedCreature, duration: WhileSourceOnBattlefield } } is the shape. ENGINE-FINDING: layers.rs:1270-1275 resolves the amount using the controller of the OBJECT BEING MODIFIED (the equipped creature), not the source Equipment's controller — so 'your hand' would read the creature's controller's hand. Identical in normal play, wrong under gain-control (e.g. Threaten on an equipped creature). Author should verify which controller the registration threads before flipping to Complete.
- **The Meathook Massacre** (`the_meathook_massacre.rs`) — Note says 'requires LayerModification::ModifyBothDynamic(EffectAmount) ... LayerModification currently only accepts i32 (fixed), not EffectAmount.' ModifyBothDynamic { amount: Box<EffectAmount>, negate: bool } exists at continuous_effect.rs:430, with an explicit 'negate=true produces -X' contract and a documented Effect::ApplyContinuousEffect spell path that substitutes to ModifyBoth(v) at resolution (CR 608.2h). EffectAmount::XValue and EffectFilter::AllCreatures both exist. RISK for the rewiring worker: the doc-comment warns XValue 'would read 0' if substitution is skipped, and this is an ETB trigger on a permanent, not the spell itself — verify the X paid is available in the trigger's resolution context before flipping to Complete.

## Targeting shape (UpToN, mode_targets, TargetAny, declared-target player filters)  (5)

- **Cloud of Faeries** (`cloud_of_faeries.rs`) — The note says 'multi-target untap with land filter not expressible'. TargetRequirement::UpToN { count, inner } exists (card_definition.rs:2798-2801, PB-T, hash disc 17) and its doc gives the exact pattern ('Destroy up to two target creatures' → UpToN{count:2, inner:TargetCreature}); DestroyAndReanimate's doc (1405) shows the multi-slot addressing convention [DeclaredTarget{0}, DeclaredTarget{1}, ...]. It is already used inside a triggered ability, not just spells — gilded_drake.rs:42 puts UpToN on an ETB trigger, and teferi_time_raveler.rs:54 uses it with a filter. So `WhenEntersBattlefield` + targets: [UpToN{2, TargetLand}] + Sequence[UntapPermanent{DT0}, UntapPermanent{DT1}] is expressible today. NOT an upgrade — no ETB ability exists in the def at all. Flying and Cycling {2} are implemented.
- **Insatiable Avarice** (`insatiable_avarice.rs`) — The note's exact claim ('the DSL has no way to attach a per-mode TargetRequirement to an individual Spree mode') is FALSE. ModeSelection.mode_targets: Option<Vec<Vec<TargetRequirement>>> exists (card_definition.rs:3668) and is fully wired: cast-time announce/validate at casting.rs:3627-3696, resolution at resolution.rs:443. The def sets mode_targets: None (line 58) and leaves mode 1 as Effect::Sequence(vec![]) — a paid {B}{B} mode that does nothing. Rewiring: mode_targets = Some(vec![vec![], vec![TargetRequirement::TargetPlayer]]), mode 1 effect = Sequence[DrawCards{DeclaredTarget,3}, LoseLife{DeclaredTarget,3}], keep Spell.targets empty (author invariant, casting.rs:3684). ENGINE-FINDING: final_showdown.rs is cited in this def's comment as sharing the same gap — it likely carries the same stale note and should be swept too.
- **Niv-Mizzet, the Firemind** (`niv_mizzet_the_firemind.rs`) — Oracle (verified): 'Whenever you draw a card, Niv-Mizzet deals 1 damage to any target.' The def instead uses Effect::ForEach{ over: EachOpponent, DealDamage 1 }, which in a 4-player game deals 1 damage to EVERY opponent on every draw — 3x the damage, to players who were never targeted, and untargetable/unfizzleable. TargetRequirement::TargetAny plainly exists and the sibling def niv_mizzet_parun.rs:37 uses exactly it for the identical oracle clause. MARKER KIND IS ALSO WRONG: this is not a partial (missing clause) but a known_wrong (wrong game state) until rewired.
- **Polymorphist's Jest** (`polymorphists_jest.rs`) — Note's stated blocker is false: TargetRequirement::TargetPlayer exists. LayerModification::SetCreatureTypes/SetColors/RemoveAllAbilities/SetPowerToughness all exist for the layer 4/5/6/7b work. Remaining question is whether an EffectFilter can scope to creatures controlled by declared-target-0 (EffectFilter::CreaturesControlledBy / ControlledBy exist; I did not verify their parameter accepts a declared target) — that is the one link a worker must trace. Def is Effect::Nothing, so it does nothing today (safe, but the spell is castable and blank).
- **Rewind** (`rewind.rs`) — Note says the 'up to' choice is missing. TargetRequirement::UpToN { count: u32, inner: Box<TargetRequirement> } EXISTS (card_definition.rs:2798-2801), which is exactly the 'up to four lands' shape (UpToN{4, TargetLand}). CounterSpell half is already wired (lines 15-19). Medium confidence: I did not trace how UpToN's variable target count maps onto DeclaredTarget indices for a Sequence of UntapPermanent, which is the remaining authoring question — verify that chain before flipping.

## Static / continuous grant with a filtered creature set  (5)

- **Dawn's Truce** (`dawns_truce.rs`) — Note says 'Gift mechanic draw + hexproof/indestructible continuous effects complex for DSL'. All three pieces now exist: AbilityDefinition::Gift { gift_type: GiftType } (card_definition.rs:866, doc explicitly covers the instant/sorcery 'If this spell's gift cost was paid, [effect]' shape), Condition::GiftWasGiven, Effect::GrantPlayerProtection { player, qualities, duration } for the 'you gain hexproof' half (dispatched at effects/mod.rs:5303), and Effect::ApplyContinuousEffect + AddKeyword(Hexproof/Indestructible) + EffectFilter::ControlledBy + EffectDuration::UntilEndOfTurn for 'permanents you control'. NOT an upgrade: the def is AbilityDefinition::Keyword(KeywordAbility::Gift) only — a bare marker with no cost def and no Spell effect at all (this is the KI-6 dual-def shape). Real authoring work.
- **Elvish Dreadlord** (`elvish_dreadlord.rs`) — Note claims 'WhenDies trigger targeting all non-Elf creatures (EffectTarget with subtype exclusion filter) not in DSL'. FALSE: EffectFilter::AllCreaturesExcludingSubtype(SubType) exists (continuous_effect.rs:225), has a live matcher at layers.rs:969-972 (CR 613.1f), and the exact -N/-N shape ships today in eyeblight_massacre.rs:16-22 (ModifyBoth(-2) + AllCreaturesExcludingSubtype(Elf), Layer PtModify, UntilEndOfTurn). TriggerCondition::WhenDies exists. SECOND, UNSTATED blocker found: Encore is only a bare Keyword marker (def line 18) with no cost ability — the encore cost lives in AbilityDefinition::AltCastAbility { kind: AltCostKind::Encore, cost } (card_definition.rs:414-423; AltCostKind::Encore at state/types.rs:166), so Encore {5}{B}{B} is currently inert (KI-6 dual-def). Both are authoring work, not DSL gaps.
- **Sarkhan Vol** (`sarkhan_vol.rs`) — Note says 'ApplyContinuousEffect to all creatures you control not wired to DSL', but EffectFilter::CreaturesYouControl exists and is dispatched in rules/layers.rs, and the SAME FILE already uses Effect::ApplyContinuousEffect with LayerModification::AddKeywords in the -2 ability (lines 40-50). The +1 ('Creatures you control get +1/+1 and gain haste until end of turn') is expressible as two ApplyContinuousEffect defs (EffectLayer::PtModify + LayerModification::ModifyBoth(1); EffectLayer::Ability + AddKeywords([Haste])) over EffectFilter::CreaturesYouControl, EffectDuration::UntilEndOfTurn. The def leaves it as Effect::Nothing (line 25) — real authoring work, not an upgrade.
- **Scion of Draco** (`scion_of_draco.rs`) — The note's 'color-conditional keyword grant not supported in card DSL' is FALSE. EffectFilter::CreaturesYouControlWithColor(color) exists AND is dispatched in rules/layers.rs:824 (checks obj_zone == Battlefield and CardType::Creature, then color). So 'Each creature you control has vigilance if it's white, hexproof if it's blue, lifelink if it's black, first strike if it's red, trample if it's green' is authorable today as five AbilityDefinition::Static entries (EffectLayer::Ability, AddKeyword, CreaturesYouControlWithColor(c), WhileSourceOnBattlefield). Flying and the domain cost reduction (self_cost_reduction: SelfCostReduction::BasicLandTypes{per: 2}, line 25) are already correct. Not an upgrade — the static is not written yet.
- **Sorin, Lord of Innistrad** (`sorin_lord_of_innistrad.rs`) — The note claims 'EmblemSpec static_effects DSL not yet wired to produce a working lord-style effect from an emblem'. FALSE: Effect::CreateEmblem carries static_effects: Vec<ContinuousEffectDef> (card_definition.rs:2118-2122) and effects/mod.rs:4994 registers each as an Indefinite ContinuousEffect; kaito_bane_of_nightmares.rs:40-43 already ships exactly this pattern (a Layer 7c PtModify lord-style emblem). The -2 needs a CreateEmblem with a PtModify/ModifyPower(1) over EffectFilter::CreaturesYouControl. Not an upgrade: the def currently has -2 => Effect::Nothing (line 51), an activatable no-op that silently burns 2 loyalty for nothing — a wrong game state, so the rewiring is a priority.

## LayerModification::AddManaAbility (PB-S)  (5)

- **Elven Chorus** (`elven_chorus.rs`) — Note claims 'Creatures you control have "{T}: Add one mana of any color."' requires GrantActivatedAbility support that does not exist. FALSE — the identical oracle clause is fully implemented today by enduring_vitality.rs:21-35: AbilityDefinition::Static { ContinuousEffectDef { layer: EffectLayer::Ability, modification: LayerModification::AddManaAbility(ManaAbility { requires_tap: true, any_color: true, .. }), filter: EffectFilter::CreaturesYouControl, duration: WhileSourceOnBattlefield } }. LayerModification::AddManaAbility is in the current enum (continuous_effect.rs). Clauses 1+2 (look at top / cast creature spells from top) are already implemented at def lines 18-25 via StaticPlayFromTop (PB-A). Copying the enduring_vitality block should reach Complete.
- **Song of Freyalise** (`song_of_freyalise.rs`) — The note's sole blocker ('blocked on Saga chapter framework (CR 714 lore counters / chapter triggers)') is FALSE. AbilityDefinition::SagaChapter exists; lore-counter addition is wired at rules/turn_actions.rs:377, chapter effect resolution at rules/resolution.rs:1964 and :1990, sacrifice-after-final at rules/sba.rs:815; and two defs already use it (urzas_saga.rs, binding_the_old_gods.rs). The note itself concedes the grant primitives (AddManaAbility, UntilYourNextTurn) exist. Not an upgrade — abilities is vec![], so all three chapters need authoring. Author should confirm SagaChapter supports a shared 'I, II' multi-chapter entry.
- **Tyvar Kell** (`tyvar_kell.rs`) — BOTH blockers named in the def's header comment are stale. (1) 'No LayerModification::AddManaAbility exists in the DSL' is FALSE — it exists at continuous_effect.rs:355 (`AddManaAbility(ManaAbility)`), is hashed at hash.rs:1923 ('PB-S: grants a single mana ability to matching permanents in Layer 6, CR 605.1a/613.1f'), and is exercised end-to-end in tests/primitives/pb_ac7_card_integration.rs:601-614. With EffectFilter::CreaturesYouControlWithSubtype(Elf) + EffectDuration::WhileSourceOnBattlefield, 'Elves you control have "{T}: Add {B}"' is directly authorable today. (2) 'WheneverYouCastSpell has no subtype filter; Elf is a subtype' is also FALSE — `spell_subtype_filter: Option<Vec<SubType>>` exists at card_definition.rs:3131-3136 and its own doc cites 'Whenever you cast an Elf spell (Leaf-Crowned Visionary)'. Real remaining blocker: only the emblem's 'it gains haste until end of turn' (granting a keyword to a spell on the stack). Oracle verified via MCP. Card cannot reach Complete, so this is worklist rewiring, not an upgrade.
- **Urza's Saga** (`urzas_saga.rs`) — Note: 'Chapter I/II need "this Saga gains [ability]" (continuous effect granting activated ability)'. STALE for chapter I and for the granting mechanism generally: LayerModification::AddManaAbility exists (continuous_effect.rs:355, PB-S; hashed hash.rs:1923 citing CR 605.1a/613.1f; exercised in tests/primitives/pb_ac7_card_integration.rs) and LayerModification::AddActivatedAbility exists — with EffectFilter::Source + EffectDuration::WhileSourceOnBattlefield, chapter I's 'This Saga gains "{T}: Add {C}"' is directly authorable today. Chapter II's grant mechanism is likewise available; what remains blocked there is the granted ability's TOKEN, a 0/0 Construct with a CDA ('gets +1/+1 for each artifact you control') — TokenSpec takes fixed power/toughness (see the Elf Warrior spec at tyvar_kell.rs:71-85), so the CDA cannot ride on the token. Oracle verified via MCP; type line 'Enchantment Land — Urza's Saga' matches. ENGINE-FINDING: chapters I and II are `Effect::GainLife { amount: Fixed(0) }` placeholders (lines 20-23, 30-33) — KI-10 no-ops that make the chapters look implemented; they should be Effect::Nothing at minimum. Minor: chapter III uses max_cmc: Some(1) for 'mana cost {0} or {1}', which is mana VALUE not mana COST (CR 202.3 vs 202.1) — diverges on e.g. an {X} artifact.
- **Wrenn and Realmbreaker** (`wrenn_and_realmbreaker.rs`) — The marker's stated blocker — 'Granting arbitrary mana abilities to permanents is a complex DSL pattern (AnyColor mana production from lands). Known DSL gap' — is FALSE. LayerModification::AddManaAbility(ManaAbility) exists (continuous_effect.rs:355, PB-S), is applied in layers.rs:1193 (append-only per the Chromatic Lantern 2018-10-05 ruling), and its own doc comment names Chromatic Lantern / Cryptolith Rite as users. chromatic_lantern.rs implements the IDENTICAL clause 'Lands you control have "{T}: Add one mana of any color"' as Static + AddManaAbility(ManaAbility { requires_tap: true, any_color: true, .. }) + EffectFilter::LandsYouControl, and it carries NO completeness marker (i.e. it is Complete). NOT upgrade-to-complete for two independent reasons: (a) the static is simply absent from `abilities` (TODO at line 26); (b) the -2 loyalty ability is `Effect::Sequence(vec![])` — a silently no-op ability the player can still activate to move loyalty, which is wrong game state, and the marker note does not mention it at all. The +1 and -7 are implemented.

## Alt-cost / keyword-cost defs that shipped (Madness, Escape, Gift, Saga, Eminence, Equip, ExileFromHand)  (4)

- **Leyline of Abundance** (`leyline_of_abundance.rs`) — The note's sole blocker is false: `AbilityDefinition::OpeningHand` exists (card_definition.rs:384), is classified Handled at crates/engine/src/rules/engine.rs (ability_definition_registry.rs:118), is implemented in start_game per CR 113.6b (engine.rs:1878-1882), and is already used by a shipped def (leyline_of_the_void.rs). The def implements the other two clauses (WhenTappedForMana{Creature} → AddMana{G}; {6}{G}{G} ForEach EachCreatureYouControl AddCounter) but simply lacks the OpeningHand ability. Not upgrade-to-complete: the clause is absent from the def body. Adding `AbilityDefinition::OpeningHand` should complete it (caveat: engine places it unconditionally; oracle says 'you may').
- **Simian Spirit Guide** (`simian_spirit_guide.rs`) — Note claims Cost::ExileFromHand does not exist. STALE: Cost::ExileFromHand { color } exists on the Cost enum and IS handled in the activation cost path at casting.rs:4168. CRITICAL — the def is not merely incomplete, it is actively wrong: it ships AbilityDefinition::Activated { cost: Cost::Mana(ManaCost::default()), effect: AddMana{red:1} } (lines 18-29), a ZERO-cost, untapped, unlimited 'Add {R}' — infinite red mana from the battlefield. Correct wiring is cost: Cost::ExileFromHand + activation_zone: Some(Hand). Until rewired the marker should be known_wrong, not partial.
- **Stensia Masquerade** (`stensia_masquerade.rs`) — The note's sole blocker ('Madness {2}{R} — AltCostKind::Madness not in DSL') is FALSE: AbilityDefinition::Madness { cost: ManaCost } exists at card_definition.rs:508, and the AbilityDefinition inventory lists Madness among its 68 variants. The def's other two clauses are already implemented (Static AddKeyword(FirstStrike) over AttackingCreaturesYouControl; WheneverCreatureYouControlDealsCombatDamageToPlayer with a Vampire filter -> AddCounter on TriggeringCreature). NOT upgrade-to-complete: the Madness clause is genuinely absent from abilities, so it needs authoring — add AbilityDefinition::Madness { cost: ManaCost { generic: 2, red: 1, .. } } plus the Keyword(KeywordAbility::Madness) presence marker (the DSL doc requires both).
- **Woe Strider** (`woe_strider.rs`) — Note says 'needs Escape ETB counter replacement effect'. STALE — AbilityDefinition::EscapeWithCounter exists (card_definition.rs:521), is classified Handled in the ability_definition_registry (:182, :471), is consumed at resolution.rs:853, and has integration tests (tests/mechanics_e_l/escape.rs:141, :790 asserts the +1/+1 counter). The def just does not carry it. NOT upgrade-to-complete for a second reason the note misses: oracle is 'Sacrifice ANOTHER creature: Scry 1', but the def uses Cost::Sacrifice with a bare creature filter (lines 45-48), and Cost::Sacrifice cannot exclude the source (see wight_of_the_reliquary evidence — eligible_sacrifice_targets takes no source id), so Woe Strider can illegally sacrifice itself. Inconsistent with wight_of_the_reliquary.rs and vampire_gourmand.rs, which omit the ability for exactly this reason.

## Other / one-off  (3)

- **Alexios, Deimos of Kosmos** (`alexios_deimos_of_kosmos.rs`) — The note leads with two shipped primitives: GameRestriction::CantBeSacrificed and GameRestriction::CantAttackOwner both exist (PB-AC8) and AbilityDefinition::StaticRestriction is the carrier — the def has neither wired (abilities ends at MustAttackEachCombat, line 30). That is real authoring work. But the card stays partial afterwards: the upkeep clause is genuinely blocked — TriggerCondition has AtBeginningOfEachUpkeep, yet 'THAT PLAYER gains control' needs the upkeep player as the GainControl recipient, and PlayerTarget has no active/upkeep-player variant (TriggeringPlayer is set from PendingTrigger::triggering_player on discard/draw triggers, not turn-based upkeep events). Do not upgrade.
- **Disciple of Freyalise // Garden of Freyalise** (`disciple_of_freyalise.rs`) — Note 'no abilities implemented' names no blocker, and the whole card is now expressible: Effect::MayPayThenEffect{cost, payer, then} (:1708, CR 118.12 'you may X. If you do, Y') + Cost::Sacrifice(TargetFilter) + EffectAmount::PowerOfSacrificedCreature (:2611) — the last of which exists precisely for this 'X is that creature's power' shape and uses the LKI on-battlefield value. Author must also add the Garden of Freyalise back face (MDFC land).
- **Voice of Victory** (`voice_of_victory.rs`) — Note says the stax restriction is 'not in DSL'. STALE — full chain verified: GameRestriction::OpponentsCantCastDuringYourTurn exists (card-types/src/state/stubs.rs:565), is enforced in casting.rs:6558 and in simulator/legal_actions.rs:988, has tests (tests/rules/restrictions.rs:287), and dragonlord_dromoka.rs:33 already ships it via AbilityDefinition::StaticRestriction. The def just does not use it (TODO at line 47). Mobilize 2 is already implemented (tapped + enters_attacking + sacrifice_at_end_step tokens, lines 18-46), so adding the StaticRestriction should make this card Complete — but that is authoring work, not this sweep.

## PlayerTarget::ControllerOf(TriggeringCreature) / TriggeringPlayer  (3)

- **Blood Seeker** (`blood_seeker.rs`) — Note's central claim — 'PlayerTarget lacks triggering player reference' — is flatly STALE: PlayerTarget::TriggeringPlayer exists (:2473), as does ControllerOf(Box<EffectTarget>) (:2469). WheneverCreatureEntersBattlefield takes filter: Option<TargetFilter> (:3079) and TargetFilter.controller: TargetController::Opponent (:2831) expresses 'a creature an opponent controls'. Only open question for the author is the 'you may' clause — see ENGINE-FINDING on Effect::Choose in duelists_heritage/global notes.
- **Edric, Spymaster of Trest** (`edric_spymaster_of_trest.rs`) — Oracle (MCP): 'Whenever a creature deals combat damage to one of your opponents, its controller may draw a card.' The note says correct resolution 'requires PB-37 DamagedPlayer/TriggeringCreature target support' — that SHIPPED. EffectTarget::TriggeringCreature exists at card_definition.rs:2442-2446 and its doc names this very card: "Used by ... 'its controller may draw a card' (Edric)". PlayerTarget::ControllerOf is present in the 8-variant PlayerTarget list. The current def (:26-29) uses PlayerTarget::Controller, so in multiplayer EDRIC's controller draws whenever ANY player's creature connects — a legal-but-wrong multiplayer bug live in the def today, and the strongest reason to rewire this one early. NOT upgradeable even after rewiring: 'MAY draw' optionality has no correct expression (audit-brief item 2; Effect::Choose always takes choices.first(), effects/mod.rs:3190), so the card stays partial.
- **Fecundity** (`fecundity.rs`) — Note claims 'needs ControllerOf(dying creature) as PlayerTarget'. That primitive SHIPPED and the full chain is traceable for this exact trigger: PlayerTarget::ControllerOf(Box<EffectTarget>) at card_definition.rs:2466; EffectTarget::TriggeringCreature at :2473; AnyCreatureDies pushes PendingTrigger with entering_object_id = dying creature at abilities.rs:4446; abilities.rs:7758 copies it to stack_obj.triggering_creature_id; resolution.rs:2109/2199 copies it into ctx.triggering_creature_id; effects/mod.rs:6260 resolves TriggeringCreature and :6333 resolves ControllerOf. Real rewiring work, NOT an upgrade: (a) 'may draw' is still inexpressible (see brief finding #2), and (b) move_object_to_zone resets the graveyard object's controller to owner (documented at abilities.rs:4358-4359), so ControllerOf(TriggeringCreature) yields OWNER not the pre-death controller — a CR 603.10a LKI deviation that matters in gain-control multiplayer. Meanwhile the def is actively wrong game state: Fecundity's own controller draws on EVERY creature death regardless of who controlled it; the marker would be better as known_wrong until rewired.

## Type-system additions that shipped (CardType::Battle, AddAllCreatureTypes, AddSubtypes, EntersWithCounters)  (3)

- **Golgari Findbroker** (`golgari_findbroker.rs`) — Note claims 'Battle is not in the CardType enum' — FALSE. CardType::Battle exists at card-types/src/state/types.rs:37. Oracle is 'target permanent card from your graveyard'; the def's has_card_types list (golgari_findbroker.rs:29-35) enumerates only Creature/Artifact/Enchantment/Land/Planeswalker, so a Battle card in the graveyard is not a legal target. Fix is to add CardType::Battle to the list — has_card_types has OR semantics and is checked by matches_filter, reached for TargetCardInYourGraveyard via casting.rs:6312.
- **Maskwood Nexus** (`maskwood_nexus.rs`) — Note claims "DSL lacks a 'grant all subtypes / changeling' layer modification for permanents you control" — STALE. LayerModification::AddAllCreatureTypes exists and is applied in layers.rs:1106-1108 (iterating ALL_CREATURE_TYPES), hashed at hash.rs:1903, and the layers.rs comment at :1104-1105 literally names 'Maskwood Nexus-style "is every creature type" continuous effects' as the use case. Pair it with EffectFilter::CreaturesYouControl + EffectDuration::WhileSourceOnBattlefield. Card is NOT Complete after rewiring: the second sentence ('the same is true for creature spells you control and creature cards you own that aren't on the battlefield') has no EffectFilter reaching stack/hand/library/graveyard objects.
- **Olivia Voldaren** (`olivia_voldaren.rs`) — The note says 'AddSubtype LayerModification may not exist' — LayerModification::AddSubtypes is present in the current enum. The {1}{R} ability appears expressible today as Effect::Sequence([DealDamage to DeclaredTarget{0}, ApplyContinuousEffect{AddSubtypes(Vampire), filter DeclaredTarget, duration Indefinite}, AddCounter{target: Source, PlusOnePlusOne, 1}]) with targets: vec![TargetCreatureWithFilter(TargetFilter{exclude_self: true, ..})] for 'another target creature'. Flagged needs-rewiring rather than upgrade: the ability is entirely absent from the def and the ApplyContinuousEffect-on-DeclaredTarget path should be verified by the author.

## X spells (ManaCost.x_count + EffectAmount::XValue)  (1)

- **Exsanguinate** (`exsanguinate.rs`) — Note claims 'EffectAmount::XValue not wired for DrainLife'. FALSE — Effect::DrainLife { amount } resolves through the generic resolver: `let loss = resolve_amount(state, amount, ctx).max(0) as u32;` at effects/mod.rs:544-546, and XValue is an EffectAmount handled by resolve_amount (ctx.x_value). The note's implicit second claim — that {X} cannot be declared — is also false: ManaCost has an `x_count` field, used today by goblin_negotiation.rs:10 ({X}{R}{R} -> `ManaCost { red: 2, x_count: 1 }` with EffectAmount::XValue at line 18) and cut_ribbons.rs:42+48 ({X}{B}{B} -> x_count: 1 with LoseLife { amount: XValue }). The def's own mana_cost is currently `{black: 2}` with x_count omitted, so it is ALSO mis-costed as a plain {B}{B}. Both oracle clauses ('each opponent loses X life' + 'you gain life equal to the life lost this way') are exactly Effect::DrainLife's semantics. This looks like a clean full implementation once rewired.

---

## NOT READY — blocked on non-interactive choice (1)

Listed for completeness, **do not pick these up**. Their stated blocker is stale but
they depend on `Effect::Choose` / `MayPayOrElse` optionality, which is a stub (EF-3).

- **By Invitation Only** (`by_invitation_only.rs`) — The primary blocker (interactive 'choose a number between 0 and 13') is REAL — no Effect/Cost expresses a player-chosen numeric value, and EffectAmount has no interactive variant. BUT the def is ALSO wrong in a way the note does not mention and which IS now fixable: oracle says 'each player sacrifices that many CREATURES of their choice', and the def passes `filter: None` (by_invitation_only.rs:23), so each player sacrifices any permanent. Effect::SacrificePermanents gained `filter: Option<TargetFilter>` in PB-SFT (card_definition.rs:1733-1736), so the creature filter is now expressible. The def's own inline comment at line 18 already admits 'Creature filter applicable'. Partial rewiring available; card stays partial on the count.


# Data sync 2026-09-14 — CR 2026-08-07 + Scryfall 2026-09-14 (`scutemob-259`)

Full record for the CHANGELOG entry of the same date. Owner ask: "this should be a check at
/start which gives the option of updating, but we should sync everything to current state."

## What was stale

| Source | Was | Now |
|---|---|---|
| `.scryfall-cache/MagicCompRules.txt` | effective 2026-01-16 (fetched 2026-02-20) | effective 2026-08-07 (`MagicCompRules 20260819.txt`) |
| `.scryfall-cache/oracle-cards.json` / `rulings.json` | 2026-02-20, JSON arrays | `oracle-cards.jsonl.gz` / `rulings.jsonl.gz`, 2026-09-14 |
| `cards.sqlite` | built 2026-03-23 | 38,765 cards / 78,949 rulings / 3,162 CR rules |
| `test-data/card-fidelity/printed-fields.tsv` (SR-37, committed) | from the March sqlite | regenerated; 8 corpus rows changed |

Scryfall's bulk-data API had changed shape (`jsonl_download_uri` + `compressed_size`; the old
`download_uri` / `size` fields are gone), so `tools/scryfall-import` could not have run at all.

## CR delta, January → August (computed with the scratchpad diff, both files parsed body-only)

- 3,260 → 3,308 numbered rules: 118 added, 70 removed, 128 reworded.
- **Sections 722–732 renumbered +1**: new 722 "Preparation Cards" inserted (Scryfall layouts
  `prepare` / `front_card` are its cards). 723 Controlling Another Player … 733 Handling Illegal
  Actions. Verified a pure shift: every old 722–732 subrule's text equals its +1 successor except
  730.2j, which gained a sentence.
- Swept **378 citations in 60 files** (crates 323, docs 40, test-data 15 — two golden scripts'
  free-text metadata) with a one-pass map; `memory/` deliberately NOT swept (historical narrative).
  The four "removed" numbers that were cited (724.3, 724.4, 729.2d, 729.3a) were renumber victims,
  not deletions; they now resolve.
- Reworded rules cited by the codebase that are substantive, for the next rules pass to read:
  - **CR 603.10a** (370 cites): "abilities that trigger when a player sacrifices a permanent" added
    to the look-back list.
  - **CR 605.1a** (249 cites): a mana ability's "cost and effect don't move any card to or from a
    library"; ignore non-self replacement effects when classifying. Deathrite Shaman and Priest of
    Forgotten Gods got "(Activate only as an instant.)" reminder text — both defs are already
    non-`Complete` (known_wrong / inert), nothing deck-legal moves.
  - **CR 714.3a/b**: Saga lore-counter placement rewritten as intrinsic replacement-effect abilities.
  - **CR 702.49c** ninjutsu reworded; **CR 508.4** "remain attacking until removed from combat";
    **CR 506.4** "or protector" added to removed-from-combat.
- Pre-existing bad citations (exist in NEITHER CR): 33 numbers / 307 cites across crates+docs+
  test-data (crates alone: 24 / 110). Untouched — that is LL-2 (`scutemob-256`), whose brief now
  starts from `tools/data-freshness.py cites`.

## Card delta

- 1,722 new oracle IDs. 8 of the 1,802 corpus cards changed printed fields: Deathrite Shaman,
  Priest of Forgotten Gods, Storm-Kiln Artist (reminder text), Delney ("triggered ability"),
  Elvish Spirit Guide ("this card"), Legolas's Quick Reflexes (keyword order), Plumb the Forbidden
  ("and lose 1 life"), **Monster Manual // Zoological Study: type line "Artifact — Book"** — def
  fixed (`types_sub(&[Artifact], &["Book"])`; `SubType` is a string newtype, no wire change).
- 94 new/changed ruling texts on 43 corpus cards (Everflowing Chalice, Badgermole Cub, Inexorable
  Tide, Fathom Mage +6 each; Umezawa's Jitte +5).
- **New Scryfall layout `front_card`** (291 rows): the "(Theme color: {G})" front of a preparation
  card, `type_line` "Card", sharing its name with a real card. It shadowed **Savage Lands** in the
  fixture AND would have been the first exact-name hit in the MCP `lookup_card`. Added to the
  exclusion list in `refresh-card-fidelity-fixture.py`, `mcp-server` lookup, `generate_skeleton.py`,
  `bulk_generate.py`. Layout `prepare` (64 rows, "Creature // Sorcery") is real cards; kept.

## What shipped

- `tools/data-freshness.py` — `check` (exit 0/1/2), `refresh` (cr → scryfall → rules → fixture,
  `--only`/`--skip`), `cites` (existence-only). `.scryfall-cache/meta.json` is its ledger.
- `tools/start-check.sh` — the `/start` hook; step 4b in both copies of the start skill
  (`~/.claude/skills/start`, `.claude/skills/start` — the project copy was a stale April
  provisioning and is now identical to the global one).
- `tools/scryfall-import`: gzipped JSON-Lines via `jsonl_download_uri`, legacy array fallback,
  streaming records, 4 unit tests. `flate2` added (already in the lock).
- `mtg-mcp-server --import-only`: re-import the CR and exit. A running MCP instance reads
  `cards.sqlite` live, so no restart is needed after a refresh.
- SR-37 gate: 9/9 after the Savage Lands filter fix and the Monster Manual def fix.

## Not done / follow-ups

- LL-2 (`scutemob-256`) owns the 33 dangling numbers and the per-section coverage report.
- A rules pass over CR 603.10a / 605.1a / 714.3 / 702.49c wording vs engine behaviour is not
  filed; seeds belong in `docs/audits/decision-point-audit.md` if someone confirms a deviation.
- `memory/` still carries old 722–732 numbers in historical notes (93 cites / 34 files).

# M9.5 Fix Session Plan

4 MEDIUM findings, 0 HIGH findings. All 4 MEDIUMs fit in a single session.

## Session 1 — Replay Viewer Security & Correctness

- [x] MR-M9.5-01 (MEDIUM) — Path traversal in `POST /api/load`: reject absolute paths, require paths relative to `--scripts-dir`
- [x] MR-M9.5-02 (MEDIUM) — Battlefield permanents show raw characteristics instead of calculated (post-layer) values in `view_model.rs`
- [x] MR-M9.5-03 (MEDIUM) — Redundant `cmd.clone()` on every `process_command` call in `replay.rs` (process_command takes ownership, clone is wasted)
- [x] MR-M9.5-04 (MEDIUM) — Serialization errors silently dropped in `get_step` handler — should return 500 with error context

### Files touched
- `tools/replay-viewer/src/api.rs` (MR-M9.5-01, MR-M9.5-04)
- `tools/replay-viewer/src/view_model.rs` (MR-M9.5-02)
- `tools/replay-viewer/src/replay.rs` (MR-M9.5-03)

### Validation
- `~/.cargo/bin/cargo build --package replay-viewer`
- `~/.cargo/bin/cargo clippy --package replay-viewer -- -D warnings`
- `~/.cargo/bin/cargo test --all` (ensure no regression in engine tests)

### Completion Notes (2026-02-23)
All 4 fixes applied cleanly. All tests pass (558 passing, 0 failed). Zero clippy warnings. Formatting clean after `cargo fmt`.

MR-M9.5-01: Rejected absolute paths with 400 BAD_REQUEST; canonicalize-and-prefix-check for `..` traversal; `main.rs` binding changed from `0.0.0.0` to `127.0.0.1`. The MR-M9.5-06 LOW (server bind address) is also resolved as a side effect of this fix.

MR-M9.5-02: Added `calculate_characteristics` import, used `calc.as_ref().unwrap_or(&obj.characteristics)` pattern so we fall back to raw chars if the object somehow isn't found (defensive). The formatter collapsed the iterator chains to one-liners.

MR-M9.5-03: Introduced `snapshot_cmd = cmd.clone()` before `process_command(current_state.clone(), cmd)` in all three command-dispatching branches. The clone is now explicit for snapshot storage; `cmd` moves directly into `process_command`.

MR-M9.5-04: Replaced `unwrap_or(Value::Null)` with `map_err(...)? ` for `script_action`; replaced `and_then(...ok())` with `.map(...map_err(...)?)  .transpose()?` for `command`; replaced `filter_map(...ok())` with `map(... map_err(...)?) .collect::<Result<_,_>>()?` for `events`. All serialization failures now propagate as HTTP 500.
